\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[ngerman]{babel}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
% Linksbündige p-Spalte (verhindert Blocksatz-Löcher in schmalen Tabellenspalten)
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, fit, backgrounds,
                decorations.pathreplacing}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[colorlinks=true, allcolors=black]{hyperref}
\usepackage{longtable}

% Quelltextauszüge: bewusst sparsam eingesetzt, nur dort, wo der Code selbst
% das Argument trägt (Reward-Formel, Lösbarkeitsgarantie, Zustandsführung).
\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries,
  commentstyle=\itshape\color{gray!65!black},
  numbers=left, numberstyle=\tiny\color{gray!80!black}, numbersep=8pt,
  frame=single, rulecolor=\color{gray!45},
  breaklines=true, showstringspaces=false,
  captionpos=b, xleftmargin=1.6em, aboveskip=1.2em, belowskip=1.2em,
  literate={ä}{{\"a}}1 {ö}{{\"o}}1 {ü}{{\"u}}1
           {Ä}{{\"A}}1 {Ö}{{\"O}}1 {Ü}{{\"U}}1 {ß}{{\ss}}1
           {Φ}{{$\Phi$}}1 {β}{{$\beta$}}1 {γ}{{$\gamma$}}1
           {≥}{{$\geq$}}1 {→}{{$\rightarrow$}}1 {·}{{$\cdot$}}1 {×}{{$\times$}}1
           {—}{{---}}1 {–}{{--}}1 {„}{{\glqq}}1 {"}{{\grqq}}1,
}
\renewcommand{\lstlistingname}{Listing}

% Lange \texttt-Bezeichner (Dateinamen, Config-Schluessel, C++-Symbole) lassen sich
% nicht trennen und ragen sonst in den Rand. Notdehnung statt uebervoller Zeilen.
\setlength{\emergencystretch}{3em}
\geometry{a4paper, left=3cm, right=2.5cm, top=2.5cm, bottom=2.5cm}

% Verzeichnisse sollen im PDF-Lesezeichenbaum und im Inhaltsverzeichnis auftauchen
\usepackage[titles]{tocloft}

% Jedes Hauptkapitel beginnt auf einer neuen Seite. \clearpage ist ein No-Op,
% wenn die Seite ohnehin frisch ist, erzeugt also keine Leerseiten.
\let\sfOldSection\section
\renewcommand{\section}{\clearpage\sfOldSection}

\begin{document}

% ===================================================================
%  DECKBLATT
% ===================================================================
\begin{titlepage}
\centering
\vspace*{1cm}

\includegraphics[width=5cm]{figures/hs_aalen_logo.pdf}

\vspace{3.2cm}

{\Large\bfseries Projektarbeit\par}

\vspace{1.2cm}

\rule{\textwidth}{0.4pt}
\vspace{0.6cm}

{\huge\bfseries \emph{Stoneforge}: Ein prozedural generiertes\\[0.25cm]
2D-Spiel als Testumgebung für Reinforcement Learning\par}

\vspace{0.7cm}

{\Large Spielentwicklung, Weltgenerator und die Generalisierung\\[0.15cm]
eines rekurrenten Navigationsagenten\par}

\vspace{0.6cm}
\rule{\textwidth}{0.4pt}

\vfill

\begin{tabular}{@{}ll@{}}
\textbf{Vorgelegt von:}    & Florian Merlau \\
                           & Laurin Rößler \\[0.5cm]
\textbf{Studiengang:}      & Masterstudiengang Informatik und Security (M.\,Sc.) \\[0.5cm]
\textbf{Betreuer:}         & Prof.\ Dr.\ Carsten Lecon \\[0.5cm]
\textbf{Abgabedatum:}      & 14.\ August 2026 \\
\end{tabular}

\vspace{1.5cm}

\end{titlepage}

% ===================================================================
%  VORSPANN  (römische Seitenzahlen)
% ===================================================================
\pagenumbering{roman}
\setcounter{page}{2}

% ===================================================================
%  KURZFASSUNG
% ===================================================================
\section*{Kurzfassung}
\addcontentsline{toc}{section}{Kurzfassung}

Diese Projektarbeit verbindet zwei Themen, die auf den ersten Blick wenig
miteinander zu tun haben: die prozedurale Erzeugung von Spielwelten und
Reinforcement Learning unter partieller Beobachtbarkeit. Im ersten Teil bauen
wir mit \emph{Stoneforge} ein Spiel, dessen Generator reproduzierbare,
nachweislich lösbare zweidimensionale Welten mit frei steuerbarer
Zielentfernung erzeugt. Jede Karte ist lösbar. Jeder Seed reproduzierbar. Im
zweiten Teil stellen wir eine unbequeme Frage: Bildet ein Agent, der nur einen
kleinen lokalen Ausschnitt seiner Umgebung sieht, wirklich eine
Navigationsstrategie, oder lernt er bloß die trainierten Karten auswendig? Wir
testen ihn dort, wo es zählt: auf Welten, die er nie gesehen hat.

Der Generator erfüllt seine Anforderungen vollständig. Determinismus bis hin zu
bitidentischen Trainingspfaden, 100\,\% Lösbarkeit über 3\,150 geprüfte Seeds,
eine Zielentfernung, die am tatsächlichen Laufweg gemessen exakt eingehalten
wird. Der Agent, ein rekurrentes Verfahren mit internem Gedächtnis, erreicht
über sieben unabhängige Läufe $65{,}7\,\% \pm 12{,}4$ auf dem Testset und
$66{,}9\,\% \pm 12{,}8$ auf einem gesperrten Holdout. Die Holdoutschwelle ist
erfüllt, die Testsetschwelle verfehlt.

Diese Zahlen tragen nicht. Eine ungelernte Kompassheuristik aus vier Zeilen
Code erreicht auf derselben Aufgabe 92\,\% und ist bei vergleichbarer
Erfolgsquote zugleich dreimal wegeffizienter. Die Erfolgsquote misst hier also
nicht die Fähigkeit, um die es geht. Die Ursache liegt nicht im Agenten,
sondern im Generator: Solange die Hindernisse aus unkorreliertem Rauschen
stammen, entstehen kaum Sackgassen, und ohne Sackgassen erzwingt die Aufgabe
kein Gedächtnis. Die zweite Forschungsfrage ist mit diesem Aufbau nicht
entscheidbar.

Daraus folgt der übertragbare Beitrag. Die Schwierigkeit einer Lernaufgabe ist
eine Eigenschaft der Weltgenerierung und gehört dort spezifiziert und geprüft,
bevor ein Lernverfahren darauf angesetzt wird. Eine ungelernte Referenzpolitik
leistet genau das, in Sekunden statt in Trainingsstunden.

\vspace{0.5cm}
\noindent\textbf{Schlagwörter:} Prozedurale Weltgenerierung, Spielentwicklung,
Reinforcement Learning, POMDP, Generalisierung, RecurrentPPO,
Curriculum Learning, Baseline-Vergleich

\clearpage

% ===================================================================
%  EIDESSTATTLICHE ERKLÄRUNG
% ===================================================================
\section*{Eidesstattliche Erklärung}
\addcontentsline{toc}{section}{Eidesstattliche Erklärung}

Wir versichern hiermit, dass wir die vorliegende Projektarbeit mit dem Thema
\glqq \emph{Stoneforge}: Ein prozedural generiertes 2D-Spiel als Testumgebung
für Reinforcement Learning\grqq{} selbstständig verfasst und keine anderen als
die angegebenen
Quellen und Hilfsmittel benutzt haben. Alle Stellen, die wörtlich oder
sinngemäß aus Veröffentlichungen oder anderen Quellen entnommen sind, sind als
solche kenntlich gemacht. Die Arbeit wurde in gleicher oder ähnlicher Form
noch keiner Prüfungsbehörde vorgelegt.

Die eingesetzten generativen KI-Werkzeuge sind auf der folgenden Seite in der
\glqq Erklärung zur Nutzung generativer KI\grqq{} vollständig ausgewiesen; die
übrigen Hilfsmittel (verwendete Software und Bibliotheken, Literatur) sind an
den jeweiligen Stellen dieser Arbeit angegeben. Der vollständige Quellcode
sowie das lückenlos geführte Experiment-Changelog liegen dem
Projekt-Repository bei.

\vspace{2.5cm}

\noindent
\begin{tabular}{@{}p{6cm}p{6cm}@{}}
\hrulefill & \hrulefill \\[0.2cm]
Ort, Datum & Florian Merlau \\[3.3cm]
\hrulefill & \hrulefill \\[0.2cm]
Ort, Datum & Laurin Rößler \\
\end{tabular}

\clearpage

% ===================================================================
%  ERKLÄRUNG ZUR NUTZUNG GENERATIVER KI
%
%  TODO VOR ABGABE — unbedingt prüfen:
%  (1) Die Hochschule Aalen stellt eine eigene „Eigenständigkeitserklärung KI"
%      in drei Varianten bereit (KI nicht erwünscht / erlaubt / erforderlich).
%      WELCHE Variante gilt, legt der Betreuer fest — mit Prof. Lecon klären
%      und, falls vorhanden, den Wortlaut der Hochschulvorlage übernehmen.
%      Ebenso gibt es einen „Fragebogen zur Dokumentation der KI-Nutzung in
%      Abschlussarbeiten"; ggf. zusätzlich ausfüllen.
%  (2) Die Tabelle unten listet derzeit NUR das Werkzeug, dessen Einsatz
%      belegbar ist. Falls weitere generative KI-Werkzeuge verwendet wurden
%      (z. B. zur sprachlichen Überarbeitung, für Gliederungsvorschläge,
%      Code-Vervollständigung oder Literaturvorsortierung), MUSS je Werkzeug
%      eine Zeile ergänzt werden: Produktname/Version, Einsatzform, betroffene
%      Teile der Arbeit, Art der eigenen Prüfung.
% ===================================================================
\section*{Erklärung zur Nutzung generativer KI}
\addcontentsline{toc}{section}{Erklärung zur Nutzung generativer KI}

Bei der Erstellung dieser Arbeit wurden generative KI-Werkzeuge als Hilfsmittel
eingesetzt; Art und Umfang sind in der folgenden Tabelle offengelegt. Wir
verantworten alle daraus übernommenen Inhalte vollumfänglich selbst, haben sie
inhaltlich nachvollzogen und gegen die Primärdaten des Projekt-Repositories
verifiziert. \textbf{Nicht} eingesetzt wurde generative KI zur Erzeugung von
Messergebnissen: Sämtliche berichteten Zahlen stammen aus eigenen Trainings-
und Evaluationsläufen und sind im Experiment-Changelog sowie über Seeds,
Modell-Checkpoints und Konfigurationsdateien reproduzierbar.

\vspace{0.4cm}
\noindent\textbf{Übersicht der verwendeten KI-Werkzeuge}
\vspace{0.15cm}

\noindent
\small
\begin{tabular}{@{}L{2.7cm}L{4.5cm}L{2.5cm}L{4.4cm}@{}}
\toprule
\textbf{Werkzeug} & \textbf{Einsatzform} & \textbf{Teile der Arbeit} & \textbf{Bemerkungen} \\
\midrule
Claude (Opus 5), Anthropic
  & Vorschläge zur Prüfung des Manuskripts auf Rechen-, Zahlen- und
    Konsistenzfehler sowie zur Formulierung des Diagnoseskripts
    \texttt{reward\_bilanz.py}
  & Kapitel~\ref{sec:methodikkapitel}--\ref{sec:evaluation}
  & Jeder Vorschlag wurde von uns gegen Quellcode, \texttt{eval\_history.json}
    und \texttt{CHANGELOG.md} geprüft und erst nach eigener Verifikation
    übernommen; drei Beanstandungen erwiesen sich dabei als unzutreffend und
    wurden verworfen. Die Messwerte des Diagnoseskripts stammen aus eigenen
    Läufen. \\
\addlinespace
Claude Code (Opus 4.8), Anthropic
  & Textentwürfe und -überarbeitungen nach unseren Vorgaben (Gliederung,
    Kürzung, sprachlicher Stil); Vorschläge für Literaturbelege; Anpassungen am
    Dokumentlayout
  & Kurzfassung, Kapitel~\ref{sec:einleitung}--\ref{sec:grundlagen},
    Abschnitt~\ref{sec:methodik}, Abschnitt~\ref{sec:rlumgebung}, Abschnitte in
    Kapitel~\ref{sec:evaluation}--\ref{sec:ausblick}, Deckblatt
  & Struktur, Aussagen und alle berichteten Ergebnisse legten wir fest; Claude
    lieferte Formulierungs- und Kürzungsvorschläge sowie Belegvorschläge, die
    wir gegen \texttt{references.bib} und die Originalquellen geprüft haben.
    Layoutänderungen (Logo, Farben, Abstände) sind rein optisch und ohne
    inhaltliche Wirkung. \\
\bottomrule
\end{tabular}
\normalsize

\vspace{0.5cm}

\noindent
\begin{tabular}{@{}p{6cm}p{6cm}@{}}
\hrulefill & \hrulefill \\[0.2cm]
Ort, Datum & Florian Merlau \\[3.3cm]
\hrulefill & \hrulefill \\[0.2cm]
Ort, Datum & Laurin Rößler \\
\end{tabular}

\clearpage

% ===================================================================
%  VERZEICHNISSE
% ===================================================================
\tableofcontents
\clearpage

\listoffigures
\addcontentsline{toc}{section}{Abbildungsverzeichnis}
\clearpage

\listoftables
\addcontentsline{toc}{section}{Tabellenverzeichnis}
\clearpage

\section*{Abkürzungs- und Symbolverzeichnis}
\addcontentsline{toc}{section}{Abkürzungs- und Symbolverzeichnis}

\begin{longtable}{@{}ll@{}}
\toprule
\textbf{Kürzel} & \textbf{Bedeutung} \\
\midrule
\endhead
A2C   & Advantage Actor-Critic, synchrone Variante von A3C \\
\texttt{approx\_kl} & Maß für die Stärke einer Politikänderung pro Update \\
BFS   & \emph{Breadth-First Search}, Breitensuche; liefert den kürzesten begehbaren Weg \\
BPTT  & \emph{Backpropagation Through Time}, Lernverfahren rekurrenter Netze \\
CNN   & \emph{Convolutional Neural Network}, faltendes neuronales Netz \\
DQN   & \emph{Deep Q-Network}, wertbasiertes RL-Verfahren \\
DRQN  & \emph{Deep Recurrent Q-Network}, DQN mit Gedächtnis \\
EV    & \emph{Explained Variance}, Gütemaß der gelernten Wertfunktion \\
GAE   & \emph{Generalized Advantage Estimation}, Schätzung des Vorteils \\
HARKing & \emph{Hypothesizing After the Results are Known} \\
LSTM  & \emph{Long Short-Term Memory}, rekurrente Netzarchitektur (Gedächtnis) \\
MDP   & \emph{Markov Decision Process}, Markow-Entscheidungsprozess \\
MLP   & \emph{Multilayer Perceptron}, vorwärtsgerichtetes Netz ohne Gedächtnis \\
MPS   & \emph{Metal Performance Shaders}, GPU-Backend von Apple \\
PBRS  & \emph{Potential-Based Reward Shaping}, politikerhaltende Belohnungsformung \\
PLR   & \emph{Prioritized Level Replay}, priorisierte Level-Wiederholung \\
POMDP & \emph{Partially Observable MDP}, teilweise beobachtbarer MDP \\
PPO   & \emph{Proximal Policy Optimization}, das hier verwendete RL-Hauptverfahren \\
RL    & \emph{Reinforcement Learning}, bestärkendes Lernen \\
SB3   & \emph{Stable-Baselines3}, verwendete RL-Bibliothek \\
SR    & \emph{Success Rate}, Erfolgsquote: Anteil gelöster Welten \\
\midrule
$\gamma$      & Diskontfaktor: Gewicht künftiger Belohnungen (hier $0{,}999$) \\
$\epsilon$      & Clipping-Grenze von PPO (Abschnitt~\ref{sec:ppo}) \\
$\varepsilon$ & Zufallsanteil der Vergleichspolitik (Abschnitt~\ref{sec:baselines}) \\
$\eta$        & Pfadeffizienz: kürzester Weg geteilt durch tatsächliche Schritte \\
$\pi(a\mid s)$ & Politik: Wahrscheinlichkeit, im Zustand $s$ die Aktion $a$ zu wählen \\
$\Phi(s)$     & Potentialfunktion des Reward Shapings \\
$\sigma$      & Standardabweichung über Trainingsläufe \\
\bottomrule
\end{longtable}

% Das Abkürzungsverzeichnis ist ein Verzeichnis, keine nummerierte Tabelle.
% longtable zählt den table-Zähler dennoch hoch; ohne diese Korrektur begänne
% das Tabellenverzeichnis bei Tabelle 2.
\addtocounter{table}{-1}

\clearpage

% ===================================================================
%  HAUPTTEIL  (arabische Seitenzahlen)
% ===================================================================
\pagenumbering{arabic}
\setcounter{page}{1}

\section{Einleitung (gemeinsam)}
\label{sec:einleitung}

\subsection{Motivation}

% TODO vor Abgabe: Semester bzw. Jahr der RL-Lehrveranstaltung einsetzen.
Bei der Entwicklung eines Spiels müssen dessen Welten entworfen werden,
entweder einzeln von Hand oder über ein Regelwerk, das sie berechnet. Diese
Regelwerke werden als prozedurale Content-Generierung bezeichnet und seit den
1980er-Jahren in Computerspielen eingesetzt \cite{shaker2016pcg}. Sie nehmen
den Entwickelnden die Erstellung
einzelner Welten ab, verlagern aber Fragen zu Vielfalt, Lösbarkeit und
Schwierigkeit jeder einzelnen Welt in den Generator selbst
\cite{togelius2011search}.

Auf der anderen Seite steht das Reinforcement Learning (RL), ein
Lernverfahren, bei dem eine Politik über Belohnungssignale aus der
wiederholten Interaktion mit einer Umgebung entsteht \cite{sutton2018rl}.
Sowohl in der Lehre als auch in weiten Teilen der Forschung wird dabei auf
fertig vorliegenden, meist deterministischen Umgebungen gearbeitet
\cite{kirk2023survey}, weil deren Aufbau vom eigentlichen Lernverfahren
ablenkt. Das führt dazu, dass die Eigenschaften einer Umgebung als gegeben
hingenommen werden und ihr Anteil am Ergebnis unsichtbar bleibt
\cite{risi2020increasing}.

Vor dieser Projektarbeit bestand bereits etwas Erfahrung in beiden
Richtungen: einzelne experimentelle Projekte im Bereich der
Spieleentwicklung und eine besuchte Lehrveranstaltung zum Reinforcement
Learning. Daraus entstand der Wunsch, beide Themengebiete im Rahmen dieser
Projektarbeit praktisch zu vertiefen und miteinander zu verknüpfen.

Dadurch, dass beide Gebiete unabhängig voneinander weit entwickelt wurden,
ergibt sich die Frage nach ihrer Verbindung: ob es möglich ist, einen Agenten
in einem selbst entwickelten Spiel (dem hier konzipierten Stoneforge) mit
prozedural erzeugten Welten zu trainieren und dabei eine Strategie zu
erhalten, die auch auf nie gesehenen Welten funktioniert.

Auswendiglernen der Trainingswelten und echtes Navigieren wirken von außen
gleich erfolgreich; erst der Test auf trainingsfremden Welten unterscheidet
beide Fälle \cite{cobbe2020procgen}.

\subsection{Die Aufgabe in Kürze}
\label{sec:aufgabe_kurz}

Im Folgenden wird die Aufgabe knapp umrissen, bevor sie in den
Implementierungskapiteln ausgearbeitet wird.

Der Agent startet in der Mitte einer Welt, die er nie zuvor gesehen hat, und
soll deren Ausgang finden. Das zugrunde liegende Spiel heißt \emph{Stoneforge}
und läuft rundenbasiert ab. Aus einer Zufallszahl, dem \emph{Seed}, entsteht
eine Landschaft aus quadratischen Feldern: Wiese, Wald, Fels und Wasser.
Gleicher Seed, gleiche Welt; anderer Seed, andere Welt. Pro Zug bewegt sich der
Agent ein Feld nach oben, unten, links oder rechts, mehr steht ihm nicht zur
Verfügung (siehe Abschnitt~\ref{sec:envobs}).

Schwierig wird die Aufgabe dadurch, dass die Welt nicht überblickt werden kann.
Sichtbar ist nur ein kleiner Ausschnitt um die eigene Position, der Rest bleibt
schwarz. Die Richtung zum Ausgang ist bekannt, vergleichbar mit einem Kompass,
der Weg dorthin jedoch nicht. Liegt eine lange Felswand dazwischen, muss der
Agent selbst herausfinden, ob sie nördlich oder südlich zu umgehen ist, und
sich merken, wo er bereits gelaufen ist.
Abbildung~\ref{fig:pomdp_schema} auf Seite~\pageref{fig:pomdp_schema} stellt
beide Sichten nebeneinander und zeigt, wie wenig vom Weltzustand in die
Beobachtung eingeht.

Gelernt wird über Belohnung: Das Erreichen des Ausgangs bringt einen großen
Pluspunkt, jeder Schritt einen kleinen Abzug. Nach Millionen Versuchen stellt
sich ein Verhalten ein, bei dem am Ende möglichst viel Belohnung übrig bleibt.
Gezählt wird zum Schluss, wie viele von 50 unbekannten Welten gelöst werden
(siehe Abschnitt~\ref{sec:evalprotokoll}).

\subsection{Problemstellung und Forschungsfrage}

Der beschriebene Aufbau enthält zwei Probleme, die zusammenhängen. Sie werden
zunächst getrennt benannt und anschließend zu zwei Forschungsfragen verdichtet
(siehe Abschnitt~\ref{sec:ziele}).

\textbf{Das erste Problem betrifft den Agenten.} Da nur ein Ausschnitt sichtbar
ist, sehen zwei verschiedene Orte für ihn gleich aus, weshalb er seine Lage aus
dem bisher Gesehenen erschließen muss. Diese Leistung ließe sich ihm abnehmen,
indem ihm der kürzeste Weg vorberechnet wird, ein \glqq Orakel\grqq. Die
Aufgabe wäre damit gelöst, ohne dass etwas gelernt wurde, das ohne dieses
Orakel trägt. Richtig gestellt ist die Aufgabe daher erst, wenn dieser Ausweg
ausgeschlossen ist (siehe Abschnitt~\ref{sec:rewarddesign}).

\textbf{Das zweite Problem betrifft den Generator.} Er soll abwechslungsreiche
Welten liefern, weil sonst keine Generalisierung geprüft wird. Er soll lösbare
Welten liefern, weil sonst die Erfolgsquote nichts aussagt. Und er soll angeben
können, wie schwer eine Welt ist, weil sich das Training sonst nicht steuern
lässt. Diese Forderungen ziehen auseinander, und bereits die letzte ist offen,
da kein etabliertes Maß dafür existiert, woran die Schwierigkeit einer
erzeugten Welt zu bemessen wäre (siehe Abschnitt~\ref{sec:weltgenerator}).

Wie schwer die Aufgabe für den Agenten ausfällt, bestimmt daher der Generator.
Aus dieser Abhängigkeit ergeben sich zwei Fragen, von denen die zweite auf der
ersten aufbaut.

\begin{quote}
\itshape
\textbf{F1} \quad Wie lässt sich ein prozeduraler Weltgenerator so
konstruieren, dass er abwechslungsreiche, nachweislich lösbare Welten mit
präzise steuerbarer Schwierigkeit erzeugt und dabei als reproduzierbare
Versuchsumgebung für Reinforcement Learning taugt?

\vspace{0.25cm}
\textbf{F2} \quad Kann ein Reinforcement-Learning-Agent in solchen, nur
teilweise einsehbaren 2D-Welten eine Navigationsstrategie lernen, die auch auf
unbekannten Welten funktioniert, ohne dass ihm der Weg zum Ziel vorberechnet
wird?
\end{quote}

\noindent
F2 legt sich absichtlich nicht auf eine Architektur fest, weil die Frage nach
einem internen Gedächtnis Teil der Untersuchung ist und nicht ihre
Voraussetzung (siehe Abschnitt~\ref{sec:methodik}).

\subsection{Zielsetzung und Erfolgskriterien}
\label{sec:ziele}

Aus den beiden Fragen folgen zwei Sorten von Kriterien. Die erste legt fest,
wann die Umgebung brauchbar ist, die zweite, wann der Agent gut genug ist. Die
zweite Sorte ist nur sinnvoll, wenn die erste erfüllt ist, da ohne tragfähige
Umgebung keine Aussage über den Agenten möglich wäre (siehe
Kapitel~\ref{sec:evaluation}).

\paragraph{Z1: Anforderungen an Spiel und Generator.}
Diese Kriterien sind erfüllt oder nicht. Sie werden an der jeweils angegebenen
Stelle nachgewiesen und in Abschnitt~\ref{sec:evalplattform} zusammengeführt.

\begin{itemize}
  \item \textbf{Determinismus:} Gleicher Seed, gleiche Welt und gleicher
        Episodenverlauf, auch wenn Weltausschnitte in anderer Reihenfolge
        oder parallel erzeugt werden (Abschnitt~\ref{sec:weltgenerator}).
  \item \textbf{Lösbarkeit:} Jede erzeugte Welt hat einen begehbaren Weg vom
        Start zum Ziel, nachgewiesen über alle verwendeten Seeds
        (Abschnitt~\ref{sec:loesbarkeit}).
  \item \textbf{Steuerbare Schwierigkeit:} Die Zielentfernung lässt sich
        vorgeben und wird eingehalten, gemessen am tatsächlichen Laufweg statt
        an der Luftlinie (Tabelle~\ref{tab:versionsverlauf}).
  \item \textbf{RL-Tauglichkeit:} Anbindung an die Gymnasium-Schnittstelle,
        schnell genug für Läufe über Millionen Schritte auf einem normalen
        Rechner (Abschnitt~\ref{sec:technik}).
  \item \textbf{Spielbarkeit:} Dieselbe Simulation ist für Menschen spielbar,
        und einem trainierten Agenten kann beim Spielen zugesehen werden
        (Abschnitt~\ref{sec:client}).
\end{itemize}

\paragraph{Z2: Schwellen für den Agenten.}

\begin{itemize}
  \item \textbf{Testset A} (Seeds 7000--7049): $\geq 70\,\%$ Erfolgsquote,
  \item \textbf{Holdout B} (Seeds 8000--8049): $\geq 60\,\%$ Erfolgsquote,
  \item pro Konfiguration mindestens 3 Trainingsläufe (Mittelwert $\pm$ Standardabweichung).
\end{itemize}

\noindent
Die beiden Schwellen unter Z2 standen vor den Auswertungsläufen fest und wurden
daher nicht nachträglich passend gemacht. Ihre Herleitung steht in
Abschnitt~\ref{sec:kriterienrevision}. Z1 ist anders entstanden, weil es sich
um Konstruktionsziele handelt, die sich während der Entwicklung geschärft
haben. Die steuerbare Schwierigkeit etwa erhielt ihre heutige Form erst,
nachdem sich das Luftlinienmaß als irreführend erwiesen hatte
(Tabelle~\ref{tab:versionsverlauf}).

\paragraph{Vorgehen.}
Das Vorgehen folgt der Reihenfolge, in der die Bestandteile voneinander
abhängen. Abbildung~\ref{fig:ueberblick} zeigt die Kette vom Seed bis zur
Auswertung und ordnet ihr die beiden Forschungsfragen zu. Daran wird sichtbar,
dass F2 keinen eigenen Untersuchungsgegenstand besitzt, sondern vollständig auf
der Umgebung aufsetzt, die F1 hervorbringt.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, minimum width=3.0cm, minimum height=1.5cm,
              align=center, font=\scriptsize},
  seed/.style={draw, rounded corners=2pt, minimum width=1.6cm, minimum height=1.5cm,
               align=center, font=\scriptsize, fill=black!8},
  kap/.style={font=\scriptsize, align=center},
  >={Stealth[length=2.2mm]}, node distance=0.6cm
]
  \node[seed] (seed) {\textbf{Seed}};
  \node[box, right=of seed]  (gen)   {\textbf{Weltgenerator}\\Biome $\cdot$ Hindernisse\\BFS-Exit nach Laufweg};
  \node[box, right=of gen]   (env)   {\textbf{Lernumgebung}\\$15{\times}15$-Ausschnitt\\4 Aktionen $\cdot$ Reward};
  \node[box, right=of env]   (train) {\textbf{Training}\\rekurrente Politik\\Curriculum in Stufen};
  \node[box, right=of train] (eval)  {\textbf{Evaluation}\\gesperrte Seeds\\Erfolgsquote $\cdot$ Pfadeffizienz};

  \draw[->, thick] (seed)  -- (gen);
  \draw[->, thick] (gen)   -- (env);
  \draw[->, thick] (env)   -- (train);
  \draw[->, thick] (train) -- (eval);

  \draw[decorate, decoration={brace, amplitude=5pt}]
       ([yshift=4pt]gen.north east) -- ([yshift=4pt]seed.north west)
       node[midway, above=7pt, font=\scriptsize] {\textbf{F1:} tragfähige Versuchsumgebung};
  \draw[decorate, decoration={brace, amplitude=5pt}]
       ([yshift=4pt]eval.north east) -- ([yshift=4pt]env.north west)
       node[midway, above=7pt, font=\scriptsize] {\textbf{F2:} Generalisierung auf unbekannte Welten};

  \node[kap, below=3pt of gen]   {Abschnitt~\ref{sec:umgebung}};
  \node[kap, below=3pt of env]   {Abschnitt~\ref{sec:rlumgebung}};
  \node[kap, below=3pt of train] {Abschnitt~\ref{sec:methodik}};
  \node[kap, below=3pt of eval]  {Kapitel~\ref{sec:evaluation}};
\end{tikzpicture}%
}
\caption[Überblick über das Vorgehen]{Einleitung - Ablauf vom Seed bis zur
Auswertung mit der Zuordnung der beiden Forschungsfragen und der zugehörigen
Abschnitte, eigene Darstellung.}
\label{fig:ueberblick}
\end{figure}

Die Welt entsteht in Stufen. Ein grobes Landschaftsfeld legt fest, welches Biom
wo liegt, woraus anschließend jedes einzelne Feld belegt wird, wobei der Anteil
an Hindernissen vom Biom abhängt. Die Erreichbarkeit des Ziels wird nicht
nachträglich geprüft, sondern durch die Konstruktion sichergestellt, da eine
Breitensuche den Ausgang unter den begehbar erreichbaren Feldern mit der
gewünschten Weglänge auswählt. Der Generator speichert nichts, weil jeder
Ausschnitt allein von Seed und Koordinaten abhängt
(Abschnitt~\ref{sec:weltgenerator}).

Auf dieser Umgebung wird ein rekurrentes Verfahren mit internem Gedächtnis
trainiert, das stufenweise erst kurze und dann zunehmend längere Wege zum Ziel
bewältigen soll. Gemessen wird auf zwei Mengen von Welten, die im Training nie
vorkommen und auch für die Modellauswahl gesperrt sind. Da einzelne
Trainingsläufe im Reinforcement Learning stark schwanken können
\cite{henderson2018matters}, beruht jede berichtete Zahl auf mehreren
unabhängigen Läufen. Zusätzlich tritt der Agent gegen ungelernte
Vergleichspolitiken an, die dieselbe Aufgabe unter demselben Protokoll lösen
(Abschnitt~\ref{sec:methodik}).

Eine Lücke in dieser Aufstellung bestimmt am Ende das Ergebnis der Arbeit. Z1
verlangt lösbare Welten mit einstellbarer Entfernung, nicht jedoch, dass diese
Welten schwer genug sind. Die Umgebung kann daher alle fünf Kriterien erfüllen
und für F2 trotzdem untauglich sein. Genau dieser Fall ist eingetreten, denn
eine ungelernte Kompassheuristik erreicht auf derselben Aufgabe 92\,\%
(Abschnitt~\ref{sec:baselines}). Ein sechstes Kriterium, das die Härte der Welt
selbst prüfen müsste, wird für Folgearbeiten in Abschnitt~\ref{sec:ausblick}
vorgeschlagen.

\subsection{Einordnung der Arbeit}
\label{sec:relatedwork}

Stoneforge liegt strukturell zwischen drei etablierten Benchmarks: MiniGrid
(teilbeobachtbare Grid-Navigation, PPO+LSTM als Standard-Baseline)
\cite{chevalier2023minigrid}, Crafter (prozedurales 2D-Survival-Spiel mit
Mining und Crafting) \cite{hafner2021crafter} und Procgen (Generalisierung
über prozedurale Level) \cite{cobbe2020procgen}. Prozedurale Generierung als
Mittel zur erzwungenen Generalisierung besitzt zudem eine eigene
Forschungstradition \cite{togelius2011search, risi2020increasing}.

Die im eigenen Generator verwendeten Verfahren, rauschbasierte
Basisbelegung, zelluläre Automaten und graphenbasierte
Erreichbarkeitsprüfung, sind Standardbausteine der prozeduralen
Inhaltserzeugung \cite{shaker2016pcg, johnson2010cellular}. Der Beitrag
dieser Arbeit liegt daher nicht im Verfahren, sondern in dessen Kombination
zu einer Umgebung, die zugleich spielbar und als Messinstrument
kontrollierbar ist. Ein eigener Generator war nötig, weil sich Wandanteil,
Hinderniskorrelation und Zielabstand bei MiniGrid, Crafter und Procgen nicht
gezielt einstellen lassen; der Preis dafür ist die fehlende Einbettung in
publizierte Vergleichswerte.

Tabelle~\ref{tab:relatedwork} vergleicht, wie die drei Benchmarks Lösbarkeit
und Schwierigkeit steuern. Die BFS-basierte Exit-Platzierung dieser Arbeit
(Abschnitt~\ref{sec:loesbarkeit}) garantiert Lösbarkeit für jede einzelne
Welt statt nur im Mittel über viele Level und macht die Zielentfernung
dadurch erst als kontrollierte Variable nutzbar.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}L{2.4cm}L{6.0cm}L{4.6cm}@{}}
\toprule
\textbf{Benchmark} & \textbf{Lösbarkeitsmechanismus} & \textbf{Schwierigkeitssteuerung} \\
\midrule
MiniGrid & Platzierungsregeln erzeugen lösbare Layouts konstruktiv, keine nachgelagerte Prüfung \cite{chevalier2023minigrid} & keine explizite Distanzvorgabe \\
\addlinespace
Crafter & kein Einzelziel, Bewertung über erreichte Achievements \cite{hafner2021crafter} & nicht anwendbar \\
\addlinespace
Procgen & Eigenschaft der handentworfenen Levelgeneratoren, gilt im Mittel über viele Level \cite{cobbe2020procgen, procgen_repo} & Verteilungsmodus easy/hard und Levelanzahl \\
\addlinespace
Stoneforge (diese Arbeit) & BFS-basierte Exit-Platzierung, für jede einzelne Welt geprüft & Zielentfernung direkt vorgebbar \\
\bottomrule
\end{tabular}
\caption[Vergleich der Lösbarkeits- und Schwierigkeitssteuerung]{Einordnung in verwandte Arbeiten - Vergleich von Lösbarkeitsmechanismus und Schwierigkeitssteuerung etablierter Benchmarks, eigene Darstellung.}
\label{tab:relatedwork}
\end{table}

\paragraph{Die Schwierigkeit steckt in der Struktur der Hindernisse.}
Zelluläre Automaten formen aus verstreutem Rauschen zusammenhängende,
konkave Strukturen wie Höhlen und Sackgassen, die eine reaktive,
gedächtnislose Navigation zuverlässig scheitern lassen
\cite{johnson2010cellular, shaker2016pcg}. Ohne diese Glättungsstufe
verbleiben verstreute Einzelhindernisse (Tabelle~\ref{tab:worldgen_aktiv}),
die ein Kompass-Folger auch ohne Gedächtnis umgeht; das erklärt die hohe
Erfolgsquote der ungelernten Referenzpolitik unmittelbar
(Abschnitt~\ref{sec:baselines}). Der Umkehrschluss trägt jedoch nicht: Die
Glättungsstufe dieses Generators härtet die Welt nicht, sondern planiert
sie, weil die Ausgangsdichte unter der Geburtsschwelle des Automaten liegt
(Wanddichte 0,037 statt 0,238, Umwegfaktor 1,001 statt 1,10,
Tabelle~\ref{tab:geometrieprobe}). Publizierte PCG-Bausteine sind daher
nicht parameterfrei übertragbar; die Schwierigkeit einer prozeduralen
Umgebung sollte vor dem Training mit einer ungelernten Heuristik kalibriert
werden (Abschnitt~\ref{sec:ausblick}).

\paragraph{Gedächtnis in teilbeobachtbaren Umgebungen.}
Die Standardantwort auf partielle Beobachtbarkeit ist eine rekurrente
Politik: DRQN erweiterte DQN um ein LSTM \cite{hausknecht2015drqn}, R2D2
zeigte die Bedeutung korrekt geführter rekurrenter Zustände
\cite{kapturowski2019r2d2}, und der POPGym-Benchmark findet einfache
GRU-Architekturen insgesamt konkurrenzfähig zu aufwändigeren Modellen
\cite{morad2023popgym}. Der hier verwendete RecurrentPPO-Ansatz
\cite{pleines2022recurrent} folgt dieser Standard-Baseline; die eigene
E3-Ablation bestätigt das Bild, da ein größeres LSTM (512 statt 256) keine
Verbesserung bringt (Abschnitt~\ref{sec:gapexperimente}).

\paragraph{Curriculum und Exploration.}
Curriculum Learning geht auf Bengio et al.\ zurück
\cite{bengio2009curriculum}; das leistungsbasierte Gating dieser Arbeit ist
eine einfache Instanz davon. Prioritized Level Replay \cite{jiang2021plr}
wiederholt gezielt Level mit hohem Lernpotenzial; der hier verwendete
Seed-Pool (Abschnitt~\ref{sec:technik}) kehrt diese Auswahlrichtung um und
ist deshalb kritisch zu sehen. Ein zählbasierter Explorations-Bonus für neu
betretene Felder übernimmt die Rolle, die kuriositätsgetriebene Verfahren
wie ICM \cite{pathak2017icm} und RND \cite{burda2018rnd} in Umgebungen mit
nicht direkt abzählbarer Neuheit spielen.

\paragraph{Zur Vergleichbarkeit der absoluten Zahlen.}
Ein direkter Vergleich der erreichten Erfolgsquote mit publizierten Werten
ist nicht seriös möglich, da die Benchmarks unterschiedliche Metriken
berichten: normalisierter Return bei Procgen \cite{cobbe2020procgen},
Achievements bei Crafter \cite{hafner2021crafter}, aufgabenspezifische
Erfolgsquoten bei MiniGrid. Belastbar ist der Vergleich nur innerhalb
dieser Arbeit: gegen die eigene Gegenüberstellung (Tabelle~\ref{tab:ablation}),
die Gap-Experimente (Abschnitt~\ref{sec:gapexperimente}) und die vorab
fixierten Kriterien (Abschnitt~\ref{sec:kriterienrevision}).

\subsection{Aufbau der Arbeit}
\label{sec:aufbau}

Nun folgt der Aufbau der Arbeit. Sie hält den üblichen Ablauf ein: zuerst die
Begriffe, dann die Methodik, dann die technische Umsetzung, dann die
Messungen und zuletzt deren Bewertung. Durchgehend steht dabei das Spiel vor
dem Agenten, weil das eine die Voraussetzung des anderen ist (siehe
Abbildung~\ref{fig:ueberblick}).

Kapitel~\ref{sec:methodikkapitel} legt die Methodik beider Teile offen, bevor
der Code dazu gezeigt wird: Abschnitt~\ref{sec:methodikwelt} begründet die
Wahl der Generierungsverfahren und die Messmethodik der Weltschwierigkeit,
Abschnitt~\ref{sec:methodik} die Wahl des Lernverfahrens, des Curriculums und
des Eval-Protokolls. Kapitel~\ref{sec:implementierung} setzt diese
Entscheidungen technisch um, wieder in zwei Abschnitten:
Abschnitt~\ref{sec:umgebung} für das Spiel mit Simulationskern,
Weltgenerator, Lösbarkeitsgarantie und Client, Abschnitt~\ref{sec:rlumgebung}
für die darauf aufsetzende Lernumgebung mit partieller Beobachtbarkeit,
Beobachtungsraum, Reward-Design, Trainings-Pipeline und Monitoring.
Kapitel~\ref{sec:evaluation} bewertet in derselben Reihenfolge erst die
Plattform (Abschnitt~\ref{sec:evalplattform}) und dann den Agenten
(Abschnitt~\ref{sec:evalagent}).

Der Versionsverlauf von Umgebung und Trainingskonfiguration ist als Tabelle
in Abschnitt~\ref{sec:rlumgebung} zusammengefasst (Tabelle~\ref{tab:versionsverlauf}).

\paragraph{Zur Versionsbezeichnung.}
Die Kürzel \emph{v11} und \emph{v12} sind nötig, weil Ergebnisse verschiedener
Entwicklungsstände nicht vergleichbar sind: Ändert sich die Weltgenerierung,
erzeugt derselbe Seed eine andere Welt. \emph{v11} bezeichnet die überarbeitete
Umgebung, auf der alle Endergebnisse beruhen, \emph{v12} die darauf aufbauende
Trainingskonfiguration (Tabelle~\ref{tab:basiskonfig}, Versionsüberblick in
Tabelle~\ref{tab:versionsverlauf}).

% TODO vor Abgabe: Arbeitsteilung gegenprüfen und ggf. präzisieren.
\paragraph{Zur Arbeitsteilung.}
Die Arbeit entstand zu zweit, weshalb Tabelle~\ref{tab:arbeitsteilung} die
Beiträge den Kapiteln zuordnet. \glqq Schwerpunktmäßig\grqq{} bedeutet, dass
Entwurf und Umsetzung überwiegend bei der genannten Person lagen. Beide Teile
hängen enger zusammen, als die Aufteilung vermuten lässt, da mehrere der
wichtigsten Befunde weder den Generator noch den Agenten allein betreffen,
sondern ihr Zusammenspiel (siehe Abschnitt~\ref{sec:baselines}).

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}L{7.4cm}L{2.7cm}L{2.6cm}@{}}
\toprule
\textbf{Beitrag} & \textbf{Schwerpunkt} & \textbf{Kapitel} \\
\midrule
Spielkonzept, C++-Simulationskern, Spiellogik & Laurin Rößler & \ref{sec:umgebung}, \ref{sec:technik} \\
Prozedurale Weltgenerierung und Lösbarkeitsgarantie & Laurin Rößler & \ref{sec:pcg_algorithmen}, \ref{sec:methodikwelt}, \ref{sec:weltgenerator} \\
Grafischer Spiel-Client mit KI-Zuschaumodus & Laurin Rößler & \ref{sec:technik} \\
\midrule
Gym-Anbindung, Beobachtungsraum, Reward-Design & Florian Merlau & \ref{sec:pbrs}, \ref{sec:rlumgebung} \\
Curriculum-Training, Hyperparameter, Fehleranalysen & Florian Merlau & \ref{sec:methodik}, \ref{sec:rlumgebung} \\
Evaluationsprotokoll, Referenzpolitiken, Auswertung & Florian Merlau & \ref{sec:evalprotokoll}, \ref{sec:evaluation} \\
\midrule
Live-Monitoring, Werkzeuge, Experiment-Changelog & gemeinsam & \ref{sec:technik} \\
\bottomrule
\end{tabular}
\caption[Arbeitsteilung]{Einleitung - Zuordnung der Beiträge zu den Autoren und
Kapiteln, eigene Darstellung.}
\label{tab:arbeitsteilung}
\end{table}

\section{Grundlagen (gemeinsam)}
\label{sec:grundlagen}

Dieses Kapitel legt die Begriffe fest, mit denen die folgenden Kapitel
arbeiten, in zwei Abschnitten: Abschnitt~\ref{sec:g_pcg} behandelt die
prozedurale Erzeugung von Spielwelten, Abschnitt~\ref{sec:g_rl} das Lernen
unter partieller Beobachtbarkeit samt der Steuerung und Bewertung dieses
Lernprozesses. Für die vertiefte Einarbeitung liefern Sutton und Barto eine
vollständige Einführung in das Reinforcement Learning \cite{sutton2018rl},
Shaker et al.\ eine in die prozedurale Inhaltserzeugung
\cite{shaker2016pcg}; beide Werke tragen die Begriffsbildung dieses
Kapitels.

\subsection{Grundlagen der Weltgenerierung (Laurin)}
\label{sec:g_pcg}

\subsubsection{Begriff, Seed und Determinismus}
\label{sec:pcg_generalisierung}

Prozedurale Inhaltserzeugung (\emph{Procedural Content Generation}, PCG) erzeugt
Spielinhalte wie Karten, Level oder Gegenstände algorithmisch statt von Hand
\cite{shaker2016pcg, togelius2011search}. Die Entwickelnden entwerfen dabei kein
Level, sondern ein Regelwerk, das die Welt bei Bedarf berechnet. Das spart
Speicher und Autorenaufwand, weil nur der Algorithmus abgelegt wird und nicht
das fertige Level, und es liefert praktisch unbegrenzte Vielfalt
\cite{shaker2016pcg}.

Gesteuert wird die Erzeugung über einen \emph{Seed}, den Startwert des
Zufallszahlengenerators. Derselbe Seed erzeugt reproduzierbar dieselbe Welt, ein
anderer Seed eine andere. Diese Reproduzierbarkeit ist keine technische
Nebensache, sondern die Grundlage jedes kontrollierten Experiments mit
prozeduralen Welten, da sich eine über ihre Seeds festgelegte Menge von Welten
jederzeit exakt wiederherstellen lässt. Damit ist eine feste, benannte
Stichprobe definiert. Wie diese Eigenschaft die Trennung von Trainings- und
Testmenge trägt, zeigt Abschnitt~\ref{sec:g_bridge}.

\subsubsection{Algorithmen der prozeduralen Weltgenerierung}
\label{sec:pcg_algorithmen}

Der vorige Abschnitt klärt, \emph{was} prozedurale Generierung ist. Dieser
klärt, \emph{wie} eine begehbare Welt entsteht. Die Darstellung folgt dem für
gitterbasierte Generatoren üblichen dreistufigen Aufbau aus Basisbelegung,
Formgebung und Validierung \cite{shaker2016pcg}.

Gitterbasierte Generatoren bauen die Welt selten am Stück, sondern in
quadratischen Kacheln fester Größe, sogenannten \emph{Chunks}. Ein Chunk
entsteht erst, wenn der Agent ihn betritt. Hängt seine Berechnung ausschließlich
von Seed und Koordinaten ab, spielt die Reihenfolge der Erzeugung keine Rolle,
weshalb die Welt ohne gespeicherten Zustand auskommt.

\paragraph{Rauschbasierte Basisbelegung.}
Die erste Stufe belegt jede Gitterzelle deterministisch aus einer Rauschfunktion
und übersetzt deren Wert über Schwellwerte in Materialklassen, eines der
Standardverfahren gitterbasierter PCG \cite{shaker2016pcg}. Zu unterscheiden
sind zwei Varianten, deren Unterschied über die Spielbarkeit bestimmt.
\emph{Hash-Rauschen} berechnet den Wert jeder Zelle unabhängig aus ihren
Koordinaten und dem Seed, wodurch unkorreliertes weißes Rauschen entsteht, aus
dem ein Schwellwert verstreute \emph{Einzelhindernisse} schneidet. \emph{Wert-}
oder \emph{Gradientenrauschen} wertet dagegen nur ein gröberes Stützgitter aus
und interpoliert dazwischen, klassisch mit Perlins Glättungsfunktion
$3t^2 - 2t^3$ \cite{perlin1985image}, weshalb benachbarte Zellen korrelieren und
\emph{zusammenhängende} Formationen entstehen.

Werden die Eingabekoordinaten vor der Auswertung selbst durch ein Rauschfeld
verschoben (\emph{Domain Warping}), verlieren die Regionen ihre am Stützgitter
ablesbare Rechteckigkeit. Beide Verfahren sind pro Seed reproduzierbar und
kommen ohne gespeicherten Zustand aus, taugen also für theoretisch unbegrenzte
Welten.

\paragraph{Zelluläre Automaten als Formgebung.}
Rein rauschbasierte Belegungen wirken häufig zerfranst, weshalb meist eine
Glättungsstufe über einen zellulären Automaten folgt. Er bewertet jede Zelle
iterativ anhand der Zahl belegter Nachbarn, üblicherweise in der
\emph{Moore-Nachbarschaft} aus den acht direkt angrenzenden Zellen (horizontal,
vertikal und diagonal). Geburts- und Überlebensregeln
(\emph{Birth}/\emph{Survival Rules}) formen daraus fließende, natürlich wirkende
Höhlen- und Klippenstrukturen: Eine freie Zelle wird belegt, sobald die Zahl
belegter Nachbarn eine Geburtsschwelle erreicht, und eine belegte Zelle bleibt
nur belegt, solange sie eine Überlebensschwelle erfüllt.

Wenige Iterationen genügen, um verstreutes Rauschen in zusammenhängende
Strukturen zu überführen. Johnson et al.\ demonstrieren das Verfahren an
echtzeitfähig erzeugten Höhlenleveln \cite{johnson2010cellular}.

\paragraph{Graphentheoretische Erreichbarkeitsanalyse.}
Prozedurale Welten haben ein Kernproblem, die Spielbarkeit
(\emph{Playability}), also die Vermeidung unlösbarer Level-Zustände
(\emph{Deadlocks}). Für die Analyse der Konnektivität wird das Zellgitter als
ungerichteter Graph gelesen, in dem begehbare Zellen die Knoten und
Nachbarschaftsbeziehungen die Kanten bilden. Welche Nachbarschaft dabei richtig
ist, folgt aus dem Bewegungsmodell: Bei ausschließlich achsenparalleler Bewegung
gilt die Vierer-Nachbarschaft (\emph{von Neumann}) und nicht die für die
Glättung verwendete Moore-Nachbarschaft, denn eine über die Diagonale
hergestellte Verbindung wäre für den Agenten nicht begehbar.

Die Breitensuche (\emph{Breadth-First Search}, BFS) traversiert den Graphen
ebenenweise vom Startknoten aus \cite{cormen2009algorithms}. Interessiert dabei
nur die erreichbare Menge und nicht die Distanz, wird von \emph{Flood-Fill}
gesprochen. Die Breitensuche beantwortet damit zwei Fragen zugleich, nämlich ob
ein Zielknoten überhaupt erreichbar ist und wie der kürzeste Pfad auf einem
ungewichteten Gitter verläuft \cite{cormen2009algorithms}. Beide Eigenschaften
kehren später wieder (siehe Abschnitte~\ref{sec:pbrs}
und~\ref{sec:g_metriken}).

\paragraph{Heuristische Sicherheitsnetze und Metriken.}
Spielbarkeit sicherzustellen und unspielbare Level zu reparieren ist ein
eigenständiger Baustein der PCG-Praxis \cite{shaker2016pcg}. Da die exakte
graphenbasierte Validierung bei großen Weltausschnitten teuer wird, wird sie
häufig mit billigeren Prüfungen kombiniert. Als Vorabschätzung dient die
\emph{Manhattan-Distanz}, die Distanz zweier Punkte auf einem orthogonalen
Gitter unter achsenparalleler Bewegung:
\begin{equation}
D_{\mathrm{Manhattan}} = |x_1 - x_2| + |y_1 - y_2|.
\end{equation}
Sie ist in konstanter Zeit berechenbar und unterschätzt den tatsächlichen
Laufweg, sobald Hindernisse dazwischenstehen. Als Ersatz für eine
Erreichbarkeitsprüfung taugt sie deshalb nicht, wohl aber als untere Schranke
und als Abbruchkriterium. Scheitert die Prüfung, greift ein zweites
Sicherheitsnetz: Eine pragmatische \emph{Fallback-Generierung} (\emph{Carving})
räumt entlang direkter Geometrieachsen einen Korridor von Start zu Ziel frei,
typischerweise erst in der einen und dann in der anderen Achse.

Beide Mittel wirken, aber sie sind nicht folgenlos. Ein gecarvter Korridor ist
per Konstruktion eine gerade, hindernisfreie Linie und damit eine strukturelle
Auffälligkeit, die das erzeugte Weltbild systematisch von einer gleichmäßig
verteilten Hindernisstruktur wegzieht.

\subsection{Grundlagen des Reinforcement Learning (Florian)}
\label{sec:g_rl}

\subsubsection{Reinforcement Learning und der Markov-Entscheidungsprozess}
\label{sec:mdp}

Reinforcement Learning beschreibt Lernen durch Interaktion: Der Agent wählt in
einem Zustand eine Aktion, erhält eine Belohnung (Reward) und landet in einem
Folgezustand \cite{sutton2018rl}. Formal ist das ein
Markov-Entscheidungsprozess (MDP), ein Tupel $(S, A, P, R, \gamma)$ mit
Zustandsmenge $S$, Aktionsmenge $A$, Übergangswahrscheinlichkeit
$P(s'\mid s,a)$, Rewardfunktion $R(s,a)$ und Diskontfaktor $\gamma \in [0,1)$.
Gesucht ist eine Politik $\pi(a\mid s)$, die den erwarteten diskontierten Return
$G_t = \sum_{k\geq 0} \gamma^k r_{t+k}$ maximiert. Die Zustandswertfunktion
$V^\pi(s) = \mathbb{E}_\pi[G_t \mid s_t=s]$ gibt den erwarteten Return ab einem
Zustand an. Der Diskontfaktor $\gamma$ gewichtet spätere Rewards geringer.

Abbildung~\ref{fig:mdp_loop} zeigt diesen Kreislauf, die Grundfigur des
gesamten Verfahrens. Jedes hier trainierte Modell ist eine Variante dieser
Schleife: Die Umgebung heißt \emph{Stoneforge}, die Aktionen sind die vier
Bewegungsrichtungen, und der Reward liefert das Lernsignal. An der Abbildung wird zugleich sichtbar,
worauf es im weiteren Verlauf ankommt, weil der Agent ausschließlich über
diesen Rückkanal etwas über die Welt erfährt.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, minimum width=3.4cm, minimum height=1.0cm,
              align=center, font=\small},
  >={Stealth[length=2.4mm]}
]
  \node[box] (agent) at (0,2.4) {\textbf{Agent}};
  \node[box] (env)   at (0,0)   {\textbf{Umgebung}};

  % rechts: Aktion vom Agenten hinunter zur Umgebung
  \draw[->, thick] (agent.east) -- (3.0,2.4) -- (3.0,0) -- (env.east);
  \node[font=\scriptsize, align=center, anchor=west] at (3.15,1.2)
       {Aktion\\[-1pt]$A_t$};

  % links: Zustand und Reward von der Umgebung hinauf zum Agenten
  \draw[->, thick] (env.west) -- (-3.0,0) -- (-3.0,2.4) -- (agent.west);
  \node[font=\scriptsize, align=center, anchor=east] at (-3.15,1.2)
       {Zustand $S_t$\\[-1pt]Reward $R_t$};

  % Zeitschritt-Trennung: gestrichelte Linie quer durch den Rueckkanal
  \draw[dashed, gray!70] (-3.9,1.2) -- (-0.9,1.2);
  \node[font=\scriptsize, gray!70, anchor=north] at (-2.0,1.1)
       {$S_{t+1},\; R_{t+1}$};
\end{tikzpicture}
\caption[Agent-Umwelt-Interaktion im MDP]{Grundlagen - Agent-Umwelt-Interaktion
im Markov-Entschei\-dungs\-pro\-zess mit dem Übergang zum nächsten Zeitschritt
als gestrichelter Linie, eigene Darstellung in Anlehnung an Sutton und Barto
\cite[Abb.~3.1]{sutton2018rl}.}
\label{fig:mdp_loop}
\end{figure}

\subsubsection{Partielle Beobachtbarkeit (POMDP)}
\label{sec:pomdp}

Sieht der Agent nicht den vollständigen Zustand, sondern nur eine Beobachtung,
liegt ein \emph{Partially Observable} MDP (POMDP) vor \cite{kaelbling1998pomdp}.
Für die Beobachtung gilt die Markov-Eigenschaft dann nicht mehr, weil dieselbe
lokale Beobachtung zu ganz verschiedenen globalen Zuständen gehören kann. Eine
reaktive Politik, die nur die aktuelle Beobachtung nutzt, ist deshalb im
Allgemeinen nicht optimal. Abhilfe schafft ein internes Gedächtnis, das
vergangene Beobachtungen zu einem \emph{Belief-State} verdichtet
\cite{hausknecht2015drqn}.

Abbildung~\ref{fig:mdp_vs_pomdp} stellt beide Fälle gegenüber. Im MDP greift die
Politik direkt auf den Zustand zu, im POMDP schiebt sich eine
Beobachtungsfunktion dazwischen. Die Politik muss aus der Vorgeschichte erst
rekonstruieren, wo sie vermutlich steht, und genau diese Zusatzleistung ist in
der Abbildung grau hinterlegt.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  b/.style={draw, rounded corners=2pt, minimum height=0.8cm, align=center, font=\small},
  >={Stealth[length=2mm]}, node distance=0.9cm
]
% ---------------- MDP ----------------
\begin{scope}
  \node[b, minimum width=1.5cm] (s1) at (0,0) {Zustand\\$s_t$};
  \node[b, minimum width=1.5cm, right=1.3cm of s1] (pi1) {Politik\\$\pi(a\mid s)$};
  \node[b, minimum width=1.5cm, right=1.3cm of pi1] (a1) {Aktion\\$a_t$};
  \draw[->, thick] (s1) -- (pi1);
  \draw[->, thick] (pi1) -- (a1);
  \node[font=\small\bfseries, above=0.35cm of pi1] {MDP: vollständig beobachtbar};
\end{scope}
% ---------------- POMDP ----------------
\begin{scope}[yshift=-2.7cm]
  \node[b, minimum width=1.5cm] (s2) at (0,0) {Zustand\\$s_t$};
  \node[b, minimum width=1.6cm, right=0.75cm of s2, fill=black!7] (o2)
        {Beobachtung\\$o_t = O(s_t)$};
  \node[b, minimum width=1.7cm, right=0.75cm of o2, fill=black!7] (b2)
        {Belief\\$b_t(h_{1:t})$};
  \node[b, minimum width=1.5cm, right=0.75cm of b2] (pi2) {Politik\\$\pi(a\mid b)$};
  \node[b, minimum width=1.2cm, right=0.75cm of pi2] (a2) {Aktion\\$a_t$};
  \draw[->, thick] (s2) -- (o2);
  \draw[->, thick] (o2) -- (b2);
  \draw[->, thick] (b2) -- (pi2);
  \draw[->, thick] (pi2) -- (a2);
  % Rekurrenz
  \draw[->, thick] (b2.north) .. controls +(0,0.75) and +(0,0.75) .. (b2.north west)
        node[midway, above, font=\scriptsize] {$b_{t-1}$};
  \node[font=\small\bfseries, above=1.05cm of pi2] {POMDP: nur Ausschnitt sichtbar};
  \draw[decorate, decoration={brace, amplitude=4pt, mirror}]
        ($(o2.south west)+(0,-0.15)$) -- ($(b2.south east)+(0,-0.15)$)
        node[midway, below=4pt, font=\scriptsize] {vom Agenten zu leisten};
\end{scope}
\end{tikzpicture}
\caption[MDP gegenüber POMDP]{Grundlagen - Vollständige (oben) gegenüber
partieller Beobachtbarkeit (unten) mit den beiden im POMDP zusätzlich
anfallenden und grau hinterlegten Schritten, eigene Darstellung; Formalismus
nach Kaelbling et al.\ \cite{kaelbling1998pomdp}.}
\label{fig:mdp_vs_pomdp}
\end{figure}

Eine zweite Konsequenz trägt diese Arbeit weit. Unter partieller
Beobachtbarkeit kann eine \emph{stochastische} gedächtnislose Politik jede
deterministische gedächtnislose deutlich übertreffen \cite{singh1994pomdp}. Auf
die Reichweite dieser Aussage kommt es an, denn sie gilt für
\emph{gedächtnislose} Politiken. Ist der Belief-State hinreichend genau
rekonstruiert, existiert wieder eine optimale deterministische Politik (siehe
Abschnitt~\ref{sec:lstm}).

Ein Leistungsabstand zwischen Sampling- und Argmax-Auswertung einer
\emph{rekurrenten} Politik ist damit gerade kein notwendiges Merkmal der
Aufgabe, sondern ein Indikator für einen unvollständigen Belief-State. Genau in
dieser Lesart wird er in der Evaluation geprüft.

Stoneforge ist ein POMDP, weil der Agent nur einen lokalen
$15{\times}15$-Ausschnitt sieht. Abbildung~\ref{fig:pomdp_schema} stellt den Unterschied am konkreten
Weltausschnitt dar und macht sichtbar, dass der überwiegende Teil des
Weltzustands außerhalb der Beobachtung liegt. Neben diesem unmittelbar
sichtbaren lokalen Ausschnitt existiert eine zweite, weniger offensichtliche
Quelle partieller Beobachtbarkeit. Sie entsteht erst aus der Forderung nach
Generalisierung auf unbekannte Welten und wird in Abschnitt~\ref{sec:g_bridge}
behandelt, wo beide Themen zusammenlaufen.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.47\textwidth]{figures/fig_game_world.png}
  \hfill
  \includegraphics[width=0.47\textwidth]{figures/fig_ai_observation.png}
  \caption[Partielle Beobachtbarkeit am Beispiel Stoneforge]{Grundlagen -
  Vollständiger Weltzustand als $31{\times}31$-Ausschnitt (links) gegenüber der
  Beobachtung des Agenten mit rot umrandetem $15{\times}15$-Sichtfeld und
  Kompasspfeil zum Exit (rechts), eigene Darstellung.}
  \label{fig:pomdp_schema}
\end{figure}

\subsubsection{Wertbasierte Verfahren: Q-Learning und DQN}

Wertbasierte Verfahren lernen die Aktionswertfunktion
$Q^\pi(s,a) = \mathbb{E}_\pi[G_t \mid s_t=s, a_t=a]$ und leiten die Politik
daraus ab, indem sie die Aktion mit dem höchsten $Q$-Wert wählen. Q-Learning
aktualisiert $Q$ über die Bellman-Gleichung. Deep Q-Networks (DQN) approximieren
$Q$ mit einem neuronalen Netz und stabilisieren das Training über einen
Replay-Buffer und ein Target-Netz \cite{mnih2015dqn}. DQN verarbeitet nur
diskrete Aktionsräume.

\subsubsection{Policy-Gradient- und Actor-Critic-Verfahren}

Policy-Gradient-Verfahren optimieren die Politik $\pi_\theta$ direkt über den
Gradienten des erwarteten Returns. Actor-Critic-Verfahren stellen ihnen eine
gelernte Wertfunktion (Critic) zur Seite, die als Baseline die Varianz der
Gradientenschätzung senkt. Der Vorteil (Advantage)
$A(s,a) = Q(s,a) - V(s)$ misst, wie viel besser eine Aktion gegenüber dem
Durchschnitt ist; die Generalized Advantage Estimation (GAE) schätzt ihn über
mehrere Zeitschritte mit einem Parameter $\lambda$ \cite{schulman2016gae}.
Advantage Actor-Critic (A2C) ist die synchrone Variante von A3C
\cite{mnih2016a3c}.

Wie gut der Critic gelernt hat, verrät die \emph{explained variance} (EV), der
Anteil der Varianz der tatsächlichen Returns, den die Wertschätzung erklärt.
EV~$\approx$~1 steht für eine verlässliche Wertfunktion, EV~$\approx$~0 dafür,
dass der Critic praktisch nichts gelernt hat. Diese Größe dient hier als
wichtigste Diagnosemetrik des Trainings, weil sie eine instabile Wertschätzung
früher anzeigt als die Erfolgsquote.

\subsubsection{Proximal Policy Optimization (PPO)}
\label{sec:ppo}

PPO ist das Hauptverfahren dieser Arbeit \cite{schulman2017ppo}. Es ist
ein Actor-Critic-Verfahren, das die Politik pro Update nur begrenzt verändert, um
Instabilität durch zu große Schritte zu vermeiden. Dazu schneidet es das
Wahrscheinlichkeitsverhältnis
$r_t(\theta) = \pi_\theta(a_t\mid s_t)/\pi_{\theta_\text{alt}}(a_t\mid s_t)$
in der Zielfunktion ab (\emph{clipping}):
\[
L^{\text{CLIP}}(\theta) = \mathbb{E}_t\!\left[\min\!\big(r_t(\theta)\,A_t,\;
\operatorname{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon)\,A_t\big)\right].
\]

Abbildung~\ref{fig:ppo_clip} zeigt die Wirkung des Clippings an einem einzelnen
Zeitschritt. War die Aktion besser als erwartet ($A_t > 0$), lohnt sich eine
höhere Wahrscheinlichkeit, allerdings nur bis zum Faktor $1+\epsilon$, weil die
Zielfunktion darüber flach verläuft und der Anreiz für einen größeren Schritt
wegfällt. War sie schlechter als erwartet ($A_t < 0$), gilt dasselbe
spiegelbildlich bei $1-\epsilon$. Genau an solchen zu großen Aktualisierungen
scheitern einfache Policy-Gradient-Verfahren, die ihre Schrittweite nicht
begrenzen.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.0, >={Stealth[length=1.8mm]}, font=\small]
% ---------------- A > 0 ----------------
\begin{scope}
  \draw[->] (-0.15,0) -- (2.75,0) node[right, font=\scriptsize] {$r$};
  \draw[->] (0,-0.25) -- (0,1.95) node[above, font=\scriptsize] {$L^{\text{CLIP}}$};
  % linear bis 1+eps, dann flach
  \draw[very thick, blue!70!black] (0,0) -- (1.7,1.7) -- (2.6,1.7);
  \draw[dotted, gray] (1.0,0) -- (1.0,1.0);
  \draw[dotted, gray] (1.7,0) -- (1.7,1.7);
  \node[font=\scriptsize] at (1.0,-0.28) {$1$};
  \node[font=\scriptsize] at (1.78,-0.28) {$1+\epsilon$};
  \fill[red] (1.0,1.0) circle (2pt);
  \node[font=\small] at (1.3,1.85) {$A_t > 0$};
\end{scope}
% ---------------- A < 0 ----------------
\begin{scope}[xshift=5.2cm]
  \draw[->] (-0.15,0) -- (2.75,0) node[right, font=\scriptsize] {$r$};
  \draw[->] (0,-1.95) -- (0,0.35) node[above, font=\scriptsize] {$L^{\text{CLIP}}$};
  % flach bis 1-eps, dann linear fallend
  \draw[very thick, blue!70!black] (0,-0.6) -- (0.6,-0.6) -- (1.85,-1.85);
  \draw[dotted, gray] (1.0,0) -- (1.0,-1.0);
  \draw[dotted, gray] (0.6,0) -- (0.6,-0.6);
  \node[font=\scriptsize] at (0.52,0.22) {$1-\epsilon$};
  \node[font=\scriptsize] at (1.05,0.22) {$1$};
  \fill[red] (1.0,-1.0) circle (2pt);
  \node[font=\small] at (2.0,-0.35) {$A_t < 0$};
\end{scope}
\end{tikzpicture}
\caption[Wirkung des PPO-Clippings]{Grundlagen - Ein einzelner Term der
abgeschnittenen Zielfunktion $L^{\text{CLIP}}$ über dem
Wahrscheinlichkeitsverhältnis $r_t(\theta)$ für positiven (links) und negativen
Advantage (rechts) mit dem Startpunkt der Optimierung als rotem Punkt, eigene
Darstellung in Anlehnung an Schulman et al.\ \cite[Abb.~1]{schulman2017ppo}.}
\label{fig:ppo_clip}
\end{figure}

Hinzu kommt ein Entropie-Term, der Exploration fördert, weil sein Gewicht
steuert, wie stochastisch die Politik bleibt. Trainiert wird in Zyklen: Zuerst
sammeln die parallelen Umgebungen einen Rollout fester Länge, anschließend
optimiert PPO diesen in Minibatches über mehrere Durchläufe. Verwendet wird die
Implementierung aus Stable-Baselines3 \cite{raffin2021stable}.

Eine gelernte stochastische Politik lässt sich auf zwei Arten ausführen:
\emph{stochastisch}, indem die Aktion aus der Verteilung $\pi(a\mid s)$ gezogen
wird (Sampling), oder \emph{deterministisch} über die stets wahrscheinlichste
Aktion (Argmax). Dasselbe trainierte Modell kann in beiden Betriebsarten
unterschiedlich gut abschneiden. Der Abstand zwischen beiden Messwerten wird
durchgängig \textbf{Det/Stoch-Gap} genannt. Er ist einer der Hauptgegenstände
dieser Arbeit, weil er sich als groß und hartnäckig erwiesen hat.

Anschaulich gesprochen löst der Agent die Aufgabe zuverlässiger, wenn er
würfelt, als wenn er stets seine eigene beste Aktion wählt. Dass das unter
partieller Beobachtbarkeit möglich ist, folgt aus Abschnitt~\ref{sec:pomdp}.
Dass es bei einer Politik \emph{mit} Gedächtnis trotzdem erklärungsbedürftig
bleibt, ebenfalls.

\subsubsection{Rekurrente Politiken (LSTM)}
\label{sec:lstm}

Alle bisher genannten Verfahren laufen auch mit einem gewöhnlichen
vorwärtsgerichteten neuronalen Netz, einem \emph{Multilayer Perceptron}
(MLP). Ein solches Netz bildet jede Beobachtung unabhängig auf eine Aktion ab
und besitzt kein Gedächtnis, weshalb es nicht unterscheiden kann, ob es eine
Stelle zum ersten oder zum zehnten Mal sieht. Unter partieller Beobachtbarkeit
ist das eine harte Einschränkung (siehe Abschnitt~\ref{sec:pomdp}). Die
Bezeichnung \emph{MLP} steht im Folgenden durchgängig für diese gedächtnislose
Variante.

Um in einem POMDP ein Gedächtnis bereitzustellen, wird die Politik um ein
rekurrentes Netz erweitert. Long Short-Term Memory (LSTM) ist eine rekurrente
Architektur, die Information über viele Zeitschritte hinweg hält
\cite{hochreiter1997lstm}, und ihr Zustand approximiert den Belief-State, indem
er die bisher gesehenen Beobachtungen zusammenfasst. Rekurrentes Netz und
wertbasiertes Reinforcement Learning wurden als DRQN kombiniert
\cite{hausknecht2015drqn}; hier kommt die policy-gradient-Variante
RecurrentPPO zum Einsatz.

Wie das Gedächtnis wirkt, zeigt Abbildung~\ref{fig:lstm_unrolled} in der über
die Zeit \glqq entrollten\grqq{} Darstellung. Der verborgene Zustand $h_t$
wandert von Schritt zu Schritt weiter und sammelt Information, die in der
einzelnen Beobachtung fehlt. Für Stoneforge ist das genau die Information
\glqq diese Stelle wurde bereits besucht\grqq{} beziehungsweise \glqq diese
Wand wurde bereits nach Norden abgelaufen\grqq.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  cell/.style={draw, rounded corners=2pt, minimum width=1.15cm, minimum height=0.8cm,
               font=\small, fill=black!7},
  io/.style={font=\scriptsize, align=center},
  >={Stealth[length=2mm]}, node distance=1.5cm
]
  \foreach \i/\lbl in {1/{t{-}1}, 2/{t}, 3/{t{+}1}} {
    \node[cell] (c\i) at (2.3*\i, 0) {LSTM};
    \node[io, below=0.7cm of c\i] (o\i) {Beobachtung\\$o_{\lbl}$};
    \node[io, above=0.7cm of c\i] (a\i) {Aktion\\$a_{\lbl}$};
    \draw[->] (o\i) -- (c\i);
    \draw[->] (c\i) -- (a\i);
  }
  \draw[->, thick] (c1) -- node[above, font=\scriptsize] {$h_{t-1}$} (c2);
  \draw[->, thick] (c2) -- node[above, font=\scriptsize] {$h_{t}$} (c3);
  \draw[->, thick, dashed] (1.35,0) -- (c1);
  \draw[->, thick, dashed] (c3) -- (8.6,0);
  \node[font=\scriptsize, anchor=south] at (8.1,0.12) {$h_{t+1}$};
\end{tikzpicture}
\caption[Rekurrente Politik über die Zeit]{Grundlagen - Über die Zeit entrollte
rekurrente Politik, bei der jeder Zeitschritt neben der aktuellen Beobachtung
$o_t$ den verborgenen Zustand $h_{t-1}$ des Vorschritts erhält, eigene
Darstellung in Anlehnung an Hausknecht und Stone \cite{hausknecht2015drqn}.}
\label{fig:lstm_unrolled}
\end{figure}

Trainiert wird ein solches Netz per \emph{Backpropagation Through Time} (BPTT),
indem das Netz über eine Sequenz fester Länge entrollt wird und der Gradient
über alle Zeitschritte zurückläuft. Die Sequenzlänge ist damit ein
Hyperparameter mit zwei Seiten. Zu kurze Sequenzen begrenzen, wie weit zurück
ein Zusammenhang überhaupt gelernt werden kann, zu lange Sequenzen lassen
Gradienten verschwinden und bremsen das Training.

Dazu kommt die Frage, mit welchem verborgenen Zustand eine Sequenz startet.
Kapturowski et al.\ zeigen für rekurrente Verfahren, dass die Behandlung dieses
Zustands die Leistung bei sonst unveränderter Architektur erheblich verschiebt,
und schlagen unter anderem eine Einlaufphase (\emph{Burn-in}) vor
\cite{kapturowski2019r2d2}. Bei der Auswertung stellt sich dieselbe Frage, denn
ein zwischen Episoden nicht zurückgesetzter Zustand ergibt die Messung einer
anderen als der trainierten Politik.

\subsubsection{Prozedurale Welten im Reinforcement Learning}
\label{sec:g_bridge}

Die beiden vorangegangenen Abschnitte behandeln zwei eigenständige Themen, die
prozedurale Erzeugung von Welten und das Lernen einer Politik unter partieller
Beobachtbarkeit. Im Folgenden laufen sie zusammen, weil erst ihre Verbindung
den Versuchsaufbau dieser Arbeit begründet.

Der Ausgangspunkt ist ein bekanntes Problem des maschinellen Lernens. Wird ein
Modell stets auf denselben Daten trainiert und geprüft, lernt es womöglich die
Antworten auswendig (\emph{Overfitting}), statt eine übertragbare Regel zu
bilden. Im Reinforcement Learning entspricht ein einzelnes festes Level solchen
festen Daten, weshalb die Politik dann die konkrete Karte memoriert, ohne
allgemeine Navigation zu erlernen \cite{cobbe2020procgen, kirk2023survey}.

Bekommt dagegen jede Episode eine neue, per Seed erzeugte Welt, entzieht sich
die Aufgabe dem Auswendiglernen, da der Agent jede einzelne Karte zu selten
sieht, um sie zu behalten. Er muss eine Strategie bilden, die über die
Trainingswelten hinaus trägt. Prozedurale Generierung ist deshalb ein
etabliertes Mittel, um Generalisierung im Reinforcement Learning zu prüfen und
Overfitting auf einzelne Level zu verhindern \cite{cobbe2020procgen,
risi2020increasing}.

Die Seed-Reproduzierbarkeit aus Abschnitt~\ref{sec:pcg_generalisierung} liefert
dafür das Werkzeug, weil sie disjunkte Seed-Mengen und damit eine strenge
Trennung von Trainings- und Testmenge erlaubt. Die Evaluation auf
trainingsfremden Seeds folgt genau diesem Prinzip. Gemessen wird nicht, wie gut
eine Politik gelernte Welten wiederfindet, sondern wie gut sie auf nie gesehene
überträgt.

Die Verbindung reicht tiefer als bis zum Versuchsaufbau. Die Forderung, auf
unbekannten Welten zu funktionieren, erzeugt selbst partielle Beobachtbarkeit,
da der Politik zur Laufzeit unbekannt ist, in welcher der möglichen Welten sie
steckt (siehe Abschnitt~\ref{sec:pomdp}). Ghosh et al.\ zeigen, dass daraus
auch dann ein POMDP wird, wenn jede einzelne Welt für sich vollständig
beobachtbar wäre, und nennen das \emph{epistemic POMDP}
\cite{ghosh2021epistemic}.

Die Unsicherheit liegt hier über der Identität der Welt und nicht über dem
Zustand innerhalb einer bekannten Welt. Mehr Gedächtnis über die laufende
Episode löst sie deshalb nicht auf. Auch die optimale Politik eines epistemic
POMDP ist im Allgemeinen stochastisch, was für die spätere Untersuchung des
Det/Stoch-Gaps zählt. Prozedurale
Generierung und Lernen unter partieller Beobachtbarkeit sind für diese Arbeit
also keine zufällig kombinierten Bausteine. Die Generalisierungsanforderung
verknüpft sie ursächlich.

\subsubsection{Reward Shaping (PBRS)}
\label{sec:pbrs}

Zusätzliche Belohnungen können das Lernen beschleunigen, dürfen die optimale
Politik aber nicht verändern. Potential-Based Reward Shaping (PBRS) leistet
genau das, denn der Zusatzreward hat die Form
$F(s,s') = \gamma\,\Phi(s') - \Phi(s)$ mit einer Potentialfunktion $\Phi$. Ng
et al.\ zeigen, dass diese Form policy-invariant ist und die optimale Politik
daher unangetastet lässt \cite{ng1999policy}. In dieser Arbeit hängt $\Phi$ an
der BFS-Distanz zum Exit, also an der Länge des kürzesten begehbaren Wegs vom
Agenten zum Exit in Schritten. Anders als die Luftlinie berücksichtigt dieses
Maß Wände und Umwege, weshalb es dem Potential zugrunde liegt.

\subsubsection{Curriculum Learning}
\label{sec:curriculum}

Curriculum Learning trainiert mit ansteigender Aufgabenschwierigkeit statt in
zufälliger Reihenfolge \cite{bengio2009curriculum}. Hier wird die Exit-Distanz
stufenweise erhöht, wobei der Übergang zwischen den Stufen leistungsbasiert
läuft und eine Stufe ab einer Ziel-Erfolgsquote als bestanden gilt.

Davon zu unterscheiden ist die Steuerung auf Ebene einzelner Welten.
\emph{Prioritized Level Replay} (PLR) wiederholt gezielt jene Level, an denen
eine Politik noch am meisten lernen kann, statt sie gleichverteilt zu ziehen,
und verbessert dadurch Sample-Effizienz und Generalisierung \cite{jiang2021plr}.
Auf die Auswahlrichtung kommt es an, weil Level mit hohem Lernpotenzial
wiederholt werden und gerade nicht die bereits gemeisterten.

\subsubsection{Messen und Bewerten}
\label{sec:g_messen}

Die bisherigen Abschnitte behandeln, wie Welten entstehen und wie Politiken
lernen. Nun folgt die Frage, woran sich beides beurteilen lässt. Das ist kein
Nebenschauplatz, weil zwei der drei Befunde dieser Arbeit nicht das Verhalten
des Agenten betreffen, sondern die Aussagekraft einer Messung.

\subsubsection{Metriken einer Navigationsaufgabe}
\label{sec:g_metriken}

\paragraph{Erfolgsquote.}
Die naheliegende Metrik ist der Anteil gelöster Welten. Formal ist sie der
Schätzwert $\hat p = k/n$ eines Bernoulli-Parameters, denn jede Welt ist ein
Versuch, der gelingt oder nicht. Daraus folgen zwei Eigenschaften unmittelbar.
Erstens ist die Erfolgsquote nur zusammen mit dem Abbruchkriterium definiert,
da von der erlaubten Suchdauer abhängt, ob eine Welt als gelöst zählt; das
Episodenlimit gehört damit zur Metrik und nicht zu ihrem Umfeld. Zweitens ist
$\hat p$ bei kleinem $n$ ungenau, wie Abschnitt~\ref{sec:g_streuung} beziffert.

\paragraph{Pfadeffizienz.}
Die Erfolgsquote sagt nichts darüber, \emph{wie} eine Welt gelöst wurde. Dafür
dient die Pfadeffizienz
\begin{equation}
\eta \;=\; \frac{d^{*}(s_0)}{T},
\end{equation}
das Verhältnis aus kürzestem Weg $d^{*}$ vom Startzustand und tatsächlich
benötigten Schritten $T$. Es gilt $0 < \eta \leq 1$, wobei $\eta = 1$ optimale
Navigation und $\eta = 0{,}05$ einen zwanzigfachen Umweg bedeutet. Zwei
Feinheiten sind zu beachten, weil die Größe sonst als Rechenfehler gelesen
wird.

Zum einen macht es einen Unterschied, ob über die \emph{Quotienten} der
Einzelepisoden gemittelt oder der Quotient der Mittelwerte gebildet wird. Beide
Größen fallen auseinander, sobald die Schrittzahl schief verteilt ist. Da
$1/T$ konvex in $T$ ist, liefert die Jensen-Ungleichung
\begin{equation}
\mathbb{E}\!\left[\frac{d^{*}}{T}\right] \;\geq\; \frac{d^{*}}{\mathbb{E}[T]},
\end{equation}
das Mittel der Quotienten liegt also systematisch höher. Welche Variante
berichtet wird, gehört daher in den Text.

Zum anderen ist $\eta$ nur auf erfolgreichen Episoden definiert. Werden zwei
Politiken mit unterschiedlicher Erfolgsquote verglichen, wird die schwächere auf
der leichteren Teilmenge bewertet. Die Verzerrung arbeitet also zugunsten der
schwächeren Politik. Bleibt sie beim Interpretieren unberücksichtigt, wird eine
Stärke gelesen, wo keine ist.

\paragraph{Betriebsarten.}
Eine gelernte stochastische Politik lässt sich auf zwei Arten ausführen, durch
Ziehen aus $\pi(a \mid s)$ oder durch Wahl der wahrscheinlichsten Aktion
(Argmax). Dazwischen liegt die temperaturskalierte Auswertung, die die Logits
vor dem Softmax durch einen Parameter $T$ teilt, wobei $T \to 0$ Argmax und
$T = 1$ das ungeänderte Sampling ergibt. Dieselbe Politik kann in beiden
Betriebsarten unterschiedlich abschneiden, weshalb jede Angabe die gemessene
Betriebsart nennen muss. Dass ein solcher Unterschied unter partieller
Beobachtbarkeit theoretisch zu erwarten ist, folgt aus
Abschnitt~\ref{sec:pomdp}.

\subsubsection{Streuung, Konfidenz und die Zahl der Läufe}
\label{sec:g_streuung}

Die Messunsicherheit dieser Experimente hat zwei voneinander unabhängige
Quellen. Werden beide vermischt, entsteht eine Aussage ohne Gehalt.

\paragraph{Unsicherheit innerhalb eines Laufs.}
Wird ein festes Modell auf $n$ Welten geprüft, ist die Erfolgsquote ein
Bernoulli-Anteil mit Standardfehler $\sqrt{p(1-p)/n}$. Für $n = 50$ und den
ungünstigsten Fall $p = 0{,}5$ ergibt das ein 95\,\%-Konfidenzintervall von rund
$\pm 14$ Prozentpunkten. Unterschiede unterhalb dieser Größe lassen sich aus
einer einzelnen Messung nicht interpretieren, unabhängig davon, wie sorgfältig
sie erhoben wurde.

\paragraph{Unsicherheit zwischen Läufen.}
Zwei Trainingsläufe derselben Konfiguration unterscheiden sich allein durch
Zufallsinitialisierung und Reihenfolge der Erfahrungen. Henderson et al.\
zeigen, dass diese Streuung im Deep Reinforcement Learning so groß ausfällt,
dass Aussagen aus wenigen Läufen systematisch in die Irre führen, und fordern
deshalb Mittelwert und Standardabweichung über mehrere unabhängige Läufe statt
des besten Laufs \cite{henderson2018matters}. Für die Aggregation über $m$
Läufe gilt die \emph{Stichproben}-Standardabweichung,
\begin{equation}
s \;=\; \sqrt{\frac{1}{m-1}\sum_{i=1}^{m}\left(x_i - \bar{x}\right)^2},
\end{equation}
mit dem Nenner $m-1$ statt $m$ (Bessel-Korrektur), da die Läufe eine Stichprobe
aus allen möglichen Läufen sind und nicht die Grundgesamtheit. Das zugehörige
Konfidenzintervall folgt bei kleinem $m$ der $t$-Verteilung mit $m-1$
Freiheitsgraden und nicht der Normalverteilung.

\paragraph{Vergleich zweier Gruppen.}
Sollen zwei Chargen von Läufen verglichen werden, etwa um zu prüfen, ob ein
Unterschied eine systematische Ursache hat oder bloß Streuung ist, eignet sich
der Welch-Test. Er ist die Variante des $t$-Tests, die ungleiche Varianzen und
ungleiche Gruppengrößen zulässt, und damit die konservative Wahl, solange über
die Varianzen nichts bekannt ist. Ein Testergebnis ohne Signifikanz auf dem
gewählten Niveau belegt keine Gleichheit, sondern besagt nur, dass die Daten
einen Unterschied nicht belegen.

\paragraph{Vorabfestlegung von Schwellen.}
Werden Erfolgskriterien erst nach Kenntnis der Ergebnisse formuliert oder
nachträglich angepasst, verlieren sie ihre Aussagekraft. Kerr nennt dieses
Vorgehen \emph{HARKing} (\emph{Hypothesizing After the Results are Known})
\cite{kerr1998harking}. Das Gegenmittel ist billig, nämlich die dokumentierte
Festlegung vor der Messung. Allgemeiner fassen Pineau et al.\ die Anforderungen an reproduzierbare Berichte
in einer Checkliste zusammen, die neben der Zahl der Läufe auch den
durchsuchten Hyperparameterbereich und die Trennung der Datenmengen umfasst
\cite{pineau2021repro}.

\subsubsection{Referenzpolitiken als Bezugspunkt}
\label{sec:g_baselines}

Ein Messwert ohne Bezugspunkt ist nicht interpretierbar. Ob eine Erfolgsquote
von 65\,\% eine gelernte Fähigkeit ausdrückt, lässt sich nur beantworten, wenn
bekannt ist, was ohne Lernen erreichbar wäre. Dafür dienen \emph{ungelernte
Referenzpolitiken}, die dieselbe Beobachtung erhalten und unter demselben
Protokoll laufen. Zu unterscheiden sind zwei Typen.

Eine \textbf{Zufallspolitik} wählt gleichverteilt aus dem Aktionsraum und
markiert die absolute Untergrenze. Sie beantwortet, ob überhaupt etwas gelernt
wurde, nicht aber, ob das Gelernte über triviale Strategien hinausgeht.

Eine \textbf{Heuristik} nutzt einen Teil der Beobachtung nach einer fest
programmierten Regel, ohne Parameter und ohne Training. Sie ist der schärfere
Bezugspunkt, weil sie ausdrückt, welcher Anteil des Ergebnisses schon ohne
Lernen zu haben ist. Übertrifft eine solche Heuristik den trainierten Agenten,
liegt das Problem nicht beim Agenten, sondern bei der Aufgabenstellung, da diese
die zu messende Fähigkeit gar nicht verlangt. Genau dieser Fall tritt in dieser
Arbeit ein.

Damit bekommt die Referenzpolitik eine zweite Rolle, die über den Vergleich
hinausgeht. Sie taugt als \emph{Kalibrierinstrument}, das die Schwierigkeit
einer Umgebung vor jedem Training bestimmt, in Sekunden statt in
Trainingsstunden. Kirk et al.\ argumentieren in dieselbe Richtung, wenn sie
einen rein prozeduralen Ansatz beim Benchmark-Entwurf als hinderlich für den
Fortschritt der Generalisierungsforschung bezeichnen \cite{kirk2023survey}. Ohne
ein Instrument, das trennt, warum ein Verfahren gelingt oder scheitert, sagt der
Messwert allein wenig.
\section{Methodik (gemeinsam)}
\label{sec:methodikkapitel}

\subsection{Methodik der Weltgenerierung (Laurin)}
\label{sec:methodikwelt}

Für die Erzeugung der Welten wird eine Kombination etablierter Verfahren der
prozeduralen Inhaltserzeugung gewählt: rauschbasierte Basisbelegung, ein
zellulärer Automat zur Formgebung und eine graphenbasierte
Erreichbarkeitsprüfung (Grundlagen in Abschnitt~\ref{sec:pcg_algorithmen}).
Maßgeblich für die Auswahl sind drei Designziele: Die Welt muss spielbar
sein, ihre Schwierigkeit muss steuerbar sein, und ihre Lösbarkeit muss für
jede einzelne generierte Welt feststehen, nicht nur im Mittel über viele
Welten (Abschnitt~\ref{sec:relatedwork}). Letzteres schließt eine
nachträgliche Erreichbarkeitsprüfung aus; die Lösbarkeit wird stattdessen
durch die Konstruktion selbst erzwungen, indem der Exit gezielt aus dem
Baum der von der Startposition erreichbaren Felder gewählt wird
(Abschnitt~\ref{sec:loesbarkeit}).

Die Schwierigkeit einer generierten Welt wird nicht geschätzt, sondern
gemessen: über die Wanddichte und den Umwegfaktor (BFS-Distanz geteilt
durch Manhattan-Distanz) sowie über eine ungelernte BFS-Orakel-Politik als
Referenz. Diese Methodik erlaubt es, Eingriffe in den Generator vor jedem
Training zu bewerten, statt ihre Wirkung erst am Agenten abzulesen; die
gemessenen Werte stehen in Tabelle~\ref{tab:geometrieprobe} und werden in
Abschnitt~\ref{sec:evalplattform} gegen die Anforderungen der Arbeit
gestellt.

Die konkrete technische Umsetzung, also Chunk-Struktur, Rauschfunktion,
Automatenregeln und Exit-Konstruktion im Code, folgt in
Abschnitt~\ref{sec:umgebung}.

\subsection{Methodik des RL-Trainings (Florian)}
\label{sec:methodik}

Dieser Abschnitt legt fest, mit welchem Lernverfahren, welchem Trainingsplan
und welchem Messverfahren die Forschungsfrage F2 beantwortet werden soll,
bevor in Kapitel~\ref{sec:implementierung} die technische Umsetzung und in
Kapitel~\ref{sec:evaluation} die Ergebnisse folgen. Keine dieser Festlegungen
entstand in einem einzigen Entwurf: Mehrere Annahmen mussten im
Projektverlauf revidiert werden, wenn sich ein Vorgehen beim Ausprobieren als
ungeeignet erwies (Überblick in Tabelle~\ref{tab:versionsverlauf}).

\subsubsection{Wahl des Lernverfahrens}

Trainiert wird ein rekurrentes Actor-Critic-Verfahren: RecurrentPPO, eine
LSTM-Erweiterung von PPO \cite{schulman2017ppo, hochreiter1997lstm},
umgesetzt über die Bibliothek Stable-Baselines3 \cite{raffin2021stable}.
Drei Eigenschaften der Aufgabe führen zu dieser Wahl.

Erstens ist der Aktionsraum diskret, was sowohl wertbasierte als auch
policy-basierte Verfahren erlaubt und die Wahl allein noch nicht entscheidet.
Zweitens ist die Aufgabe partiell beobachtbar (Abschnitt~\ref{sec:pomdp}):
Der Agent sieht nur einen Ausschnitt der Welt, wofür eine Politik mit
Gedächtnis im Allgemeinen besser geeignet ist als eine rein reaktive.
Drittens war die Umgebung während der Entwicklung selbst noch fehlerbehaftet;
ein off-policy-Verfahren trägt die Folgen eines zwischenzeitlich fehlerhaften
Reward-Signals über dessen Korrektur hinaus im Replay-Buffer mit, ein
on-policy-Verfahren wie PPO nicht. Diese Robustheit gegenüber eigenen
Fehlern gab den Ausschlag.

Die Trainings-Infrastruktur unterstützt zusätzlich DQN und A2C als mögliche
Vergleichsverfahren; sie wurden auf der finalen Umgebung nicht vermessen, was
als Grenze der Arbeit ausgewiesen wird (Abschnitt~\ref{sec:ausblick}). Die
vollständige Parameterbelegung steht in Tabelle~\ref{tab:basiskonfig} am Ende
dieses Abschnitts.

\subsubsection{Curriculum}

Ein Agent, der von Anfang an auf der vollen Zielentfernung von 35 bis 45
Feldern trainiert wird, erreicht den Exit fast nie und erhält dadurch kaum
verwertbares Lernsignal. Das Training wird deshalb in vier Phasen mit
wachsender Exit-Distanz gegliedert (Curriculum Learning,
Abschnitt~\ref{sec:curriculum}). Eine Phase endet, sobald die Politik eine
feste Ziel-Erfolgsquote erreicht, spätestens aber nach einem festen
Schrittbudget; Abbildung~\ref{fig:curriculum_ablauf} zeigt den Ablauf,
Tabelle~\ref{tab:curriculum} die genauen Distanzbereiche, Budgets und
Ziel-Erfolgsquoten je Phase.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, minimum width=2.7cm, minimum height=1.7cm,
              align=center, font=\scriptsize},
  >={Stealth[length=2.2mm]}, node distance=1.0cm
]
  \node[box] (p1) {\textbf{Phase 1}\\5--12 Felder\\Ziel 85\,\% stoch.};
  \node[box, right=of p1] (p2) {\textbf{Phase 2}\\12--25 Felder\\Ziel 70\,\% stoch.};
  \node[box, right=of p2] (p3) {\textbf{Phase 3}\\25--45 Felder\\Ziel 70\,\% det.};
  \node[box, right=of p3] (p4) {\textbf{Phase 4}\\Fine-Tune\\Ziel 60\,\% det.};
  \draw[->, thick] (p1) -- (p2) node[midway, above, font=\tiny] {Ziel-SR o.\ Budget};
  \draw[->, thick] (p2) -- (p3) node[midway, above, font=\tiny] {Ziel-SR o.\ Budget};
  \draw[->, thick] (p3) -- (p4) node[midway, above, font=\tiny] {Ziel-SR o.\ Budget};
\end{tikzpicture}
\caption[Ablauf des leistungsbasierten Curriculums]{Methodik - Ablauf des
leistungsbasierten Curriculums über vier Phasen, eigene Darstellung.}
\label{fig:curriculum_ablauf}
\end{figure}

Der wachsende Abstand verändert nebenbei, wie sichtbar das Ziel für den
Agenten überhaupt ist. Eine Messung über 200 frisch erzeugte Welten je Phase
zeigt: Der Exit liegt beim Episodenstart in Phase~1 noch in 72\,\% der Welten
im Sichtfeld, in Phase~2 nur noch in 6\,\%, in Phase~3 in keiner. Das
Curriculum führt die Politik damit implizit von einfacher Zielsuche im
Sichtfeld zu langreichweitiger Kompass-Navigation mit Gedächtnis, ohne dass
das Sichtfeld selbst verändert werden muss. Ein zusätzliches Curriculum über
die Sichtfeldgröße wäre daher redundant und zudem riskant, weil ein großes
Sichtfeld anfangs eine rein reaktive Strategie belohnen würde, die beim
späteren Verkleinern wertlos wird.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lccrlll@{}}
\toprule
 & \multicolumn{2}{c}{\textbf{Exit-Distanz}} & & & & \\
\cmidrule(lr){2-3}
\textbf{Phase} & \textbf{Training} & \textbf{Eval} & \textbf{Budget} & \textbf{Ziel-SR} & \textbf{Gating} & \textbf{ent\_coef} \\
\midrule
1 & 5--12 & 5--12 & 500k & 85\,\% & stoch. & 0.05 \\
2 & 12--25 & 12--25 & 500k & 70\,\% & stoch. & 0.05 \\
3 & 25--45 & 35--45 & 1{,}0M & 70\,\% & det. & 0.05 $\to$ 0.001 \\
4 & 25--45 & 35--45 & 200k & 60\,\% & det. & 0.0001 \\
\bottomrule
\end{tabular}
\caption[Curriculum in vier Phasen]{Methodik - Distanzbereiche, Schrittbudget,
Ziel-Erfolgsquote und Gating-Metrik der vier Curriculum-Phasen (Stand v11),
eigene Darstellung.}
\label{tab:curriculum}
\end{table}

Das Distanz-Curriculum staffelt dabei implizit auch die
\emph{Beobachtbarkeit} des Ziels, und damit die Fähigkeit, die je Phase
gelernt werden muss. Eine Messung über je 200 frisch erzeugte Welten pro Phasenkonfiguration
ergibt (eine Eigenschaft des Generators, unabhängig von den
Evaluations-Seedmengen):
In Phase~1 liegt der Exit beim Episodenstart in \textbf{72\,\%} der Welten
bereits im $15\times15$-Sichtfeld (zu lernen: \glqq geh zum sichtbaren Ziel,
weiche lokalen Hindernissen aus\grqq); in Phase~2 nur noch in \textbf{6\,\%}
(zu lernen: dem Kompass folgen, Umwege um Hindernisse); in Phase~3 in
\textbf{0\,\%} (reine Kompass-Navigation über lange Distanz, inklusive
Sackgassen, die nur mit Gedächtnis überwindbar sind). Ein zusätzliches
Curriculum über die Sichtfeldgröße (in der Literatur als Übergang von
voller zu partieller Beobachtbarkeit beschrieben) wäre daher redundant;
zudem wäre es hier riskant, weil ein groß\-es Sichtfeld anfangs eine
\emph{reaktive} Strategie ohne Gedächtnisnutzung belohnt, die beim
Verkleinern des Fensters wertlos wird (dieselbe Nicht-Übertragbarkeit, die
die Gegenüberstellung in Abschnitt~\ref{sec:ablation} für das BFS-Orakel
zeigt).

\subsubsection{Evaluationsprotokoll}
\label{sec:evalprotokoll}

Alle in dieser Arbeit berichteten Messungen folgen dem hier festgelegten
Protokoll. Mehrere seiner Festlegungen, insbesondere das Episodenlimit und
die Seed-Trennung, haben sich als ergebnisbestimmend erwiesen.

\paragraph{Metrik.}
Zentrale Metrik ist die \emph{Erfolgsquote} (\emph{Success Rate}, SR): der
Anteil von 50 festen, trainingsfremden Welten, in denen der Agent den Exit
innerhalb des Episodenlimits erreicht. Ergänzend wird die \emph{Pfadeffizienz}
$\eta$ berichtet, das Verhältnis aus kürzestem Weg und tatsächlich benötigten
Schritten (Abschnitt~\ref{sec:baselines}). Als Diagnosemetrik des Trainings
dienen \texttt{explained\_variance} des Critics und \texttt{approx\_kl}. Eine
hohe Erfolgsquote allein ist dabei kaum aussagekräftig: Bei einem
Episodenlimit von 4\,000 Schritten und einer mittleren Optimallänge von rund
40 Feldern erreicht schon eine ungelernte Kompass-Heuristik 92\,\%
(Abschnitt~\ref{sec:baselines}). Die Erfolgsquote wird deshalb stets
zusammen mit der Pfadeffizienz und einer ungelernten Referenzpolitik
berichtet.

\paragraph{Episodenlimit.}
\label{sec:evalfallstricke}
Gemessen wird stets mit dem vollen Limit von 4\,000 Schritten, dem Maximum
der Umgebung: Bei einem Limit von 600 Schritten misst sich dasselbe Modell
nur als 48-\%-, bei 4\,000 Schritten als 86-\%-Agent, weil erfolgreiche
Episoden ohne Orakel lange Suchwege enthalten und ein zu knappes Limit
gerade die kompetenten, aber langsamen Läufe abschneidet. Ein Episodenlimit
gehört deshalb zu jedem berichteten SR-Wert dazu. Zusätzlich bleibt die
Early-Stop-Truncation der Umgebung aktiv (256 Schritte ohne positiven Reward,
Abschnitt~\ref{sec:rlumgebung}) und gilt für Agent und Referenzpolitiken
gleichermaßen (Abschnitt~\ref{sec:baselines}); die berichteten Erfolgsquoten
sind dadurch eine untere Schranke gegenüber einem Protokoll ohne Truncation.

\paragraph{Seed-Trennung.}
Die Seed-Mengen sind disjunkt und fest: Ein Validierungsbereich
(6000--6199, davon 6000--6049 für Phasenwechsel und Modellauswahl) steuert
Training und Diagnose. Testset~A (7000--7049) und Holdout~B (8000--8049)
sind ausschließlich für die finale Messung reserviert und fließen in keine
Auswahl- oder Diagnoseentscheidung ein.

\paragraph{Betriebsarten.}
Jede Evaluation misst beide Betriebsarten, stochastisch (Sampling aus der
Politik) und deterministisch (Argmax). Bei der rekurrenten Politik werden
LSTM-Zustand und Episodenstart-Signal bei jedem Vorhersageschritt korrekt
geführt und zu Beginn jeder Episode zurückgesetzt; wird das versäumt, misst
die Auswertung ein Gedächtnismodell ohne Gedächtnis und damit systematisch
zu niedrige Werte, ohne dass der Fehler auffällt \cite{kapturowski2019r2d2}.

\paragraph{Aggregation und Kurvenvergleich.}
Für das Endergebnis werden sieben unabhängige Trainingsläufe gemittelt und
mit Standardabweichung berichtet, nie der beste Einzellauf
\cite{henderson2018matters}. Vergleiche zwischen Konfigurationen werden
zudem nur auf ganzen Eval-Kurven gezogen, nie auf einzelnen Endständen, weil
die Lerndynamik dieser Umgebung stark schwankt: Eine Änderung der
Batch-Größe (8 auf 64) etwa galt nach einem günstigen Messpunkt zunächst als
Verbesserung, erwies sich bei Betrachtung der gesamten Kurve aber als
destabilisierend.

\subsubsection{Versuchsaufbau}
\label{sec:versuchsuebersicht}

\paragraph{Herkunft der Erfolgsschwellen.}
\label{sec:kriterienrevision}
Die in Abschnitt~\ref{sec:ziele} genannten Schwellen (70\,\% auf Testset~A,
60\,\% auf Holdout~B) ersetzen die im Exposé angesetzten $\geq 90\,\%$ über
100 unbekannte Seeds und stehen bereits seit dem 16.05.2026 im
Experiment-Changelog, also vor den finalen Läufen und ohne Kenntnis der
späteren Ergebnisse (kein Fall von \emph{HARKing} \cite{kerr1998harking}).
Drei zu Projektbeginn unbekannte Eigenschaften der Aufgabe machen einen Wert
nahe 90\,\% unerreichbar: die partielle Beobachtbarkeit der Umgebung
\cite{singh1994pomdp, ghosh2021epistemic}, die vor der Umgebungsrevision v11
als Luftlinie unterschätzte Weglänge (Tabelle~\ref{tab:versionsverlauf}) und
eine Streuung von rund $\pm 12$ Prozentpunkten über Trainingsläufe
(Tabelle~\ref{tab:v12}). Dass am Ende nur das Holdout-, nicht aber das
Testset-Kriterium erreicht wird, zeigt: Auch die revidierte Schwelle war
keine bequeme. Gegen die ursprüngliche Exposé-Metrik gemessen verfehlen
Testset~A und Holdout~B mit $66{,}3\,\% \pm 12{,}1$ (stochastisch) die
90\,\%-Schwelle deutlich, die zweite Zielgröße (minimale Schritte pro
Episode) mit einer Pfadeffizienz von $\eta \approx 0{,}05$
(Abschnitt~\ref{sec:baselines}) noch deutlicher.

Alle Läufe der Arbeit teilen die in Tabelle~\ref{tab:basiskonfig} gelistete
Basiskonfiguration. Je Lauf existiert eine \texttt{config.json} mit genau
diesen Werten; die vollständige Historie inklusive Zwischenständen liegt im
Experiment-Changelog.

Auf dieser Basis wurden drei Varianten mit je einem veränderten inhaltlichen
Faktor gerechnet (E1 Critic-Gewicht, E2 Curriculum-Zuschnitt, E3
Gedächtnisgröße). Da sie einer Fragestellung dienen, die sich erst aus den
Messergebnissen stellt, sind Setup und Befund gemeinsam in
Abschnitt~\ref{sec:gapexperimente} dargestellt.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Komponente} & \textbf{Wert} \\
\midrule
Algorithmus & RecurrentPPO (SB3-contrib), \texttt{MlpLstmPolicy} \\
$n_\text{steps}$ / \texttt{batch\_size} / $n_\text{epochs}$ & 256 / 8 / 10 \\
Lernrate / $\gamma$ / $\lambda_\text{GAE}$ / clip & $3\cdot10^{-4}$ / 0.999 / 0.95 / 0.2 \\
\texttt{ent\_coef} & 0.05 (Phase 3: Annealing auf 0.001; Phase 4: 0.0001) \\
\texttt{vf\_coef} & 0.5 \\
LSTM & 1 Schicht, 256 Hidden Units, separater Critic-LSTM \\
Beobachtung & 229 Features (Grid $15\times15$ + HP + Kompass + Step-Frac), Env v11 \\
Parallele Umgebungen & 16 (DummyVecEnv), Device CPU \\
Seed-Pool (\glqq Swarm\grqq) & aktiv, Sampling-Wahrscheinlichkeit 0.3 \\
Curriculum & 4 Phasen (Tab.~\ref{tab:curriculum}), leistungsbasiert mit Schrittbudget \\
Eval & Testset A 7000--7049, Holdout B 8000--8049, Cap 4000, det + stoch \\
\bottomrule
\end{tabular}
\caption[Gemeinsame Basiskonfiguration aller Läufe]{Methodik - Gemeinsame
Basiskonfiguration aller v12-Läufe, eigene Darstellung.}
\label{tab:basiskonfig}
\end{table}

\section{Implementierung (gemeinsam)}
\label{sec:implementierung}

Kapitel~\ref{sec:methodikkapitel} hat begründet, welche Verfahren gewählt
wurden und warum. Dieses Kapitel zeigt die technische Umsetzung dieser
Entscheidungen, in zwei aufeinander aufbauenden Abschnitten:
Abschnitt~\ref{sec:umgebung} behandelt das Spiel, Abschnitt~\ref{sec:rlumgebung}
die darauf aufsetzende Lernumgebung. Beide Teile sind technische
Ingenieursleistungen gleichen Gewichts und Bestandteil derselben
Implementierung; die Trennung beschreibt die Zuständigkeit
(Tabelle~\ref{tab:arbeitsteilung}), nicht eine Rangfolge.

\subsection{Implementierung des Spiels Stoneforge (Laurin)}
\label{sec:umgebung}

Stoneforge ist ein selbst entwickeltes, rundenbasiertes 2D-Spiel: ein
C++-Simulationskern, ein grafischer Client, in dem ein Mensch das Spiel
tatsächlich spielen kann, und eine Python-Anbindung über pybind11, über die ein
Lernverfahren \emph{dieselbe} Simulation ansteuert. Was der Agent erlebt, ist
damit bis auf die Wahrnehmung identisch mit dem, was ein Spieler erlebt.

Dieser Abschnitt behandelt das Spiel: den
Simulationskern, den Weltgenerator und den grafischen Client. Er endet mit
einer Welt, die reproduzierbar, lösbar und in ihrer Zielentfernung steuerbar
ist, aber von Reinforcement Learning noch nichts weiß.
Abschnitt~\ref{sec:rlumgebung} setzt darauf auf und macht daraus eine
Lernumgebung. Der Schwerpunkt dieses Abschnitts liegt auf dem Weltgenerator
(Abschnitt~\ref{sec:weltgenerator}), weil er festlegt, wie die Welten
aussehen, in denen später alles gemessen wird.

\subsubsection{Technologie-Stack}
\label{sec:technik}

Das System besteht aus drei Schichten: einem C++-Kern für Simulation und
Weltgenerierung, einem Python-Binding und dem Python-Trainingsstack.
Tabelle~\ref{tab:stack} listet die verwendeten Komponenten.

\begin{table}[h]
\centering
\footnotesize
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Komponente} & \textbf{Technologie} & \textbf{Zweck} \\
\midrule
Simulationskern & C++20 (ca.\ 4\,300 LOC) & Weltgenerierung, Spiellogik, Reward, BFS \\
Binding & pybind11 2.13 & C++-Kern als Python-Modul (\texttt{stoneforge\_sim}) \\
Build & CMake & Kern, Binding, Clients \\
RL-Umgebung & Python 3.12, Gymnasium 1.2 & \texttt{StoneforgeWorldEnv} (Gym-API) \\
RL-Algorithmen & Stable-Baselines3 / sb3-contrib 2.8 & PPO, DQN, A2C, RecurrentPPO \\
Deep Learning & PyTorch 2.12 (CPU) & Netz- und LSTM-Training \\
Spiel-Client & raylib 5.5 (C++, ca.\ 4\,500 LOC) & spielbarer Client, KI-Zuschaumodus \\
Monitoring & TensorBoard, WebSockets & Trainingsmetriken, Live-Visualisierung \\
Auswertung & NumPy, Matplotlib & Evaluation, Abbildungen \\
\bottomrule
\end{tabular}
\caption[Technologie-Stack]{Implementierung - Technologie-Stack der
Simulation und des Trainings, eigene Darstellung.}
\label{tab:stack}
\end{table}

Die Simulation ist deterministisch pro Seed. Training und Evaluation laufen
vollständig auf der CPU; ein Vergleich mit der GPU ergab für dieses kleine Netz
keine Beschleunigung. Der Durchsatz liegt je nach Konfiguration bei ca.\
88--190 Umgebungsschritten pro Sekunde; die für die Endergebnisse verwendete
Konfiguration (\texttt{batch\_size}=8) liegt am unteren Ende dieser Spanne. Die
verwendete Hardware und der zugehörige Benchmark sind in einem kontrollierten
Vergleich beider Geräteklassen ermittelt worden.

\subsubsection{Die Bausteine: Tile, Chunk und Welt}
\label{sec:bausteine}

Bevor von Generierung die Rede sein kann, braucht es die drei Dinge, die
erzeugt werden. Sie bauen streng aufeinander auf.

\paragraph{Das Tile.} Die kleinste Einheit der Welt ist ein Feld auf einem
ganzzahligen Gitter. Ein Tile trägt genau einen Typ aus einer Aufzählung von
15 Werten (\texttt{TileType} in \texttt{types.hpp}): leer, Wand, Erz, Exit,
Baum, Werkbank, Holzwand, Holzstamm und sieben biomspezifische
Struktur-Tiles. Es gibt keine Höhe, keine Ebenen und keine Teilbelegung, ein
Feld ist genau eine Sache.

Jedem Typ ist ein \texttt{WorldObject} zugeordnet, das seine Eigenschaften
kennt, darunter die für alles Weitere entscheidende Frage, ob man darüber
laufen kann. Die Voreinstellung lautet \emph{nein}: Nur leere Felder und der
Exit sind begehbar, alles andere blockiert. Das klingt nach einem Detail, hat
aber Folgen bis in die Ergebnisse, denn Bäume und Erzadern sind damit
genauso Hindernis wie Wände (Tabelle~\ref{tab:tilecensus}).

\paragraph{Der Chunk.} Tiles werden nicht einzeln verwaltet, sondern in
Blöcken von $8\times8$ Feldern. Ein Chunk ist ein flaches Array aus 64
Tile-Typen plus einem Flag, ob er bereits erzeugt wurde. Mehr nicht.

\paragraph{Die Welt.} \texttt{World} hält eine Hashtabelle von
Chunk-Koordinate auf Chunk. Diese Tabelle ist zu Beginn leer und füllt sich
erst, während gespielt wird: Jeder Zugriff auf ein Tile rechnet die
Weltkoordinate in Chunk- und lokale Koordinate um, und findet sich der Chunk
nicht in der Tabelle, wird er in diesem Moment erzeugt und abgelegt.
Materialisiert wird also nur, was jemand tatsächlich ansieht.

Daraus folgt die zentrale Eigenschaft des Generators, und sie ist bewusst so
gebaut: Die Erzeugung eines Chunks hängt \emph{ausschließlich} vom Seed und
seinen eigenen Koordinaten ab, von keinem anderen Chunk und von keinem
Zustand. Der Generator ist eine reine Funktion
\begin{equation}
  (\mathit{seed}, c_x, c_y) \;\longmapsto\; \text{64 Tile-Typen}.
\end{equation}
Die Welt ist damit praktisch unbegrenzt, ohne je gespeichert zu werden, und
zwei Durchläufe liefern denselben Chunk unabhängig davon, in welcher
Reihenfolge der Spieler die Welt betreten hat.

\paragraph{Warum das nicht nur elegant, sondern notwendig ist.} Genau diese
Reproduzierbarkeit trägt später das Evaluationsprotokoll
(Abschnitt~\ref{sec:evalprotokoll}). Testset~A besteht nicht aus gespeicherten
Karten, sondern aus 50 Zahlen (Seeds 7000 bis 7049). Der Seed \emph{ist} die
Welt. Nur weil derselbe Seed bitgenau dieselben Tiles liefert, lassen sich
sieben Trainingsläufe auf demselben Testset vergleichen, lässt sich ein
Trainingsseed sauber von einem Testseed trennen, und lässt sich eine Episode
im grafischen Client exakt nachspielen. Ein Generator mit einem
fortlaufenden Zufallsgenerator statt einer reinen Funktion würde all das
zerstören, weil dann die Reihenfolge der Aufrufe das Ergebnis mitbestimmt.

\subsubsection{Der Weltgenerator}
\label{sec:weltgenerator}

Damit ist die Frage gestellt, die dieser Abschnitt beantwortet: Wie werden aus
einem Seed und zwei Chunk-Koordinaten 64 Tile-Typen? Die begrifflichen
Grundlagen dazu (Hash- gegenüber Wertrauschen, Domain Warping, zelluläre
Automaten, Breitensuche) stehen in Abschnitt~\ref{sec:pcg_algorithmen}; hier
geht es darum, wie sie zusammengeschaltet sind.

Der Einstieg ist \texttt{World::generateChunk()}, und die Funktion ist kurz
genug, um sie ganz zu zeigen (Listing~\ref{lst:genchunk}). Sie ist die
Landkarte für alles Weitere: Erst wird jedes der 64 Felder einzeln
ausgewürfelt, dann optional geglättet, dann eventuell eine Landmarke
eingestempelt. Mehr passiert nicht. Die Biomwahl steckt dabei in
\texttt{sampleBaseTile} und wird gleich als eigene Stufe behandelt.

\begin{lstlisting}[language=C++,caption={[Die Generator-Pipeline eines
Chunks]Die vollständige Erzeugung eines Chunks
(\texttt{World::generateChunk()}, \texttt{src/core/world.cpp}). Jedes Feld
wird unabhängig aus seinen Weltkoordinaten bestimmt; nichts in dieser
Funktion greift auf einen Nachbar-Chunk oder auf gespeicherten Zustand
zu.},label={lst:genchunk}]
void World::generateChunk(int cx, int cy, Chunk& chunk) const {
    const auto& cfg = gameConfig().world;

    // Stufe 0+1: Basisbelegung, Feld fuer Feld
    for(int ly = 0; ly < kChunkSize; ++ly) {
        for(int lx = 0; lx < kChunkSize; ++lx) {
            const int wx = cx * kChunkSize + lx;   // lokal -> Weltkoordinate
            const int wy = cy * kChunkSize + ly;
            chunk.tiles[ly * kChunkSize + lx] = sampleBaseTile(wx, wy, cfg);
        }
    }
    // Stufe 2: optionale Glaettung (in dieser Arbeit abgeschaltet)
    runCellularSmoothingStage(cx, cy, chunk, cfg);
    // Stufe 3: seltene Landmarke
    placeBiomeStructure(cx, cy, chunk);
}
\end{lstlisting}

\paragraph{Woher der Zufall kommt.} Alle Stufen ziehen ihre Zufallszahlen aus
einer einzigen Funktion, \texttt{noise01(x, y, salt)}
(Listing~\ref{lst:noise}). Sie tut nichts weiter, als ihre drei Argumente zu
einer 64-Bit-Zahl zu verrühren und daraus einen Wert in $[0,1)$ zu machen: Die
Koordinaten und der Seed werden mit großen ungeraden Konstanten multipliziert
und per XOR verknüpft, dann läuft das Ergebnis durch einen Bit-Mixer, dessen
Multiplikatoren aus SplitMix64 stammen \cite{steele2014splitmix}. Der Mixer
sorgt dafür, dass sich benachbarte Eingaben auf völlig verschiedene Ausgaben
verteilen. Die unteren 48 Bit ergeben, skaliert, die Zufallszahl.

\begin{lstlisting}[language=C++,caption={[Die Rauschfunktion des
Generators]Die zustandsfreie Rauschquelle des Generators
(\texttt{src/core/world.cpp}). Der Rückgabewert hängt ausschließlich von den
Argumenten ab, nicht davon, wann oder wie oft die Funktion aufgerufen
wird.},label={lst:noise}]
double World::noise01(int worldX, int worldY, std::uint64_t salt) const {
    std::uint64_t value = static_cast<std::uint64_t>(worldX) * 0x9e3779b185ebca87ULL;
    value ^= static_cast<std::uint64_t>(worldY) * 0xc2b2ae3d27d4eb4fULL;
    value ^= seed_ * 0x165667b19e3779f9ULL;
    value ^= salt;
    value = mix(value);   // SplitMix64-Finalizer

    constexpr double kScale = 1.0 / static_cast<double>(0xFFFFFFFFFFFFULL);
    return static_cast<double>(value & 0xFFFFFFFFFFFFULL) * kScale;
}
\end{lstlisting}

Der Unterschied zu einem gewöhnlichen Zufallszahlengenerator ist der Kern der
Sache. Ein solcher Generator besitzt einen Zustand und liefert seine Werte in
einer festen Reihenfolge; wer den tausendsten Wert braucht, muss die 999
davor ziehen. Damit hinge die Welt davon ab, in welcher Reihenfolge man ihre
Chunks erzeugt, und die in Abschnitt~\ref{sec:bausteine} begründete
Reproduzierbarkeit wäre dahin. \texttt{noise01} hat keinen Zustand. Der Wert
eines Feldes hängt nur von dessen Koordinaten ab, ist also jederzeit,
beliebig oft und in beliebiger Reihenfolge neu berechenbar, immer mit
demselben Ergebnis.

Das dritte Argument, der \emph{Salt}, ist eine feste Konstante je Merkmal. Er
fließt in den Hash ein und erzeugt so aus derselben Koordinate mehrere
voneinander unabhängige Rauschfelder. Der Generator hält getrennte Salts für
Wanddichte, Erz und Bäume bereit. Ohne sie lägen alle drei Merkmale an exakt
denselben Stellen, weil sie dieselbe Zahl auswerten würden.

Damit ist auch schon gesagt, welche Art von Rauschen hier vorliegt: Es ist
\emph{Hash-Rauschen} im Sinne von Abschnitt~\ref{sec:pcg_algorithmen}, also
unkorreliertes weißes Rauschen. Nachbarfelder wissen nichts voneinander.
Abbildung~\ref{fig:noisevergleich} zeigt, was das für die Form der Welt
bedeutet, und der Unterschied ist drastisch: Derselbe Schwellwert schneidet aus
Hash-Rauschen verstreute Einzelfelder, aus interpoliertem Wertrauschen dagegen
zusammenhängende Barrieren. Diese Eigenschaft wird uns am Ende des Abschnitts
wieder einholen.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.78\textwidth]{figures/fig_noise_vergleich.pdf}
\caption[Hash-Rauschen gegenüber interpoliertem Wertrauschen]{Implementierung -
Hash-Rauschen gegenüber interpoliertem Wertrauschen, oben die Rohfelder und
unten die daraus gelesenen Hindernisse, eigene Darstellung.}
\label{fig:noisevergleich}
\end{figure}

\subsubsection*{Stufe 0: Welches Biom hat dieser Chunk?}

Zuerst bekommt der \emph{ganze Chunk} ein Biom, nicht jedes Feld einzeln. Das
ist eine bewusste Vergröberung: Sie hält die Landschaft zusammenhängend, statt
Wald und Wüste feldweise abwechseln zu lassen.

Für das Hash-Rauschen aus Listing~\ref{lst:noise} allein wäre das nicht zu
haben, denn es liefert für benachbarte Chunks völlig unabhängige Werte. Die
Stufe baut deshalb daraus \emph{Wertrauschen}
(Abschnitt~\ref{sec:pcg_algorithmen}): Sie wertet \texttt{noise01} nur an den
Ecken eines groben Stützgitters aus und interpoliert dazwischen bilinear.
Damit die Übergänge nicht als Gitterkanten sichtbar werden, läuft der
Interpolationsparameter zuvor durch Perlins Glättungsfunktion
$3t^2 - 2t^3$ \cite{perlin1985image}. Benachbarte Chunks bekommen so ähnliche
Werte, und es entstehen Regionen statt Flimmern.

Darüber liegen zwei weitere Kunstgriffe (Listing~\ref{lst:biomefield}). Erstens
werden drei solcher Felder mit steigender Frequenz gewichtet addiert
($0{,}62 : 0{,}28 : 0{,}10$); das grobe Feld gibt die Grundform vor, die
feineren rauen die Ränder auf. Zweitens \emph{Domain Warping}: Bevor das Feld
ausgewertet wird, verschieben zwei weitere Rauschfelder die Abtastkoordinate
selbst. Ohne diesen Schritt lägen die Biomgrenzen erkennbar am Stützgitter
und sähen rechteckig aus.

\begin{lstlisting}[language=C++,caption={[Aufbau des Biomfelds]Kern der Stufe 0
(\texttt{World::biomeFieldForChunk()}, \texttt{src/core/world.cpp}, gekürzt).
\texttt{sampleValue} ist das interpolierte Wertrauschen; \texttt{warpX} und
\texttt{warpY} verschieben die Abtastkoordinate, bevor die drei Oktaven
überlagert werden.},label={lst:biomefield}]
double x = static_cast<double>(cx) * 0.22;   // Chunk- -> Feldkoordinate
double y = static_cast<double>(cy) * 0.22;

// Domain Warping: die Abtastkoordinate selbst verrauschen
const double warpX = sampleValue(x * 0.57 + 19.3, y * 0.57 - 7.1, ...);
const double warpY = sampleValue(x * 0.57 - 11.8, y * 0.57 + 13.4, ...);
x += (warpX - 0.5) * 2.8;
y += (warpY - 0.5) * 2.8;

// Drei Oktaven: grobe Form + zwei feinere Detailebenen
const double n1 = sampleValue(x,             y,             ...);
const double n2 = sampleValue(x * 2.1 - 3.7, y * 2.1 + 1.9, ...);
const double n3 = sampleValue(x * 4.2 + 8.4, y * 4.2 - 5.6, ...);

return std::clamp(n1 * 0.62 + n2 * 0.28 + n3 * 0.10, 0.0, 1.0);
\end{lstlisting}

Der Rückgabewert liegt in $[0,1)$ und wird in sieben gleich breite Intervalle
geschnitten. Diese ergeben die Biome Grasland, Wald, Wüste, Bergland, Steppe,
Tundra und Hölle. Weil die Chunk-Koordinaten mit $0{,}22$ skaliert eingehen,
verändert sich das Feld nur langsam: Ein Biom erstreckt sich typischerweise
über vier bis fünf Chunks, also rund 36 Felder, und ist damit deutlich größer
als das $15\times15$-Sichtfenster eines Agenten. Ein Agent sieht also fast nie
eine Biomgrenze, sondern immer nur eine Landschaft.

\subsubsection*{Stufe 1: Was liegt auf diesem einzelnen Feld?}

Jetzt erst wird Feld für Feld entschieden, und hier fällt die Entscheidung, die
für die Navigation zählt: Hindernis oder frei. Der Generator zieht für jedes
Feld drei unabhängige Rauschwerte, einen für Wanddichte, einen für Erz, einen
für Bäume, und vergleicht jeden mit einem Schwellwert, der vom Biom des Chunks
abhängt. Die Schwellwerte stehen vollständig in Tabelle~\ref{tab:biome}, die
Entscheidungslogik in Listing~\ref{lst:basetile}.

\begin{lstlisting}[language=C++,caption={[Belegung eines einzelnen Feldes]Die
Entscheidung über ein einzelnes Feld (\texttt{World::sampleBaseTile()},
\texttt{src/core/world.cpp}, gekürzt). Die Reihenfolge der vier Prüfungen ist
die Regel: See schlägt alles, Erz überschreibt bedingungslos, ein Baum wächst
nur auf einem bis dahin leeren Feld.},label={lst:basetile}]
const double density = noise01(worldX, worldY, cfg.densitySalt);
const double ore     = noise01(worldX, worldY, cfg.oreSalt);
const double trees   = noise01(worldX, worldY, cfg.treeSalt);
// wallThreshold / oreThreshold / treeThreshold: je nach Biom gesetzt

TileType tile = TileType::Empty;

// 1. Seen sind hindernisfrei und schlagen alles Weitere
if(lakeMaskAt(worldX, worldY)) { return TileType::Empty; }
// 2. Wand
if(density < wallThreshold)    { tile = TileType::Wall; }
// 3. Erz, nur im Bergland; ueberschreibt auch eine Wand
if(biomeTag == 3 && ore < oreThreshold) { tile = TileType::Resource; }
// 4. Baum, nur auf noch leerem Feld
if(treeThreshold > 0.0 && tile == TileType::Empty && trees < treeThreshold) {
    tile = TileType::Tree;
}
return tile;
\end{lstlisting}

Zwei Dinge daran sind leicht zu übersehen. Erstens ist die Reihenfolge der
Prüfungen selbst eine Regel und kein Implementierungsdetail: Erz wird
bedingungslos gesetzt und kann eine bereits bestimmte Wand ersetzen, ein Baum
dagegen nur dort wachsen, wo bis dahin nichts liegt. Zweitens ist
\texttt{oreThreshold} für sechs der sieben Biome wirkungslos, weil die Prüfung
zusätzlich \texttt{biomeTag == 3} verlangt. Erz gibt es ausschließlich im
Bergland; die übrigen Zeilen der Erzspalte in Tabelle~\ref{tab:biome} laufen
ins Leere.

Die vorgelagerte Seenmaske ergibt sich aus einer gewichteten Linearkombination
zweier Rauschfelder,
\begin{equation}
  \mathit{Score} = 0{,}75\,a + 0{,}25\,b,
\end{equation}
und weist eine Zelle oberhalb eines harten Cutoffs von $0{,}86$ als Wasser aus.
Beide Felder werden dabei auf ganzzahlig geteilten Koordinaten ausgewertet
($x/7$ bzw.\ $x/3$), sind also blockweise konstant; die Seen sind deshalb
grobkörnig und nicht rund. Sie sind hindernisfrei und überschreiben die
Basisbelegung vollständig.

\paragraph{Ein Chunk, Feld für Feld.} Wie das konkret aussieht, zeigt
Tabelle~\ref{tab:trace} an einer echten Zeile aus einer Eval-Welt. Alle acht
Felder liegen im selben Chunk und damit im selben Biom, sehen also dieselben
Schwellwerte; allein die drei Rauschwerte unterscheiden sie. Feld
$(-32,-31)$ zeigt dabei die Vorrangregel: Die Dichte 0,330 hätte für sich
genommen keine Wand erzeugt, der Erzwert 0,002 setzt trotzdem Erz, weil Erz
eine Wand bedingungslos überschreibt. Die gezeigten Werte stammen aus einem
Nachbau des Generators in Python, der auf 3\,663 geprüften Feldern dieser
Welt ausnahmslos dieselben Tile-Typen liefert wie der C++-Kern.

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}lrrrl@{}}
\toprule
\textbf{Feld} & \textbf{Dichte} & \textbf{Erz} & \textbf{Baum} & \textbf{Ergebnis} \\
 & \tiny{$<0{,}25$?} & \tiny{$<0{,}10$?} & \tiny{$<0{,}02$?} & \\
\midrule
$(-32,-31)$ & 0,330 & \textbf{0,002} & 0,622 & Erz \\
$(-31,-31)$ & 0,401 & 0,249 & \textbf{0,013} & Baum \\
$(-30,-31)$ & 0,712 & 0,164 & 0,537 & leer \\
$(-29,-31)$ & \textbf{0,078} & 0,436 & 0,378 & Wand \\
$(-28,-31)$ & 0,357 & 0,994 & 0,217 & leer \\
$(-27,-31)$ & 0,370 & 0,376 & 0,154 & leer \\
$(-26,-31)$ & \textbf{0,186} & 0,149 & 0,064 & Wand \\
$(-25,-31)$ & 0,559 & 0,481 & 0,579 & leer \\
\bottomrule
\end{tabular}
\caption[Belegung einer Chunk-Zeile Feld für Feld]{Implementierung - Belegung
einer vollständigen Chunk-Zeile aus der Welt zu Seed 7000 mit den drei
Rauschwerten je Feld, eigene Darstellung.}
\label{tab:trace}
\end{table}

\subsubsection*{Stufe 2: Glättung, in dieser Arbeit abgeschaltet}

Auf die Basisbelegung könnte ein zellulärer Automat folgen, der verstreute
Einzelhindernisse zu zusammenhängenden Formationen verschmilzt
(Moore-Nachbarschaft, Geburts- und Überlebensschwelle,
Abschnitt~\ref{sec:pcg_algorithmen}). Er ist implementiert, in allen
berichteten Läufen aber abgeschaltet; Abbildung~\ref{fig:pcgpipeline} zeigt ihn
deshalb gestrichelt. Warum das Einschalten die Welten nicht härter, sondern
leerer macht, steht weiter unten.

Seine Konstruktion verdient trotzdem eine Bemerkung, weil sie ein echtes
Problem löst. Ein Automat braucht die acht Nachbarn jeder Zelle. Am Chunk-Rand
liegen die im Nachbar-Chunk, und den zu erzeugen würde eine Abhängigkeit
zwischen Chunks einführen und damit genau die Reinheit zerstören, auf der die
Reproduzierbarkeit beruht. Die Stufe berechnet deshalb einen \emph{Halo} in
Breite der Iterationszahl aus der Basisbelegung mit, lässt den Automaten
darüber laufen und verwirft ihn danach. Das Ergebnis hängt so weiterhin nur von
Seed und Chunk-Koordinaten ab.

\subsubsection*{Stufe 3: Landmarken}

Zuletzt setzt der Generator mit einer Wahrscheinlichkeit von $P = 0{,}10$ je
Chunk eine biomspezifische Landmarke. Ihre Form stammt aus handgeschriebenen
ASCII-Matrizen der Größe $5\times5$, je eine pro Biom, die an einer aus dem
Rauschen bestimmten Position im Chunk eingestempelt wird. Bei einer
Chunk-Kantenlänge von 8 bleiben dafür drei Felder Spielraum in jeder Richtung.
Chunks, die Spawn oder Exit enthalten, überspringt die Stufe, damit die
Strukturen weder den Start noch das Ziel verbauen.

\paragraph{Die Pipeline an einer echten Welt.} Abbildung~\ref{fig:worldgenreal}
zeigt alle Stufen nebeneinander an einer der 50 Welten des Testsets. Links das
Biomfeld, dessen Blöcke sichtbar an den Chunk-Grenzen ausgerichtet sind und
dessen Regionen sich über mehrere Chunks erstrecken. In der Mitte die
Basisbelegung: Der Biomwechsel ist als Dichtewechsel zu erkennen, im Bergland
liegen sichtbar mehr Wände und Erz als in der Tundra. Rechts die fertige Welt
mit Landmarken, Spawn, Exit und dem kürzesten Weg. Die Tile-Darstellungen
stammen direkt aus dem C++-Kern; das im Python-Binding nicht zugängliche
Biomfeld wurde aus einem Nachbau der Generatorfunktionen berechnet, der die
Tile-Typen des Kerns auf allen 8\,115 geprüften Feldern dieses Ausschnitts
exakt reproduziert.

Dieser Weg ist die eigentliche Aussage der Abbildung. Er ist eine grobe Treppe
zwischen zwei Punkten und weicht kaum einmal seitlich aus. Ein knappes Viertel
der Felder ist blockiert, und trotzdem gibt es nichts zu umrunden, weil die
Hindernisse einzeln stehen statt Wände zu bilden. Genau das quantifiziert der
Umwegfaktor weiter unten.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_worldgen_pipeline.pdf}
\caption[Die Generator-Pipeline an einer echten Welt]{Implementierung -
Biomfeld, Basisbelegung und fertige Welt zu Seed 7000 nebeneinander, eigene
Darstellung.}
\label{fig:worldgenreal}
\end{figure}

Tabelle~\ref{tab:biome} listet die Schwellwerte der Stufe~1 je Biom: Ein Feld
wird zur Wand, wenn sein Dichte-Rauschwert unter der Wandschwelle liegt,
entsprechend für Erz und Bäume mit eigenen Rauschfeldern; Erz entsteht
ausschließlich im Bergland, die Erzspalte der übrigen Biome bleibt
wirkungslos. Die Werte sind im Quellcode der Weltgenerierung hartkodiert und
über die Konfigurationsdatei nicht erreichbar.

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}lrrrl@{}}
\toprule
\textbf{Biom} & \textbf{Wand} & \textbf{Erz} & \textbf{Baum} & \textbf{Charakter} \\
\midrule
Wald      & 0,07 & 0,015 & 0,23 & dicht bewachsen, kaum Stein \\
Grasland  & 0,10 & 0,03  & 0,05 & offen \\
Steppe    & 0,11 & 0,03  & 0,09 & offen, etwas mehr Bewuchs \\
Tundra    & 0,14 & 0,05  & 0,07 & ausgeglichen \\
Wüste     & 0,19 & 0,08  & 0,00 & steinig, baumlos \\
Hölle     & 0,23 & 0,09  & 0,06 & steinig \\
Bergland  & 0,25 & 0,10  & 0,02 & dichtester Fels, \emph{einziges Erzbiom} \\
\bottomrule
\end{tabular}
\caption[Biomabhängige Schwellwerte der Basisbelegung]{Implementierung -
Wand-, Erz- und Baumschwellwerte der Basisbelegung je Biom, eigene
Darstellung.}
\label{tab:biome}
\end{table}

\paragraph{Was am Ende tatsächlich im Weg steht.} Die Schwellwerte in
Tabelle~\ref{tab:biome} sind Parameter, keine Messwerte, und sie beschreiben
allein die Wände. Wie viel eine erzeugte Welt dem Spieler wirklich in den Weg
legt, ist eine andere Zahl: Weil Bäume, Erz und Landmarken ebenfalls
blockieren, liegt der Anteil unpassierbarer Felder deutlich über jeder
einzelnen Wandschwelle. Tabelle~\ref{tab:tilecensus} schlüsselt ihn auf,
gezählt über die 100 Seeds beider Eval-Sets in einem $81\times81$-Fenster um
den Spawn (656\,100 Tiles): Nur ein knappes Sechstel der Welt besteht aus
Wänden, Bäume und Erz steuern zusammen weitere sieben Prozentpunkte zur
Undurchdringlichkeit bei. Die
Zusammensetzung erklärt zugleich, warum die Wandschwellen im Bereich 0,07 bis
0,25 liegen, die weiter unten diskutierte Hindernisdichte aber bei rund 0,24:
Es sind zwei verschiedene Größen, und nur die zweite entscheidet über die
Geometrie der Wege.

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Tile} & \textbf{Anteil} & \textbf{begehbar} \\
\midrule
Leer            & 0,7511 & ja \\
Wand            & 0,1562 & nein \\
Baum            & 0,0421 & nein \\
Erz             & 0,0322 & nein \\
Landmarken      & 0,0183 & nein \\
Exit            & 0,0002 & ja \\
\midrule
\textbf{unpassierbar gesamt} & \textbf{0,2487} & \\
\bottomrule
\end{tabular}
\caption[Tile-Zusammensetzung der Eval-Welten]{Implementierung -
Tile-Zusammensetzung der erzeugten Eval-Welten über die 100 Seeds beider
Testsets, eigene Darstellung.}
\label{tab:tilecensus}
\end{table}

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  stage/.style={draw, rounded corners=2pt, minimum width=2.9cm, minimum height=1.9cm,
                align=center, font=\scriptsize, fill=black!5, text width=2.7cm},
  off/.style={draw, dashed, rounded corners=2pt, minimum width=2.9cm, minimum height=1.9cm,
              align=center, font=\scriptsize, fill=black!2, text width=2.7cm},
  io/.style={font=\scriptsize, align=center, text width=2.1cm},
  >={Stealth[length=2.2mm]}, node distance=0.45cm
]
  \node[stage] (s0) {\textbf{Stufe 0}\\[1pt]Biomfeld\\[2pt]
    interpoliertes Wertrauschen + Domain Warping $\to$ 7 Biome};
  \node[stage, right=of s0] (s1) {\textbf{Stufe 1}\\[1pt]Basisbelegung\\[2pt]
    Hash-Rauschen je Tile, biomabh. Schwellen; Seenmaske};
  \node[off, right=of s1] (s2) {\textbf{Stufe 2}\\[1pt]Zelluläre Glättung\\[2pt]
    optional, \emph{in dieser Arbeit deaktiviert}};
  \node[stage, right=of s2] (s3) {\textbf{Stufe 3}\\[1pt]Landmarken\\[2pt]
    POI-Struktur mit $P=0{,}10$ je Chunk};

  \node[io, left=0.4cm of s0] (in) {Seed +\\Chunk-Koord.};
  \node[io, right=0.4cm of s3] (out) {Exit-\\Platzierung\\(BFS)};

  \draw[->, thick] (in) -- (s0);
  \draw[->, thick] (s0) -- (s1);
  \draw[->, thick] (s1) -- (s2);
  \draw[->, thick] (s2) -- (s3);
  \draw[->, thick] (s3) -- (out);
\end{tikzpicture}%
}
\caption[Pipeline der Weltgenerierung je Chunk]{Implementierung - Vierstufige
Pipeline der Weltgenerierung je Chunk mit nachgelagerter Exit-Platzierung,
eigene Darstellung.}
\label{fig:pcgpipeline}
\end{figure}

\paragraph{Nachbearbeitung und eine Grenze der Konfigurierbarkeit.} Nach den
vier Stufen räumt der Generator noch feste Radien um Spawn und Exit frei
($5\times5$ bzw.\ $3\times3$ Felder), damit weder Spieler noch Ziel in einem
Hindernis stecken. Die Biomzuordnung dagegen ist starr: Die
Sieben-Intervall-Teilung des Biomfeldes steht im Code und lässt sich über
\texttt{game\_config.json} nicht verändern. Ein Audit der Konfiguration
entfernte hier mehrere Schlüssel (\texttt{coldBiomeMax}, \texttt{warmBiomeMax}
und die \texttt{moss}-Schwellwerte), die zwar eingelesen, durch die feste
Binning-Logik aber nie ausgewertet wurden. Sie versprachen ein Tuning, das
faktisch wirkungslos war (Tabelle~\ref{tab:versionsverlauf}). Dasselbe gilt für die
Schwellwerte in Tabelle~\ref{tab:biome}: Wer die Welten härter machen will,
muss den Generator anfassen, nicht die Konfiguration.

Die Exit-Platzierung und die Lösbarkeitssicherung setzen auf der so erzeugten
Welt auf und sind in Abschnitt~\ref{sec:loesbarkeit} beschrieben. Der Generator
implementiert darüber hinaus die in Abschnitt~\ref{sec:pcg_algorithmen}
genannten Sicherheitsnetze. Für die Bewertung der Ergebnisse ist entscheidend,
welche davon in den berichteten Läufen tatsächlich wirksam waren
(Tabelle~\ref{tab:worldgen_aktiv}).

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}L{5.3cm}L{2.2cm}L{6.6cm}@{}}
\toprule
\textbf{Mechanismus} & \textbf{Aktiv} & \textbf{Begründung} \\
\midrule
BFS-Exit-Platzierung nach Laufweg & \textbf{ja} & Garantiert Erreichbarkeit per Konstruktion und steuert zugleich die Curriculum-Distanz (Tabelle~\ref{tab:versionsverlauf}). \\
\addlinespace
Zelluläre Glättung (Stufe 2) & nein & Abgeschaltet; die Welten entstehen allein aus Stufe~1. \\
\addlinespace
Flood-Fill-Validierung des Start-Ziel-Fensters & nein & Durch die BFS-Platzierung redundant: Der Exit wird ausschließlich aus begehbar erreichbaren Zellen gewählt. \\
\addlinespace
Carving (\texttt{forceGuaranteedPath} und Fallback) & nein & Aus demselben Grund nicht erforderlich; damit entfällt zugleich der gerade Korridor als strukturelle Auffälligkeit. \\
\bottomrule
\end{tabular}
\caption[Aktive Mechanismen der Weltgenerierung]{Implementierung - Im
Generator implementierte Mechanismen und ihr Status in der berichteten
Konfiguration, eigene Darstellung.}
\label{tab:worldgen_aktiv}
\end{table}

\paragraph{Eine Konsequenz für die Ergebnisse.} Aus Tabelle~\ref{tab:worldgen_aktiv}
folgt eine Eigenschaft der Welten, die für die Bewertung in
Abschnitt~\ref{sec:baselines} unmittelbar relevant ist: Da die Glättung
abgeschaltet ist, stammt die Hindernisverteilung ausschließlich aus
\emph{unkorreliertem} Hash-Rauschen. Jedes Feld entscheidet für sich, ohne
seine Nachbarn zu kennen, denn \texttt{noise01} ist genau dafür gebaut. Die
Hindernisse sind damit statistisch unabhängig voneinander verteilt und bilden
überwiegend verstreute Einzelfelder statt zusammenhängender Barrieren
(Abbildung~\ref{fig:pomdp_schema}). Was den Generator reproduzierbar macht,
kostet ihn also zugleich jede Struktur: Genau die Eigenschaft, die
Abschnitt~\ref{sec:bausteine} als notwendig begründet, verhindert hier
Sackgassen. Genau diese Struktur macht die Welten
\glqq offen\grqq{} im Sinne von Abschnitt~\ref{sec:baselines}: Sackgassen, die
Gedächtnis erfordern würden, entstehen selten, und eine ungerichtete
Zufallsbewegung verlässt sie zuverlässig.

\paragraph{Wie offen die Welten sind, misst der Umwegfaktor.}
Quantifizieren lässt sich diese Offenheit über das Verhältnis aus BFS-Laufweg
und Manhattan-Distanz zwischen Spawn und Exit. Bei 1,0 ist der kürzeste Weg die
Luftlinie, es gibt also nichts zu umrunden. Über beide Eval-Sets gemessen
(100 Seeds, \texttt{scripts/probe\_world\_geometry.py}) liegt der Faktor bei
\textbf{1,10}, bei einer Hindernisdichte von 0,238
(Tabelle~\ref{tab:tilecensus}): Der kürzeste Weg ist zehn Prozent
länger als die Luftlinie. Ein knappes Viertel der Welt ist blockiert, und
trotzdem gibt es fast nichts zu umrunden. Das ist die quantitative Ursache dafür, dass ein
vierzeiliger Kompass-Folger 92\,\% erreicht (Abschnitt~\ref{sec:baselines}).
Nicht die Politik ist stark, die Geometrie ist schwach.

\paragraph{Die naheliegende Reparatur funktioniert nicht.}
Es liegt nahe, dagegen die vorhandene Glättungsstufe einzuschalten; in der
PCG-Literatur ist gerade sie es, die aus verstreutem Rauschen konkave
Strukturen formt \cite{johnson2010cellular, shaker2016pcg}. Für diesen
Generator haben wir das nachgemessen, und es trifft nicht zu. Mit den
hinterlegten Regeln (Geburt ab 5, Überleben ab 4, zwei Iterationen) fällt die
Wanddichte von 0,238 auf 0,037 und der Umwegfaktor auf 1,001. Die Welt wird
zur leeren Ebene statt zum Labyrinth. Schuld ist die niedrige Ausgangsdichte:
Bei 24\,\% Wandanteil hat kaum ein Wandtile die geforderten fünf soliden
Nachbarn, also stirbt fast jede Wand und keine wird geboren. Wer die Stufe
aktiviert, treibt die Erfolgsquote der ungelernten Heuristik nach oben statt
sie einbrechen zu lassen.

Der Regelsweep in Tabelle~\ref{tab:geometrieprobe} ($b$ die Geburts-, $s$ die
Überlebensschwelle des Automaten) zeigt darüber hinaus einen
harten Zielkonflikt. Jede Kombination, die den Umwegfaktor spürbar hebt,
zerstört die Lösbarkeit; alles, was bei 50 von 50 lösbaren Welten bleibt, liegt
beim Umweg im Rauschen der Referenz. Die Ursache liegt außerhalb des Automaten.
\texttt{enableFloodFillValidation} und \texttt{enableMacroGraphPrecheck} stehen
beide auf \texttt{false} (\texttt{assets/base/game\_config.json}), und die
Basisdichte ist in \texttt{World::sampleBaseTile()} hartkodiert. Der Automat
zählt Nachbarn, von Erreichbarkeit weiß er nichts. Eine Härtung der Umgebung
ist deshalb kein Konfigurationsschalter, sondern ein Eingriff in den Generator
(Abschnitt~\ref{sec:ausblick}).

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Regel der Glättungsstufe} & \textbf{lösbar} & \textbf{Wanddichte} &
\textbf{BFS} & \textbf{Umweg} & \textbf{p90} \\
\midrule
aus (berichtete Konfiguration) & 50/50 & 0,240 & 40,1 & \textbf{1,123} & 1,312 \\
\addlinespace
an, 2 Iter., $b{=}5$ / $s{=}4$ (Default) & 50/50 & 0,036 & 40,1 & 1,001 & 1,000 \\
an, 2 Iter., $b{=}4$ / $s{=}3$ & 50/50 & 0,169 & 40,1 & 1,101 & 1,327 \\
an, 1 Iter., $b{=}3$ / $s{=}3$ & 50/50 & 0,271 & 39,8 & 1,145 & 1,637 \\
an, 2 Iter., $b{=}3$ / $s{=}2$ & 41/50 & 0,447 & 39,4 & 1,158 & 1,357 \\
an, 4 Iter., $b{=}3$ / $s{=}2$ & 19/50 & 0,699 & 39,7 & 1,173 & 1,607 \\
an, 2 Iter., $b{=}2$ / $s{=}1$ & \textbf{3/50} & 0,875 & 35,3 & \textbf{1,636} & 2,299 \\
\bottomrule
\end{tabular}
\caption[Regelsweep der zellulären Glättung]{Implementierung - Regelsweep
der zellulären Glättungsstufe auf Testset~A, eigene Darstellung.}
\label{tab:geometrieprobe}
\end{table}

\subsubsection{Exit-Platzierung und garantierte Lösbarkeit}
\label{sec:loesbarkeit}

\paragraph{Warum das überhaupt eine Rolle spielt.} Die zentrale Messgröße
dieser Arbeit ist die Erfolgsquote: der Anteil der Welten, in denen der Agent
den Exit erreicht. Diese Zahl ist nur dann interpretierbar, wenn jede
gescheiterte Episode auch tatsächlich am Agenten lag. Erzeugt der Generator
Welten, in denen der Exit hinter einer geschlossenen Wandbarriere liegt, mischt
sich in jede Erfolgsquote ein unbekannter Anteil unlösbarer Karten. Ein Modell
mit 65\,\% wäre dann von einem Modell mit 80\,\% nicht mehr zu unterscheiden,
wenn 15\,\% der Welten von vornherein niemand lösen kann. Der Generator muss
also \emph{jede} Welt lösbar erzeugen, und zwar nicht \glqq meistens\grqq{},
sondern immer.

\paragraph{Der übliche Weg und warum wir ihn nicht gehen.} Naheliegend wäre
Rejection Sampling: Welt erzeugen, Exit zufällig setzen, Erreichbarkeit per
Flood-Fill prüfen, bei Misserfolg neu würfeln. Das ist teuer, weil der
Verwerfungsanteil mit der Wanddichte steigt, und es hat keine obere
Laufzeitschranke. Die verbreitete Alternative, das nachträgliche Freiräumen
eines Korridors zwischen Spawn und Exit (\texttt{forceGuaranteedPath}), ist
noch schlechter: Sie hinterlässt einen geraden, ein Tile breiten Gang durch die
Welt. Ein Agent kann lernen, genau diese Struktur zu erkennen und ihr zu
folgen, statt zu navigieren. Beide Verfahren sind im Generator implementiert,
beide sind in den berichteten Läufen abgeschaltet
(Tabelle~\ref{tab:worldgen_aktiv}).

\paragraph{Stattdessen: den Exit aus dem Erreichbarkeitsbaum ziehen.} Statt
einen Exit zu setzen und danach zu prüfen, kehrt der Generator die Reihenfolge
um. Er lässt eine Breitensuche vom Spawn aus über \emph{ausschließlich
begehbare} Tiles laufen und wählt den Exit erst danach, aus genau der Menge,
die diese Suche erreicht hat. Damit ist Erreichbarkeit keine Eigenschaft, die
man prüfen müsste, sondern eine Eigenschaft der Konstruktion: Ein Tile, das die
Breitensuche nie besucht hat, kann gar nicht erst Kandidat werden. Der Ablauf
in \texttt{World::chooseExitPoint()} umfasst vier Schritte.

\begin{enumerate}
  \item \textbf{Spawn freiräumen.} Um den festen Spawn wird ein $5\times5$-Feld
    (Radius 2) auf \emph{begehbar} gesetzt, damit der Agent nicht in einem
    Hindernis startet.
  \item \textbf{Breitensuche vom Spawn.} Eine Vier-Nachbarschafts-BFS
    expandiert über begehbare Tiles, begrenzt auf ein Fenster um den Spawn.
    Ihre Tiefe ist der \emph{echte Laufweg} in Schritten, nicht der
    geometrische Abstand. Die Suche bricht bei Tiefe
    \texttt{exitMaxDistance} ab, weil die BFS-Tiefe monoton wächst.
  \item \textbf{Kandidatenmenge bilden.} Jedes besuchte Tile mit einer Tiefe
    zwischen \texttt{exitMinDistance} und \texttt{exitMaxDistance} kommt in die
    Kandidatenliste. Diese Liste steuert zugleich das Curriculum: Ein
    Distanzfenster von 5 bis 12 erzeugt kurze Welten, eines von 35 bis 45 die
    Welten des Evaluationsprotokolls.
  \item \textbf{Kandidat wählen.} Ein aus dem Seed gemischter Index bestimmt den
    Startpunkt in der Liste. Von dort prüft der Generator bis zu 24 Kandidaten
    gegen die Distanzbedingung des nächsten Absatzes und nimmt den ersten, der
    besteht.
\end{enumerate}

\paragraph{Die Falle: die Freiräumung erzeugt eine Abkürzung.} Nach der Wahl
räumt \texttt{World::reset()} auch um den Exit ein Feld frei, hier
$3\times3$ (Radius 1), damit das Ziel nicht in einer Wand verschwindet. Genau
das kann die eben gemessene Distanz wieder kaputt machen. Öffnet die
Freiräumung eine Wand, die vorher einen Umweg erzwungen hat, entsteht eine
Abkürzung, und der reale Laufweg fällt unter \texttt{exitMinDistance}. Bei
einem Fenster von 35 bis 45 wäre der Agent dann in einer Welt, die
nominell zur Phase 3 gehört, tatsächlich aber deutlich leichter ist. Für ein
distanzgesteuertes Curriculum ist das kein Schönheitsfehler, sondern ein
verfälschter Schwierigkeitsgrad.

Die Lösung ist eine zweite Breitensuche, die die Freiräumung vorwegnimmt.
Listing~\ref{lst:exit} zeigt den Kern: Tiles im Freiräumungsradius des
Kandidaten gelten in dieser Suche bereits als begehbar, obwohl sie es noch
nicht sind. Gemessen wird also der Laufweg der Welt, die \emph{nach} der
Freiräumung existiert. Nur wenn dieser Weg noch mindestens
\texttt{exitMinDistance} beträgt, wird der Kandidat angenommen.

\begin{lstlisting}[language=C++,caption={[Exit-Platzierung mit virtueller
Freiräumung]Kandidatenprüfung bei der Exit-Platzierung
(\texttt{src/core/world.cpp}, gekürzt). Die zweite Breitensuche behandelt
Tiles im Freiräumungsradius des Kandidaten als begehbar und misst damit den
Laufweg der Welt \emph{nach} der Freiräumung. Ohne diese Vorwegnahme kann die
Freiräumung eine Abkürzung öffnen und den Laufweg nachträglich unter
\texttt{exitMinDistance} drücken.},label={lst:exit}]
// BFS vom Spawn zum Kandidaten. Tiles im Freiraeumungsradius zaehlen
// als begehbar, weil World::reset() sie ohnehin oeffnen wird:
const bool virtuallyCleared =
    std::abs(nx - target.x) <= cfg.exitClearRadius &&
    std::abs(ny - target.y) <= cfg.exitClearRadius;
if(!virtuallyCleared && !isPassable(nx, ny)) { continue; }

// ... Kandidat nur annehmen, wenn der Weg auch danach lang genug bleibt:
for(std::size_t k = 0; k < maxTries; ++k) {
    const Vec2i cand = candidates[(start + k) % n];
    if(clearedDistance(cand) >= minDist) { return cand; }
}
return candidates[start];   // Fallback: erreichbar, Distanz ggf. kuerzer
\end{lstlisting}

\paragraph{Was garantiert ist und was nicht.} Die beiden Zusagen sind
unterschiedlich stark, und die Trennung ist wichtig:

\begin{description}
  \item[Erreichbarkeit: hart garantiert.] Jeder Kandidat stammt aus dem
    Erreichbarkeitsbaum der ersten Breitensuche. Ein unlösbarer Exit ist damit
    nicht unwahrscheinlich, sondern konstruktiv ausgeschlossen. Das gilt auch
    für den Fallback in der letzten Zeile von Listing~\ref{lst:exit}.
  \item[Distanz: garantiert bis auf einen Fallback.] Besteht keiner der 24
    geprüften Kandidaten die Distanzbedingung, nimmt der Generator den zuerst
    gezogenen. Die Welt bleibt lösbar, ihr Laufweg kann aber unter
    \texttt{exitMinDistance} liegen. Dass dieser Zweig in der berichteten
    Konfiguration nicht greift, ist gemessen und nicht angenommen: Über 150
    geprüfte Seeds lag der reale Laufweg ausnahmslos im Fenster
    $[35, 45]$ (Minimum 35, Mittel 39,7 bis 40,1, Maximum 45), und in
    Phase 1 ebenso im Fenster $[5, 12]$. Bei deutlich höherer Wanddichte oder
    einem engeren Distanzfenster wäre die Annahme neu zu prüfen.
\end{description}

\paragraph{Belege.} Die Konstruktion wurde nicht nur behauptet, sondern
nachgemessen. Über 3\,150 geprüfte Seeds (alle Eval-Sets und die
Trainingsbereiche) sowie eine erneute Stichprobe am 06.07.2026 (150 von 150)
ergaben durchgängig \textbf{100\,\% Lösbarkeit}. Ein zweiter, schärferer Test
prüft nicht nur die Weltgenerierung, sondern die gesamte Kette: Ein
BFS-Orakel-Agent, der den kürzesten Weg direkt aus dem Distanzfeld des Kerns
abliest, erreicht den Exit auf allen Test-Seeds in \emph{exakt} der
BFS-Distanz. Dass die tatsächlich gelaufene Schrittzahl mit der berechneten
Distanz übereinstimmt, schließt neben unlösbaren Welten auch stille Fehler in
Bewegungsmechanik, Kollisionsprüfung und Distanzfeld aus. Der gemessene
Umwegfaktor von 1,10 (Abschnitt~\ref{sec:weltgenerator}) stammt aus derselben
Rechnung.

Damit ist eine methodische Frage entschieden, die im Evaluationsteil sonst
offen bliebe: Wenn ein Agent in einer Welt scheitert, liegt es nie an der
Welt.

\subsubsection{Grafischer Client und Zuschaumodus}
\label{sec:client}

Der raylib-Client (ca.\ 4\,500 LOC) macht Stoneforge zu einem tatsächlich
spielbaren 2D-Spiel: Ein Mensch steuert dieselbe Simulation, die auch das
Training ansteuert, in Echtzeit über Tastatur und Maus. Jede Episode lässt
sich damit nachspielen. Abbildung~\ref{fig:player_client} zeigt die Oberfläche
aus Sicht eines menschlichen Spielers.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.72\textwidth]{figures/fig_game_real.png}
  \caption[Benutzeroberfläche des spielbaren C++-Clients]{Benutzeroberfläche des spielbaren C++-Clients (Stoneforge 2D, echter Screenshot): Gezeigt ist die Spielumgebung aus Sichtweise eines menschlichen Spielers mit vollem Sichtbereich, dem Status-Header (Lebenspunkte, Energie, Schritte) und der Spielfigur im Zentrum.}
  \label{fig:player_client}
\end{figure}

Neben dem Spieler-Modus besitzt der Client einen KI-Zuschaumodus:
\texttt{demo\_agent.py} lädt einen trainierten Checkpoint und reicht dessen
Aktionen über die Standardeingabe an den Client weiter, der dabei denselben
Seed wie die Trainings-Umgebung verwendet und so dieselbe Welt erzeugt. Ein
Mensch kann dem Agenten auf diese Weise beim Navigieren zusehen. Eine
Launcher-GUI (Tkinter) bündelt Training, Evaluation und Abspielen zu einer
gemeinsamen Oberfläche.

\subsection{Implementierung der Lernumgebung (Florian)}
\label{sec:rlumgebung}

Der vorige Abschnitt endet mit einem lauffähigen Spiel. Dieser Abschnitt macht
daraus eine Lernumgebung, und zwar ohne die Simulation dafür zu verändern: Der
C++-Kern aus Abschnitt~\ref{sec:technik} bleibt derselbe, ein Mensch spielt
weiterhin dieselbe Welt wie der Agent. Hinzu kommen eine Übersetzungsschicht,
die das Spiel als Markov-Entscheidungsproblem darstellt, ein Reward-Design und
die Trainings- und Monitoring-Infrastruktur.

\subsubsection{Versionsverlauf}
\label{sec:versionsverlauf}

Umgebung und Trainingskonfiguration durchliefen mehrere Überarbeitungen.
Tabelle~\ref{tab:versionsverlauf} fasst die Versionen zusammen, auf die im
weiteren Verlauf dieses Kapitels sowie in Methodik und Evaluation Bezug
genommen wird; die vollständige Änderungshistorie liegt im
Experiment-Changelog des Repositories.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lL{8.4cm}l@{}}
\toprule
\textbf{Version} & \textbf{Wesentliche Änderungen} & \textbf{Ergebnis (Testset A)} \\
\midrule
vor v11 (bis 06.07.2026) & BFS-Orakel-Feature in der Observation, Exit-Platzierung nach Luftlinie, Straf-Stacking aus Wand- und Loop-Penalty & 98\,\% stoch.\ / 2\,\% det.\ unter einem inzwischen verworfenen Kurzdistanz-Protokoll, nicht mit v11/v12 vergleichbar \\
\addlinespace
v11 (06.07.2026) & Tote Features entfernt (231 $\to$ 229 Dimensionen), Straf-Stacking entschärft, Exit-Platzierung nach BFS-Laufweg statt Luftlinie, prozess-globales Konfigurations\-leck behoben, Mining/Bauen/Kampf aus dem RL-Pfad entfernt, Entropie-Annealing korrigiert & neue Baseline für alle folgenden Messungen \\
\addlinespace
v12 (ab 07.07.2026) & \texttt{batch\_size} von 64 auf 8 korrigiert (stabilisiert den LSTM-Critic), Eval-Episoden-Cap durchgängig auf 4\,000 vereinheitlicht, finale Konfiguration über sieben unabhängige Läufe & \textbf{65,7\,\% / 66,9\,\%} (A/B, stochastisch, $n=7$) \\
\bottomrule
\end{tabular}
\caption[Versionsverlauf von Umgebung und Training]{Implementierung der
Lernumgebung - Versionsverlauf von Umgebung und Training, eigene Darstellung.}
\label{tab:versionsverlauf}
\end{table}

\subsubsection{Was der Agent davon sieht: POMDP-Charakter}
\label{sec:pomdpchar}

Die Welt ist damit vollständig beschrieben: Sie ist reproduzierbar,
lösbar und in ihrer Zielentfernung gesteuert. Für den Agenten ist von alledem
nichts unmittelbar zugänglich. Er sieht nur einen lokalen
$15{\times}15$-Ausschnitt der Welt sowie einen
\emph{Luftlinien}-Kompass zum Exit, nicht den Weg. Wände, Sackgassen und
Umwege außerhalb des Sichtfensters sind unbekannt: Die Aufgabe ist ein
\emph{Partially Observable Markov Decision Process} (POMDP,
Abschnitt~\ref{sec:pomdp}). Beide Perspektiven stellt
Abbildung~\ref{fig:pomdp_schema} auf Seite~\pageref{fig:pomdp_schema}
gegenüber: links die Welt, wie sie tatsächlich aussieht, rechts das, was der
Agent davon sieht. Für eine gedächtnislose Politik sind dabei verschiedene
Weltpositionen ununterscheidbar (Abschnitt~\ref{sec:ablation}).

\subsubsection{Umgebung, Beobachtung und Aktionen}
\label{sec:envobs}

Damit ein Lernverfahren die Simulation ansteuern kann, muss sie sich als
\emph{Markov-Entscheidungsproblem} präsentieren: ein Zustand, den der Agent
beobachtet, eine Menge von Aktionen, aus denen er wählt, und ein Reward pro
Schritt. Stoneforge ist von Haus aus kein solches Problem, sondern ein Spiel
mit Kamera, Inventar und Echtzeitschleife. \texttt{StoneforgeWorldEnv} (Python)
übersetzt zwischen beiden Welten und kapselt den C++-Kern hinter der
Gymnasium-API. Was der Agent dabei zu sehen bekommt, listet
Tabelle~\ref{tab:obs}.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llr@{}}
\toprule
\textbf{Bereich} & \textbf{Inhalt} & \textbf{Dim.} \\
\midrule
Lokales Grid & $15{\times}15$ Tile-Typen um den Agenten (Sichtradius 7) & 225 \\
HP & Lebenspunkte (normiert) & 1 \\
Kompass & exitDx, exitDy (Luftlinie, normiert) & 2 \\
Episodenfortschritt & Schritt / 4000 & 1 \\
\midrule
\textbf{Gesamt (Env v11)} & & \textbf{229} \\
\bottomrule
\end{tabular}
\caption[Aufbau der Observation (Env v11)]{Implementierung der Lernumgebung -
Aufbau der 229-dimensionalen Observation in Umgebungsversion v11, eigene
Darstellung.}
\label{tab:obs}
\end{table}

Der Aktionsraum ist \texttt{Discrete(4)} (hoch, runter, links, rechts). Die im
Spiel vorhandenen Aktionen Mining, Bauen und Kampf sind im RL-Binding
deaktiviert und werfen bei Aufruf einen Fehler; sie existieren nur im
spielbaren Client (Abschnitt~\ref{sec:client}). Eine Episode endet bei
Erreichen des Exits (Sieg), nach 4\,000 Schritten (Timeout) oder als
Early-Stop-Truncation nach 256 Schritten ohne positiven Reward.

Tabelle~\ref{tab:reward} führt daneben \glqq Tod\grqq{} als terminales Ereignis
und einen Schadens-Penalty auf. Beide können im RL-Pfad nicht eintreten: Die
einzigen Schadensquellen des Simulationskerns sind Mob-Nähe und Verhungern, und
die Umgebung schaltet beides ab (\texttt{disable\_mobs}, \texttt{disable\_energy}).
Die Lebenspunkte bleiben damit über die gesamte Episode konstant. \textbf{Das
HP-Merkmal in Tabelle~\ref{tab:obs} ist folglich ein totes Feature}, so wie es
Energie und Inventar vor der v11-Revision waren (Tabelle~\ref{tab:versionsverlauf}); es
wurde dort übersehen und ist eine der 229 Dimensionen ohne Informationsgehalt.
Der Schadens-Penalty ist entsprechend inert. Beides ist folgenlos für die
berichteten Ergebnisse, aber bei einer Neufassung der Beobachtung zu
streichen.

\subsubsection{Die Schnittstelle: was bei \texttt{reset()} und \texttt{step()}
tatsächlich passiert}
\label{sec:schnittstelle}

Die Aufgabenteilung zwischen den beiden Sprachen ist eng geschnitten. Der
C++-Kern besitzt den Weltzustand und rechnet alles, was pro Schritt anfällt:
Weltgenerierung, Bewegung samt Kollision, das BFS-Distanzfeld und die
Reward-Terme aus Tabelle~\ref{tab:reward}. Die Python-Seite besitzt keinen
Weltzustand. Sie führt Buch über die Episode, formt die Beobachtung und
implementiert genau jene Regeln, die sich ohne Neuübersetzung ändern lassen
sollten. Das pybind11-Binding dazwischen ist bewusst dünn: Es exportiert im
Wesentlichen \texttt{reset(seed)}, \texttt{step(action)} und eine Handvoll
lesender Abfragen wie \texttt{current\_bfs\_distance\_to\_exit()} oder
\texttt{player\_pos()}. Es enthält keine Spiellogik. Wer den Reward ändern
will, ändert C++ und übersetzt neu; wer das Curriculum ändert, ändert Python.

\paragraph{Ein \texttt{reset()} in vier Schritten.} Der Aufruf legt eine neue
Episode an, und die Reihenfolge ist nicht beliebig:

\begin{enumerate}
  \item \textbf{Seed bestimmen.} Übergibt der Aufrufer einen Seed, wird dieser
    verwendet. Das ist der Fall im Evaluationsprotokoll, wo die Testsets aus
    festen Seeds bestehen. Im Training zieht die Umgebung stattdessen selbst
    einen Zufallsseed, sodass jede Episode eine neue Welt sieht.
  \item \textbf{Episodenbuchführung zurücksetzen.} Schrittzähler,
    Besuchszähler je Tile, Zähler für Schritte ohne positiven Reward und der
    Aktionspuffer werden geleert. Diese Zähler tragen den Revisit-Penalty und
    den Early Stop, existieren also nur auf der Python-Seite.
  \item \textbf{Weltgenerator-Konfiguration neu setzen.} Erst danach folgt der
    eigentliche Reset des Kerns. Der Grund steht im nächsten Absatz.
  \item \textbf{Kern zurücksetzen und Beobachtung bauen.} Der Kern erzeugt die
    Welt zum Seed, platziert den Exit (Abschnitt~\ref{sec:loesbarkeit}) und
    liefert einen rohen Zustandsvektor. Die Python-Seite normiert ihn und
    schneidet die toten Legacy-Felder heraus.
\end{enumerate}

\paragraph{Ein Detail, das lange Zeit die Messungen verfälscht hat.} Die
Weltgenerator-Konfiguration liegt im C++-Kern als \emph{prozessglobale}
Struktur. Das ist für ein Spiel unproblematisch, in dem genau eine Welt
existiert. Im Training existieren aber 16 parallele Umgebungen, und der
Evaluations-Callback erzeugt zusätzlich eine eigene. Setzt diese
Evaluations-Umgebung ihr Distanzfenster auf 35 bis 45, überschreibt sie damit
das Fenster \emph{aller} Trainingsumgebungen, obwohl sie fachlich nichts
miteinander zu tun haben. In Phase 3 verschob der Evaluations-Callback auf
diese Weise die Trainingsverteilung nach der ersten Evaluation still von
25 bis 45 auf 35 bis 45. Das Curriculum trainierte also nicht mehr auf dem
Bereich, der in der Konfiguration stand.

Die Lösung ist die dritte Zeile der Aufzählung oben: Jede Umgebung stempelt
ihre Konfiguration vor jedem einzelnen \texttt{reset()} neu, statt sie einmalig
im Konstruktor zu setzen. Nachweisen lässt sich der Fehler mit einem
BFS-Orakel: Vor dem Fix brauchte es auf den Test-Seeds durchschnittlich 8,3
Schritte, nachdem eine zweite Umgebung mit dem Fenster 5 bis 12 erzeugt worden
war; danach wieder die korrekten 40,2 (Tabelle~\ref{tab:versionsverlauf}). Der Fehler
war im Python-Code nicht zu sehen, denn dort stand die richtige Konfiguration.
Sichtbar wurde er erst an Zahlen, die sich nicht erklären ließen.

\paragraph{Ein \texttt{step()} in zwei Hälften.} Der Aufruf reicht die Aktion
an den Kern durch, der Bewegung, Kollision und Reward rechnet und den neuen
Zustand zurückgibt. Danach ergänzt die Python-Seite drei Regeln, die
absichtlich nicht in C++ stehen, weil sie zur Trainingsmethodik gehören und
nicht zum Spiel:

\begin{itemize}
  \item \textbf{Revisit-Penalty.} Ein Besuchszähler je Tile bestraft ab dem
    26.\ Besuch desselben Feldes, gedeckelt ab dem 50. Er greift damit nur bei
    ausgeprägtem Kreiseln (Tabelle~\ref{tab:reward}).
  \item \textbf{Early Stop.} Bleiben 256 Schritte ohne positiven Reward, endet
    die Episode als Truncation. Ohne diese Regel verbraucht ein feststeckender
    Agent bis zu 4\,000 Schritte pro Episode und der Durchsatz bricht ein.
  \item \textbf{Seed-Pool (\glqq Swarm\grqq).} Erfolgreiche Seeds werden
    gesammelt und in 30\,\% der Fälle wiederholt.
\end{itemize}

Zwei Dinge sind an dieser Aufteilung erwähnenswert. Erstens ist der Early Stop
eine \emph{Truncation} und kein Terminal: Der Wert des Endzustands wird
weiter gebootstrappt, die Episode gilt nicht als Misserfolg im Sinne des
Zielkriteriums. Zweitens ist die Trennung nicht kosmetisch. Jede Änderung an
den drei Python-Regeln wirkt sofort, während jede Änderung am Reward einen
Rebuild des Kerns erfordert. Über die Laufzeit des Projekts hat das die
Iterationsgeschwindigkeit spürbar bestimmt.

\subsubsection{Reward-Design}
\label{sec:rewarddesign}

Das Reward-Design folgt dem Prinzip: \emph{sparse Primärsignal + theoretisch
sauberes dichtes Shaping}. Kernstück ist Potential-Based Reward Shaping (PBRS)
auf der BFS-Pfaddistanz zum Exit:
\begin{equation}
F(s, s') \;=\; \gamma\,\Phi(s') - \Phi(s), \qquad
\Phi(s) \;=\; -\,\frac{d_{\mathrm{BFS}}(s)}{128},
\end{equation}
mit $\gamma = 0.999$ (identisch zum RL-Discount) und Gewicht $\beta = 2.5$.
Ein Tile echter Fortschritt liefert damit rund $+0{,}02$; abzüglich des
Schritt-Malus von $-0{,}01$ (Tabelle~\ref{tab:reward}) verbleiben netto etwa
$+0{,}01$. Listing~\ref{lst:pbrs} zeigt die Umsetzung im Kern.

\begin{lstlisting}[language=C++,caption={[PBRS im Simulationskern]%
Potential-Based Reward Shaping auf der BFS-Distanz
(\texttt{Simulation::computeReward()}, \texttt{src/core/simulation.cpp}).
Der Shaping-Discount ist bewusst identisch zum Discount des Lernverfahrens;
nur unter dieser Bedingung ist $F$ policy-invariant
\cite{ng1999policy}.},label={lst:pbrs}]
constexpr float PBRS_MAX_DIST = 128.0F;  // Normierung der BFS-Distanz
constexpr float PBRS_GAMMA    = 0.999F;  // identisch zum RL-Discount
constexpr float PBRS_BETA     = 2.5F;    // +0,01 netto pro Tile bei d=40

const float phi_prev = -static_cast<float>(previousDistance) / PBRS_MAX_DIST;
const float phi_cur  = -static_cast<float>(currentDistance)  / PBRS_MAX_DIST;
const float shaping  = PBRS_GAMMA * phi_cur - phi_prev;
reward += PBRS_BETA * shaping;
\end{lstlisting}

Beide Konstanten sind aus dieser Netto-Vorgabe rückwärts bestimmt. Der Nenner
128 normiert die BFS-Distanz auf die Größenordnung der übrigen Reward-Terme
und entspricht zugleich der Skalierung des BFS-Puffers im C++-Kern, sodass
$\Phi$ auch im ungünstigsten Fall betragsmäßig klein gegenüber dem
Terminal-Reward bleibt. Das Gewicht $\beta = 2{,}5$ ist so gewählt, dass sich
bei der mittleren Eval-Distanz $d \approx 40$ genau die genannten $+0{,}01$
netto pro Fortschritts-Tile ergeben. Ein zuvor verwendetes $\beta = 5{,}0$
wurde halbiert, nachdem ein zu klein dimensionierter BFS-Puffer in Kombination
mit dem Manhattan-Fallback zeitweise Beträge um $+2{,}5$ pro Schritt erzeugt
und damit ein falsches Lernziel gesetzt hatte. PBRS ist beweisbar
\emph{policy-invariant}; es beschleunigt die Konvergenz, ohne die optimale
Politik zu verändern \cite{ng1999policy}. Entscheidend ist, dass das Potential
die \emph{echte} Pfaddistanz (BFS durch Wände gerechnet) verwendet, nicht die
Luftlinie: Ein Luftlinien-Potential würde den Agenten systematisch in Sackgassen
führen. Abbildung~\ref{fig:potentialvergleich} zeigt den Unterschied an einem
minimalen Beispiel.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  every node/.style={font=\scriptsize},
  wall/.style={fill=black!55, draw=black},
  marker/.style={circle, fill, minimum size=3.2mm, inner sep=0pt}
]
  % ---------- Panel A: Luftlinien-Potential ----------
  \begin{scope}
    \draw[step=0.7cm, gray!45, thin] (0,0) grid (3.5,2.8);
    \draw[thick] (0,0) rectangle (3.5,2.8);
    \filldraw[wall] (1.4,0)   rectangle (2.1,0.7);
    \filldraw[wall] (1.4,1.4) rectangle (2.1,2.1);
    \filldraw[wall] (2.1,0.7) rectangle (2.8,1.4);
    \node[marker, fill=blue!70!black] (sA) at (0.35,1.05) {};
    \node[below=1pt of sA] {S};
    \node[marker, fill=yellow!85!black, star, star points=5] (zA) at (3.15,1.05) {};
    \node[below=1pt of zA] {Z};
    \draw[->, thick, red, dashed] (0.35,1.05) -- (1.05,1.05) -- (1.75,1.05);
    \node[red, font=\scriptsize\bfseries] at (1.75,1.05) {$\times$};
    \node[align=center, below=0.35cm] at (1.75,-0.1)
      {\textbf{Luftlinien-Potential}\\[1pt]zieht direkt in die Sackgasse};
  \end{scope}
  % ---------- Panel B: BFS-Potential ----------
  \begin{scope}[xshift=4.7cm]
    \draw[step=0.7cm, gray!45, thin] (0,0) grid (3.5,2.8);
    \draw[thick] (0,0) rectangle (3.5,2.8);
    \filldraw[wall] (1.4,0)   rectangle (2.1,0.7);
    \filldraw[wall] (1.4,1.4) rectangle (2.1,2.1);
    \filldraw[wall] (2.1,0.7) rectangle (2.8,1.4);
    \node[marker, fill=blue!70!black] (sB) at (0.35,1.05) {};
    \node[below=1pt of sB] {S};
    \node[marker, fill=yellow!85!black, star, star points=5] (zB) at (3.15,1.05) {};
    \node[below=1pt of zB] {Z};
    \draw[->, thick, blue!70!black, rounded corners=3pt]
      (0.35,1.05) -- (1.05,1.05) -- (1.05,2.45) -- (2.45,2.45) --
      (2.45,1.75) -- (3.15,1.75) -- (3.15,1.05);
    \node[align=center, below=0.35cm] at (1.75,-0.1)
      {\textbf{BFS-Potential}\\[1pt]führt um die Wand herum};
  \end{scope}
\end{tikzpicture}
\caption[Luftlinien- gegenüber BFS-Potential an einer Sackgasse]{Implementierung
der Lernumgebung - Schematische Sackgasse, in der ein Luftlinien-Potential den
Agenten hineinführt (links) und das verwendete BFS-Pfaddistanz-Potential
korrekt um die Wand herumführt (rechts), eigene Darstellung.}
\label{fig:potentialvergleich}
\end{figure}

Diese Garantie gilt allerdings nur für den PBRS-Term selbst. Die übrigen
Komponenten in Tabelle~\ref{tab:reward} (Explorations-Bonus, Schritt-, Loop-,
Idle-, Schadens- und Revisit-Penalty) sind gewöhnliche Reward-Terme und
verändern die optimale Politik prinzipiell mit. Der frühere eskalierende
Wand-Penalty ($-0.05$/$-0.25$) wurde aus genau diesem Grund entfernt: Die Summe
vieler kleiner Strafen machte Erkunden in engen Korridoren unwirtschaftlicher
als Stillstand. Der Revisit-Penalty ist der einzige verbliebene Term dieser
Art; er greift erst ab dem 26.\ Besuch \emph{desselben} Tiles und damit
ausschließlich bei ausgeprägtem Kreiseln, wirkt in der Sache aber in dieselbe
Richtung und ist bei einer Neufassung des Reward-Designs
(Abschnitt~\ref{sec:ausblick}) mit zu prüfen.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lr@{}}
\toprule
\textbf{Komponente} & \textbf{Wert} \\
\midrule
Exit erreicht (terminal) & $+100$ \\
Timeout / Tod (terminal) & $-10$ \\
PBRS (BFS-Distanz, pro Schritt) & $\approx \pm 0.02$ \\
Explorations-Bonus (neue Tile) & $+0.02$ \\
Schritt-Penalty & $-0.01$ \\
2-Schritt-Loop-Penalty & $-0.05$ \\
Idle-Penalty (Aktion ohne Positionswechsel) & $-0.04$ \\
Schadens-Penalty (pro verlorenem HP) & $-0.5$ \\
\midrule
Revisit-Penalty (ab 26.\ Besuch eines Tiles) & $-0.03$ bis $-0.06$ \\
\bottomrule
\end{tabular}
\caption[Reward-Komponenten (Env v11)]{Implementierung der Lernumgebung -
Vollständige Reward-Komponenten der Umgebungsversion v11, eigene
Darstellung.}
\label{tab:reward}
\end{table}

\subsubsection{Trainings-Pipeline}

\texttt{train\_curriculum.py} orchestriert die vier Phasen aus
Tabelle~\ref{tab:curriculum}. Ein Lauf ist damit kein einzelnes Training,
sondern eine Kette aus vier Trainings, die einander das Modell weiterreichen.
Vier Bausteine tragen diese Kette.

\paragraph{Parallele Umgebungen.} Je Lauf arbeiten 16 Umgebungsinstanzen in
einem \texttt{DummyVecEnv}. Sie laufen im selben Prozess, nacheinander pro
Schritt, und bündeln ihre Beobachtungen zu einem Batch. Der Gewinn liegt nicht
in echter Parallelität, sondern darin, dass ein Rollout Erfahrung aus 16
verschiedenen Welten gleichzeitig enthält, statt aus einer.

\paragraph{Evaluation und Gating.} Alle 25\,000 Schritte misst ein Callback
das aktuelle Modell auf 50 Validierungs-Seeds, in beiden Betriebsarten. Diese
Messung erfüllt zwei Aufgaben zugleich. Sie entscheidet erstens, wann eine
Phase endet: Erreicht die maßgebliche Metrik die Ziel-Erfolgsquote in zwei
aufeinanderfolgenden Evaluationen, schaltet das Curriculum weiter. Die
Doppelbedingung filtert Ausreißer, denn ein einzelner guter Messpunkt ist bei
dieser Lerndynamik kein Beleg (Abschnitt~\ref{sec:evalfallstricke}). Zweitens
wählt sie das beste Modell der Phase aus, und genau dieses wird beim
Phasenwechsel geladen. Welche Metrik dabei zählt, wechselt: Die Phasen 1 und 2
gaten auf der stochastischen Erfolgsquote, die Phasen 3 und 4 auf der
deterministischen.

\paragraph{Absenkung der Exploration.} Der Entropie-Koeffizient hält die
Politik früh breit gestreut und muss später aus dem Weg. In Phase 3 senkt ein
Callback ihn linear von 0{,}05 auf 0{,}001 über 500\,000 Schritte, Phase 4
trainiert mit 0{,}0001. Diese letzte Phase existiert nur, um die
deterministische Politik nachzuziehen (Abschnitt~\ref{sec:detstochgap}).

\paragraph{Seed-Pool.} Ein optionaler Pool (\glqq Swarm\grqq) sammelt bereits
gelöste Seeds und startet 30\,\% der Episoden auf einem davon. Bei jedem
Phasenwechsel wird er geleert, damit gelöste Welten der leichten Phase nicht
die schwere Phase verwässern.

Jeder Lauf legt pro Phase Modell-Checkpoints sowie \texttt{eval\_history.json}
und \texttt{config.json} ab; alle Experimente sind zusätzlich im
\texttt{CHANGELOG.md} des Repositories dokumentiert.

\subsubsection{Monitoring und Werkzeuge}

Trainingsmetriken werden nach TensorBoard geschrieben. Neben der bereits
eingeführten \texttt{explained\_variance} (Güte der Wertfunktion,
Abschnitt~\ref{sec:grundlagen}) und der Entropie der Politik ist dabei
\texttt{approx\_kl} von Bedeutung: ein Schätzwert dafür, wie stark sich die
Politik durch ein Update verändert hat. Werte nahe null zeigen an, dass das
Training faktisch stillsteht; sehr große Werte deuten auf zu grobe Schritte
hin. Die Größe dient in dieser Arbeit als schneller Test, ob ein Lauf
überhaupt lernt. Zusätzlich streamt
ein WebSocket-Server den Trainingszustand in eine Browser-Ansicht:
Erfolgsquoten-Kurven (deterministisch und stochastisch), Minimaps der 16
parallelen Agenten und eine Explorations-Heatmap.

Abbildung~\ref{fig:architektur} fasst die Implementierung dieses Kapitels
zusammen: Ein einziger C++-Simulationskern wird auf zwei unabhängigen Wegen
angesteuert, einmal über das Python-Binding zum Training, einmal direkt vom
grafischen Client. Beide Pfade erzeugen bei gleichem Seed dieselbe Welt, was
den KI-Zuschaumodus (Abschnitt~\ref{sec:client}) überhaupt erst sinnvoll
macht.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  comp/.style={draw, rounded corners=2pt, minimum width=3.4cm, minimum height=1.1cm,
               align=center, font=\scriptsize},
  small/.style={draw, rounded corners=2pt, minimum width=2.4cm, minimum height=0.8cm,
                align=center, font=\scriptsize, fill=black!4},
  core/.style={draw, rounded corners=2pt, minimum width=9.6cm, minimum height=1.1cm,
               align=center, font=\small, fill=black!8},
  >={Stealth[length=2.2mm]}, node distance=0.5cm
]
  \node[core] (core) {\textbf{C++-Simulationskern}\\Weltgenerierung
    $\cdot$ Spiellogik $\cdot$ Reward $\cdot$ BFS};

  \node[comp, above left=0.9cm and 1.6cm of core.north] (bind) {\textbf{pybind11-Binding}\\\texttt{stoneforge\_sim}};
  \node[comp, above right=0.9cm and 1.6cm of core.north] (client) {\textbf{raylib-Client (C++)}\\Spieler-Modus / KI-Zuschaumodus};

  \node[comp, above=of bind] (env) {\textbf{StoneforgeWorldEnv}\\Python, Gymnasium-API};
  \node[comp, above=of env] (sb3) {\textbf{Stable-Baselines3}\\PPO, RecurrentPPO};

  \node[small, left=0.6cm of sb3] (tb) {TensorBoard};
  \node[small, above=0.35cm of tb] (ws) {WebSocket $\to$ Browser};

  \draw[->, thick] (core.north -| bind) -- (bind);
  \draw[->, thick] (core.north -| client) -- (client);
  \draw[->, thick] (bind) -- (env);
  \draw[->, thick] (env) -- (sb3);
  \draw[->, thick] (sb3) -- (tb);
  \draw[->, thick] (sb3) -- (ws);

  \node[font=\scriptsize, align=center, above=0.15cm of client, text width=2.6cm]
       (launcher) {\textit{orchestriert von Launcher-GUI (Tkinter)}};

  \draw[->, thick, dashed] (sb3.east) to[out=15, in=165]
       node[below, font=\scriptsize, pos=0.8, yshift=-3pt] {\texttt{demo\_agent.py}}
       (client.west);
\end{tikzpicture}%
}
\caption[Architektur: gemeinsamer Simulationskern für Training und Client]{Implementierung -
Gemeinsamer C++-Simulationskern mit den beiden Ansteuerungswegen Training und
Client, eigene Darstellung.}
\label{fig:architektur}
\end{figure}

\section{Evaluation (gemeinsam)}
\label{sec:evaluation}

Das Evaluationsprotokoll (Seed-Trennung, Metrik, Betriebsarten) ist in
Abschnitt~\ref{sec:methodik} beschrieben. Bewertet werden beide Teile des
Projekts, und zwar in der Reihenfolge, in der sie aufeinander aufbauen:
zuerst die Plattform gegen die Anforderungen Z1
(Abschnitt~\ref{sec:evalplattform}), dann der Agent gegen die Schwellen Z2
(Abschnitt~\ref{sec:evalagent}).

\subsection{Bewertung der Plattform (Z1, Laurin)}
\label{sec:evalplattform}

Die Anforderungen aus Abschnitt~\ref{sec:ziele} sind funktionaler Art; sie
werden hier zusammengeführt und gegen die jeweils erhobene Evidenz gestellt.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}L{3.3cm}L{7.4cm}L{2.0cm}@{}}
\toprule
\textbf{Anforderung} & \textbf{Nachweis} & \textbf{Status} \\
\midrule
Determinismus
  & Weltgenerierung und Simulation hängen ausschließlich von Seed und
    Koordinaten ab. Die chunkweise Erzeugung ist durch die Halo-Berechnung der
    Glättungsstufe reihenfolgeunabhängig; ein prozessglobales Konfigurationsleck,
    das dies zeitweise verletzte, wurde gefunden und behoben
    (Tabelle~\ref{tab:versionsverlauf}). Zwei Trainingspfade stimmen über
    8\,192 Schritte \emph{bit-identisch} überein.
  & \textbf{erfüllt} \\
\addlinespace
Garantierte Lösbarkeit
  & Exit-Platzierung ausschließlich über BFS-erreichbare Zellen. 3\,150 geprüfte
    Seeds sowie eine Nachmessung über 150 Seeds ergaben 100\,\% Lösbarkeit; ein
    BFS-Oracle-Agent erreicht das Ziel auf allen Test-Seeds in exakt der
    BFS-Distanz (Abschnitt~\ref{sec:loesbarkeit}).
  & \textbf{erfüllt} \\
\addlinespace
Steuerbare Schwierigkeit
  & Nach der Umstellung von Luftlinie auf BFS-Laufweg liegen alle 150
    Eval-Seeds im vorgegebenen Distanzband; zuvor streute eine Vorgabe von
    \glqq 35--45\grqq{} real auf 42--75 Tiles (Tabelle~\ref{tab:versionsverlauf}).
  & \textbf{erfüllt} \\
\addlinespace
RL-Tauglichkeit
  & Gymnasium-Schnittstelle, 16 parallele Umgebungen, 88--190 Schritte/s auf
    einem Notebook; ein vollständiger Curriculum-Lauf über 2{,}2\,Mio.\ Schritte
    dauert rund 8\,h (Abschnitt~\ref{sec:technik}).
  & \textbf{erfüllt} \\
\addlinespace
Spielbarkeit
  & Grafischer raylib-Client mit KI-Zuschaumodus; Client und Trainingsumgebung
    erzeugen aus demselben Seed dieselbe Welt
    (Abschnitt~\ref{sec:technik}).
  & \textbf{erfüllt} \\
\bottomrule
\end{tabular}
\caption[Bewertung der Plattform gegen die Anforderungen Z1]{Evaluation -
Bewertung der Plattform gegen die Anforderungen Z1, eigene Darstellung.}
\label{tab:z1}
\end{table}

\paragraph{Was damit belegt ist: und was nicht.} Alle fünf Anforderungen
sind erfüllt; die Plattform leistet, wofür sie gebaut wurde. Drei der fünf
Nachweise entstanden allerdings erst als Korrektur eines zuvor unbemerkten
Fehlers: Der Determinismus war durch das prozessglobale Konfigurationsleck
verletzt, die Schwierigkeitssteuerung durch das Luftlinienmaß, und die
Lösbarkeitsgarantie stand vor der BFS-basierten Exit-Platzierung auf dem
schwächeren Fundament eines gecarvten Korridors. Keiner dieser Fehler war am
Verhalten des Agenten unmittelbar ablesbar, sie wurden sichtbar, weil das
RL-Experiment Zahlen lieferte, die sich nicht erklären ließen. Das ist der erste
von zwei Punkten, an denen die beiden Projektteile ineinandergreifen.

Der zweite folgt in Abschnitt~\ref{sec:baselines} und wiegt schwerer: Z1
verlangt lösbare und in ihrer Entfernung steuerbare Welten, nicht aber
\emph{hinreichend schwierige}. Genau diese fehlende Anforderung führt dazu,
dass die erzeugten Welten die Fähigkeit, um die es in F2 geht, gar nicht
einfordern. Die Plattform erfüllt damit ihre Spezifikation vollständig, und
die Spezifikation erweist sich als unvollständig.

\subsection{Bewertung des Agenten (Z2, Florian)}
\label{sec:evalagent}

Der zweite Teil der Evaluation bewertet den Agenten gegen die Schwellen Z2.
Berichtet werden die Ergebnisse in der Reihenfolge, in der sie sich
gegenseitig einschränken: zuerst die Gegenüberstellung mit und ohne lokales
BFS-Orakel, dann das Endergebnis über sieben Läufe, dann der Vergleich gegen
ungelernte Referenzpolitiken, der die Aussagekraft der vorherigen Zahlen
relativiert, zuletzt der Det/Stoch-Gap und die Versuche zu seiner Schließung.

\subsubsection{Zentrale Gegenüberstellung: Gedächtnis statt lokalem Orakel}
\label{sec:ablation}

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llcccrr@{}}
\toprule
\textbf{Bedingung} & \textbf{Arch.} & \textbf{BFS in Obs} & \textbf{Curric.} & \textbf{Obs-Dim.} & \textbf{SR stoch.} & \textbf{SR det.} \\
\midrule
A (\texttt{ppo\_phase4}) & MLP & ja & ja & 236 & 32\,\% & 0\,\% \\
B (\texttt{ppo\_no\_bfs}) & MLP & nein & nein & 230 & -- & $\approx 0\,\%$ \\
C (\texttt{ppo\_lstm\_curriculum}) & \textbf{LSTM} & nein & ja & 231 & \textbf{86\,\%} & 36\,\% \\
\bottomrule
\end{tabular}
\caption[Gegenüberstellung: Gedächtnis gegenüber lokalem BFS-Orakel]{Evaluation -
Gegenüberstellung dreier Modelle auf Testset~A, je ein Einzellauf, Stand
Juni 2026, eigene Darstellung.}
\label{tab:ablation}
\end{table}

Die drei Bedingungen sind keine kontrollierte Ablation: A und C unterscheiden
sich gleichzeitig in Architektur und Beobachtungsraum, B ist ein bei 720\,k
von 2\,Mio.\ Schritten abgebrochener Lauf, und alle drei stammen aus der Zeit
vor der Umgebungsrevision v11 unter einem inzwischen verworfenen
Kurzdistanz-Protokoll (Tabelle~\ref{tab:versionsverlauf}). Belastbar ist
daher nur die schwache Aussage, dass eine rekurrente Politik ohne lokale
BFS-Features eine Leistung erreicht, die eine gedächtnislose Politik mit
diesen Features unter demselben Protokoll nicht erreicht hat.

Bedingung~C erzielte ihre 86\,\% zudem auf kürzeren Wegen als das finale
Modell: Die damalige Exit-Vorgabe \glqq 35--45\grqq{} war eine
Luftlinien-Distanz und entsprach real 42--75 Feldern, gegenüber der heute
exakt eingehaltenen BFS-Laufweg-Distanz (Tabelle~\ref{tab:versionsverlauf}).
Die 65{,}7\,\% des finalen Modells (Abschnitt~\ref{sec:endergebnis}) sind
damit nicht als Rückschritt zu lesen. Abbildung~\ref{fig:lernkurve} zeigt den
Trainingsverlauf dieses historischen Referenzlaufs.

\begin{figure}[h]
\centering
\includegraphics[width=0.72\textwidth]{figures/fig_lernkurve.pdf}
\caption[Lernkurve des historischen Referenzlaufs]{Evaluation - Lernkurve
des historischen Referenzlaufs (Bedingung C, Umgebung vor der v11-Revision),
eigene Darstellung.}
\label{fig:lernkurve}
\end{figure}

\subsubsection{Endergebnis (v12, $n=7$)}
\label{sec:endergebnis}

Die sieben finalen Curriculum-Läufe (alle vier Phasen durchlaufen) ergeben auf
dem je Lauf nach Validierungs-SR besten Modell:

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Lauf} & \textbf{A stoch.} & \textbf{A det.} & \textbf{B stoch.} & \textbf{B det.} \\
\midrule
Seed 1 & 62\,\% & 38\,\% & 76\,\% & 46\,\% \\
Seed 2 & 84\,\% & 26\,\% & 76\,\% & 44\,\% \\
Seed 3 & 76\,\% & 32\,\% & 80\,\% & 36\,\% \\
Seed 4 & 74\,\% & 40\,\% & 74\,\% & 38\,\% \\
Seed 5 & 58\,\% & 20\,\% & 62\,\% & 34\,\% \\
Seed 6 & 50\,\% & 20\,\% & 50\,\% & 14\,\% \\
Seed 7 & 56\,\% & 28\,\% & 50\,\% & 16\,\% \\
\midrule
\textbf{Mittel $\pm$ Std} & \textbf{65{,}7 $\pm$ 12{,}4} & 29{,}1 $\pm$ 8{,}0 & \textbf{66{,}9 $\pm$ 12{,}8} & 32{,}6 $\pm$ 12{,}7 \\
95\,\%-CI & [54{,}2;\,77{,}2] & [21{,}8;\,36{,}5] & [55{,}0;\,78{,}7] & [20{,}8;\,44{,}4] \\
\bottomrule
\end{tabular}
\caption[Endergebnis der v12-Läufe über sieben Seeds]{Evaluation -
Endergebnis der sieben v12-Läufe auf Testset~A und Holdout~B mit
Standardabweichung und Konfidenzintervall, eigene Darstellung.}
\label{tab:v12}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.78\textwidth]{figures/fig_v12_endergebnis.pdf}
\caption[Endergebnis v12 mit Streuung über sieben Läufe]{Evaluation -
Endergebnis aus Tabelle~\ref{tab:v12} als Balken mit Fehlerbalken, den sieben
Einzelläufen und den vorab festgelegten Erfolgsschwellen, eigene
Darstellung.}
\label{fig:v12endergebnis}
\end{figure}

Die sieben Einzelläufe streuen auf Testset~A zwischen 50\,\% und 84\,\%; die
Erfolgsschwelle aus Abschnitt~\ref{sec:ziele} liegt mitten in dieser
Streuung. Das Holdout-Kriterium ist damit erfüllt ($66{,}9\,\% \geq 60\,\%$),
das Testset-Kriterium verfehlt ($65{,}7\,\%$ gegenüber $\geq 70\,\%$, Abstand
$4{,}3$ Prozentpunkte); deterministisch werden beide Kriterien verfehlt
($29{,}1\,\%$ / $32{,}6\,\%$, Einordnung in Abschnitt~\ref{sec:detstochgap}).
Ausgewertet wird je Lauf nicht der letzte Checkpoint, sondern das nach
Validierungs-Erfolgsquote beste Modell, ausschließlich auf den disjunkten
Seeds 6000--6049 bestimmt.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llrr@{}}
\toprule
\textbf{Phase} & \textbf{Ziel-SR} & \textbf{Ziel berührt} & \textbf{Phase bestanden} \\
\midrule
1 & 85\,\% (stoch.) & 5 / 7 & \textbf{1 / 7} \\
2 & 70\,\% (stoch.) & 7 / 7 & \textbf{3 / 7} \\
3 & 70\,\% (det.)   & 0 / 7 & 0 / 7 \\
4 & 60\,\% (det.)   & 0 / 7 & 0 / 7 \\
\bottomrule
\end{tabular}
\caption[Gating-Bilanz der sieben v12-Läufe]{Evaluation - Gating-Bilanz der
sieben v12-Läufe je Curriculum-Phase, eigene Darstellung.}
\label{tab:gating}
\end{table}

Nur die Phasen 1 und 2 erreichten ihr Ziel in einzelnen Läufen; die Phasen~3
und~4, aus denen das berichtete Modell stammt, erreichten ihr Ziel in
\emph{keinem} der sieben Läufe und endeten stattdessen ausnahmslos am
Schrittbudget (Tabelle~\ref{tab:gating}). Das Curriculum war für das
berichtete Modell damit faktisch budget- statt leistungsgesteuert.
Abbildung~\ref{fig:v12kurven} zeigt die zugrunde liegenden Trainingsverläufe:
Die hohe Volatilität der Lerndynamik innerhalb der Phasen ist dieselbe
Streuungsquelle, die sich in den $\pm 12$ Punkten des Endergebnisses
niederschlägt.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/fig_v12_lernkurven.pdf}
\caption[Trainingsverläufe der sieben v12-Läufe]{Evaluation -
Trainingsverläufe der sieben v12-Läufe auf den Validierungs-Seeds, getrennt
nach Curriculum-Phase, eigene Darstellung.}
\label{fig:v12kurven}
\end{figure}

Eine Zwischenauswertung über die ersten drei Läufe hatte mit $73{,}3\,\%$ /
$80{,}0\,\%$ (A/B) beide Kriterien noch erfüllt; die gezielte Aufstockung auf
sieben Läufe senkte beide Mittelwerte deutlich. Messfehler, Unterschiede im
Code-Stand zwischen den beiden Lauf-Chargen und die Laufzeitumgebung wurden
als Ursachen geprüft und ausgeschlossen. Es handelt sich um Seed-Varianz
zwischen unabhängigen Trainingsläufen, wie sie Henderson et al.\
\cite{henderson2018matters} für Deep RL beschreiben und weshalb sie
Mittelwert und Standardabweichung über mehrere Läufe statt eines
Einzellaufs fordern.

\subsubsection{Ungelernte Referenzpolitiken und die Grenzen der Erfolgsquote}
\label{sec:baselines}

Eine Erfolgsquote ist ohne Untergrenze nicht interpretierbar: Ohne Bezugspunkt
bleibt offen, ob 65\,\% eine gelernte Fähigkeit ausdrücken oder bereits durch
Zufall erreichbar sind. Verglichen werden zwei ungelernte Politiken, die
\emph{dieselbe} Beobachtung erhalten wie der Agent:

\begin{itemize}
  \item \textbf{Random:} gleichverteilte Bewegung; absolute Untergrenze.
  \item \textbf{Kompass-Zufallslauf:} mit Wahrscheinlichkeit $\varepsilon$
        gleichverteilt zufällig, sonst ein Schritt in die dominante Richtung des
        Luftlinien-Kompasses ($\mathit{exitDx}/\mathit{exitDy}$). Diese Politik
        nutzt \emph{zwei} der 229 Merkmale, hat kein Gedächtnis, keine
        Parameter und wurde nicht trainiert. Berichtet werden zwei
        Einstellungen, ausschließlich auf den Validierungs-Seeds gewählt: die
        erfolgsoptimale ($\varepsilon = 0{,}9$) und die effizienzoptimale
        ($\varepsilon = 0{,}4$).
\end{itemize}

Als zweite Metrik dient die \textbf{Pfadeffizienz}
$\eta = d_{\mathrm{BFS}}(s_0)\,/\,T_{\mathrm{Erfolg}}$, gemittelt über die
erfolgreichen Episoden: das Verhältnis aus kürzestem Weg und tatsächlich
benötigten Schritten. $\eta = 1$ bedeutet optimale Navigation, $\eta = 0{,}05$
einen zwanzigfachen Umweg. Die Metrik ist erforderlich, weil die Erfolgsquote
bei einem Episodenlimit von 4\,000 Schritten und einer mittleren Optimallänge
von 40 Schritten kaum diskriminiert. Tabelle~\ref{tab:baselines} stellt beide
Metriken für alle Politiken gegenüber.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
& \multicolumn{2}{c}{\textbf{Testset A}} & \multicolumn{2}{c}{\textbf{Holdout B}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Politik} & \textbf{SR} & \textbf{$\eta$} & \textbf{SR} & \textbf{$\eta$} \\
\midrule
Random (gleichverteilt) & $5{,}2 \pm 2{,}7$ & 0{,}021 & $10{,}4 \pm 1{,}7$ & 0{,}025 \\
Kompass-Zufallslauf, $\varepsilon = 0{,}4$ & $60{,}4 \pm 4{,}8$ & \textbf{0{,}153} & $61{,}6 \pm 5{,}0$ & \textbf{0{,}161} \\
Kompass-Zufallslauf, $\varepsilon = 0{,}9$ & $\mathbf{92{,}0 \pm 5{,}1}$ & 0{,}047 & $\mathbf{91{,}6 \pm 4{,}3}$ & 0{,}048 \\
\midrule
RecurrentPPO v12 ($n=7$, stoch.) & $65{,}7 \pm 12{,}4$ & 0{,}049 & $66{,}9 \pm 12{,}8$ & 0{,}057 \\
\bottomrule
\end{tabular}
\caption[Ungelernte Referenzpolitiken gegenüber dem trainierten Agenten]{Evaluation -
Erfolgsquote und Pfadeffizienz ungelernter Referenzpolitiken gegenüber dem
trainierten Agenten unter identischem Protokoll, eigene Darstellung.}
\label{tab:baselines}
\end{table}

\paragraph{Befund.} Der Kompass-Zufallslauf übertrifft den trainierten Agenten
auf \emph{beiden} Achsen. Bei vergleichbarer Pfadeffizienz
($\eta \approx 0{,}05$) erreicht er 92\,\% statt 65{,}7\,\%; bei
vergleichbarer Erfolgsquote ($\approx 60\,\%$) ist er mit $\eta = 0{,}15$ rund
dreimal wegeffizienter. Der Agent liegt zwar deutlich über der
Zufallsuntergrenze von 5--10\,\%, erreicht aber an keinem Betriebspunkt die
Leistung einer Heuristik aus vier Zeilen Code, die 227 seiner 229
Eingangsmerkmale ignoriert. Der Befund ist robust gegenüber der Wahl des
Politik-Zufallsstroms: Ein Wechsel des Zufallsstroms verschiebt die
Random-Zeile deutlich (Verdopplung), die Kompass-Zeile dagegen kaum
($92{,}0 \to 89{,}6$ bei $\varepsilon = 0{,}9$), und der Abstand zwischen
Kompass und Agent bleibt in beiden Fällen groß.

\paragraph{Kein Episodenbudget kehrt das Bild um.} Naheliegend wäre der
Einwand, das Problem liege allein am großzügigen Limit von 4\,000 Schritten.
Da wer eine Welt bei Schritt $T$ löst, sie auch unter jedem Budget $\geq T$
löst, folgt aus den protokollierten Erfolgsschritten die vollständige Kurve
\emph{Erfolgsquote über Budget}:

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}rrrrrr@{}}
\toprule
& \multicolumn{2}{c}{\textbf{Kompass-Zufallslauf}} & \multicolumn{3}{c}{\textbf{RecurrentPPO v12}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-6}
\textbf{Budget} & $\varepsilon = 0{,}4$ & $\varepsilon = 0{,}9$ & Lauf 1 & Lauf 2 & Lauf 3 \\
\midrule
120 ($3\times$ Optimum) & \textbf{4\,\%}  & 0\,\%  & 0\,\%  & 2\,\%  & 0\,\% \\
200                     & \textbf{22\,\%} & 0\,\%  & 0\,\%  & 4\,\%  & 0\,\% \\
300                     & \textbf{28\,\%} & 0\,\%  & 2\,\%  & 10\,\% & 6\,\% \\
500                     & \textbf{36\,\%} & 10\,\% & 12\,\% & 22\,\% & 12\,\% \\
800                     & \textbf{54\,\%} & 18\,\% & 20\,\% & 32\,\% & 30\,\% \\
1\,500                  & \textbf{62\,\%} & 54\,\% & 40\,\% & 48\,\% & 54\,\% \\
4\,000                  & 64\,\%          & \textbf{88\,\%} & 64\,\% & 78\,\% & 74\,\% \\
\bottomrule
\end{tabular}
\caption[Erfolgsquote als Funktion des Episodenbudgets]{Evaluation -
Erfolgsquote auf Testset~A in Abhängigkeit des Episodenbudgets, je Zeile der
Höchstwert hervorgehoben, eigene Darstellung.}
\label{tab:capsweep}
\end{table}

Bei \emph{jedem} Budget erreicht oder übertrifft mindestens eine der beiden
ungelernten Politiken alle drei trainierten Läufe. Ein engeres Episodenlimit
ist als Korrektur damit nicht ausreichend: Drei Eigenschaften der Aufgabe
wirken zusammen, das großzügige Episodenlimit, der dauerhaft verfügbare
Luftlinien-Kompass und die hinreichend offenen Welten, unter denen Gedächtnis
für die Erfolgsquote schlicht nicht erforderlich ist.

\paragraph{Das Reward-Design verstärkt diesen Effekt.} Tabelle~\ref{tab:rewardbilanz}
bilanziert die dichten Reward-Terme über eine typische erfolgreiche Episode
(Läufe 1--3, Testset~A).

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Term} & \textbf{Summe je Episode} & \textbf{Anteil (Betrag)} \\
\midrule
Explorations-Bonus ($+0{,}02 \times 203$ neue Tiles) & $+4{,}1$ & 16\,\% \\
Schritt-Malus ($-0{,}01 \times 1\,329$ Schritte)     & $-13{,}3$ & 53\,\% \\
Revisit-Penalty                                      & $-6{,}2$ & 25\,\% \\
\midrule
\textbf{PBRS gesamt} (gemessen)                      & $\mathbf{+1{,}3}$ & \textbf{5\,\%} \\
\quad davon teleskopierter Anteil $\beta d_0/128$    & $+0{,}8$ & \\
\quad davon Restterm $\beta(1-\gamma)\sum_t d_t/128$ & $+0{,}5$ & \\
\midrule
Summe der dichten Terme & $-14{,}1$ & \\
Terminal-Reward (sparse) & $+100$ & \\
\bottomrule
\end{tabular}
\caption[Bilanz der dichten Reward-Terme]{Evaluation - Bilanz der dichten
Reward-Terme über eine mittlere erfolgreiche Episode, eigene Darstellung.}
\label{tab:rewardbilanz}
\end{table}

Entscheidend ist die Asymmetrie in der letzten Spalte. Das einzige dichte
Signal, das \emph{Richtungen unterscheidet}, ist der PBRS-Term, und er trägt
nur rund 5\,\% der dichten Reward-Masse; die übrigen 95\,\% hängen
ausschließlich davon ab, \emph{wie lange} der Agent unterwegs ist und
\emph{wie viel Fläche} er dabei abdeckt. Der Terminal-Reward bleibt zwar mit
$100 \cdot \mathbb{E}[\gamma^{T}] = +37{,}7$ (diskontiert) das dominierende
Zielsignal gegenüber $-5{,}3$ dichten Termen, doch über den Verlauf der
Episode hinweg sieht der Agent vor allem ein Signal über Weglänge und
Flächendeckung, kaum eines über die Richtung zum Ziel. Herumlaufen ist dabei
nicht kostenlos, die dichte Bilanz ist mit $-14{,}1$ klar negativ; die
Schwäche liegt also nicht darin, dass Umherirren belohnt würde, sondern
darin, dass der Agent aus dem dichten Signal kaum lernen kann, \emph{wohin}
er gehen soll.

Damit ist die zentrale Leistungsaussage dieser Arbeit einzuschränken: Belegt
ist, dass der Agent die Aufgabe löst und dabei deutlich über der
Zufallsuntergrenze liegt; \emph{nicht} belegt ist, dass er eine Strategie
gelernt hat, die über gerichtete Zufallssuche hinausgeht. Diese Einschränkung
betrifft auch die Gegenüberstellung in Abschnitt~\ref{sec:ablation}: Wenn eine
kompassgetriebene Zufallspolitik 92\,\% erreicht, ist der Vergleich
\glqq LSTM ohne BFS\grqq{} gegen \glqq MLP mit BFS\grqq{} kein Test der
Gedächtnisfähigkeit, sondern misst überwiegend, wie gut die jeweilige Politik
den Kompass ausnutzt und wie stark sie in Schleifen gerät. Die notwendige
Konsequenz, eine Härtung der Weltgenerierung vor jedem weiteren Training, ist
in Abschnitt~\ref{sec:ausblick} ausgeführt.

\subsubsection{Der Det/Stoch-Gap: Weglängen-Analyse und POMDP-Einordnung}
\label{sec:detstochgap}

Der Abstand zwischen stochastischer und deterministischer SR ist kein
Schwelleneffekt, sondern wächst kontinuierlich mit der Weglänge: Bei 5--15
Feldern erreicht die deterministische Betriebsart 79\,\%, bei 35--45 Feldern
noch 31\,\%, während die stochastische zwischen 100\,\% und 82\,\% bleibt
(Abbildung~\ref{fig:detgap}; 120 Welten aus dem Validierungsbereich, nach
Startdistanz gruppiert). Je länger der Weg ohne direkte Zielsicht
(Sichtradius 7), desto mehr Kreuzungsentscheidungen unter unsicherem
Belief-State; an jeder liegen die Aktionswahrscheinlichkeiten nahe
beieinander, und jede einzelne kann die Argmax-Politik in eine
Oszillationsschleife führen, aus der Sampling entkommt.

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{figures/fig_det_gap_distanz.pdf}
\caption[Erfolgsquote nach Startdistanz zum Exit]{Evaluation - Erfolgsquote
des finalen v12-Modells nach Startdistanz zum Exit, eigene Darstellung.}
\label{fig:detgap}
\end{figure}

Der Gap hat zwei sich ergänzende Ursachen. Die unmittelbare, mechanistische
Ursache ist die eben beschriebene Oszillation der Argmax-Politik an
gleichwahrscheinlichen Kreuzungsentscheidungen. Theoretisch begrenzt wird das
Erreichbare zusätzlich dadurch, dass die Generalisierungsanforderung auf
unbekannte Seeds selbst eine Form partieller Beobachtbarkeit induziert
(\emph{epistemic POMDP}), deren optimale Politik stochastisch bleibt und die
durch mehr Gedächtnis über die aktuelle Episode nicht auflösbar ist
\cite{ghosh2021epistemic}; darauf, nicht auf die allgemeinere POMDP-Optimalität
stochastischer Politiken \cite{singh1994pomdp}, stützt sich die Wahl der
stochastischen Evaluation als Primärmetrik. Der Gap ist damit teils erwartbar
(epistemische Unsicherheit über Seeds), teils ein offenes Defizit des
gelernten Belief-States; die Trennung beider Anteile ist mit den
vorliegenden Daten nicht möglich (Diagnoseschritt im Ausblick,
Abschnitt~\ref{sec:ausblick}).

\subsubsection{Versuche zur Schließung des Det/Stoch-Gaps}
\label{sec:gapexperimente}

Um zu prüfen, ob der Gap durch gezielte Eingriffe verkleinerbar ist, wurden drei
Varianten gerechnet, die je einen inhaltlichen Faktor gegenüber der
v12-Basiskonfiguration (Tabelle~\ref{tab:basiskonfig}) verändern:
\textbf{E1} verstärkt den Critic (\texttt{vf\_coef} $0{,}5\to1{,}0$),
\textbf{E2} glättet den Curriculum-Übergang (Phase~3 nur bis Distanz~35 statt~45),
\textbf{E3} vergrößert das Gedächtnis (LSTM $256\to512$). Diese drei Läufe
sind Einzelmessungen ($n=1$) gegen eine Baseline aus drei Läufen und damit
keine Wirksamkeitsnachweise, sondern eine Vorauswahl: Sie können eine
Hypothese schwächen, aber nicht widerlegen; ihre Replikation mit weiteren
Seeds steht im Ausblick (Abschnitt~\ref{sec:ausblick}).

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llccc@{}}
\toprule
\textbf{Variante} & \textbf{Änderung ggü.\ Baseline} & \textbf{Start} & \textbf{Test A} & \textbf{Holdout B} \\
 & & & \textbf{(stoch/det)} & \textbf{(stoch/det)} \\
\midrule
\textbf{v12} (Baseline, $n{=}3$) & -- (LSTM 256, \texttt{vf\_coef} 0.5) & Phase 1 & 73/32\,\% & 80/42\,\% \\
\textbf{E1} critic & \texttt{vf\_coef} $0.5\!\to\!1.0$ & Phase 3 & 56/42\,\% & 72/36\,\% \\
\textbf{E2} curric & Phase 3 Exit $25\text{--}45\!\to\!25\text{--}35$ & Phase 3 & 26/16\,\% & 42/24\,\% \\
\textbf{E3} lstm512 & LSTM $256\!\to\!512$ & Phase 1 & 44/14\,\% & 58/20\,\% \\
\bottomrule
\end{tabular}
\caption[Varianten zur Schließung des Det/Stoch-Gaps]{Evaluation - Setup und
Ergebnis der drei Gap-Experimente gegenüber der Baseline, eigene
Darstellung.}
\label{tab:iterationen}
\end{table}

\textbf{Kein Arm zeigt einen Hinweis auf Verbesserung.} E1 hebt die
deterministische SR (42\,\% statt 32\,\% auf A), liegt stochastisch aber
niedriger (56\,\% statt 73\,\%); beide Abstände bleiben bei $n=1$ innerhalb
der üblichen Lauf-zu-Lauf-Streuung und sind nicht belastbar. E2 liegt mit
26\,\% auf A deutlich unter der Baseline, was angesichts des reduzierten
Phase-3-Trainingsbudgets dieser Variante nur teilweise ein Faktoreffekt ist.
E3 liegt mit 44\,\% auf A ebenfalls klar unter der Baseline, ist aber ein
voller Lauf mit identischem Budget und zeigt in \emph{allen}
Curriculum-Phasen Werte auf oder unter der 256er-Baseline bei rund
dreifachem Rechenaufwand: Mehr Gedächtnis-\emph{Kapazität} bedeutet in dieser
Umgebung jedenfalls nicht mehr Gedächtnis-\emph{Nutzen}, passend zu den
bekannten Grenzen rekurrenter Politiken in prozeduraler Navigation, deren
Gedächtnisanforderungen über reine Kapazität hinausgehen
\cite{pleines2022recurrent}.

Ergänzend wurde geprüft, ob der Gap lediglich ein Kalibrierungsartefakt der
Argmax-Auswertung ist. Ein Temperatur-Sweep zwischen reinem Argmax ($T{=}0$)
und vollem Sampling ($T{=}1$) auf den drei v12-Modellen zeigt einen
\emph{monotonen} Verlauf: Mehr Stochastik ist durchweg besser, keine
Zwischentemperatur übertrifft das volle Sampling.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Temperatur} & \textbf{Testset A} & \textbf{Holdout B} \\
\midrule
Argmax ($T{=}0$)     & $32{,}0 \pm 6{,}0$ & $42{,}0 \pm 5{,}3$ \\
$T = 0{,}25$         & $52{,}0 \pm 0{,}0$ & $52{,}7 \pm 2{,}3$ \\
$T = 0{,}5$          & $63{,}3 \pm 5{,}0$ & $71{,}3 \pm 8{,}1$ \\
$T = 0{,}75$         & $63{,}3 \pm 9{,}2$ & $72{,}0 \pm 8{,}0$ \\
\textbf{Sampling ($T{=}1$)} & $\mathbf{70{,}7 \pm 10{,}1}$ & $\mathbf{79{,}3 \pm 8{,}1}$ \\
\bottomrule
\end{tabular}
\caption[Temperatur-Sweep der Evaluation]{Evaluation - Erfolgsquote der drei
v12-Läufe in Abhängigkeit der Sampling-Temperatur, eigene Darstellung.}
\label{tab:temperatur}
\end{table}

Der Gap ist damit \emph{kein} Argmax-Artefakt, sondern hat eine der oben
diskutierten Ursachen. Bereits $T{=}0{,}5$ holt den Großteil des
Argmax-Defizits zurück (A: $+31$, B: $+29$ Prozentpunkte) und ist ein
geeigneter Betriebspunkt, falls ein weniger zufälliges Verhalten gewünscht
ist. Zusammengenommen widersteht der Det/Stoch-Gap dem Tuning einzelner
Stellschrauben und ist teils fundamental (POMDP); das harte, fundamentale
Langhorizont-Belief-Tracking ist am ehesten über strukturiertes statt
größeres Gedächtnis anzugehen (Abschnitt~\ref{sec:ausblick}).

\section{Fazit und Ausblick}
\label{sec:ausblick}

\subsection{Ergebnis (gemeinsam)}

Die Arbeit hatte zwei Fragen gestellt. Sie fallen unterschiedlich aus, und
gerade ihr Verhältnis zueinander ist das eigentliche Ergebnis.

\paragraph{F1 (Weltgenerator): beantwortet.} Der entwickelte Generator
erzeugt aus einem Seed reproduzierbare, chunkweise und damit unbegrenzt
erweiterbare Welten. Alle fünf Anforderungen aus Z1 sind nachgewiesen erfüllt
(Tabelle~\ref{tab:z1}): Determinismus bis hin zu bit-identischen
Trainingspfaden, 100\,\% Lösbarkeit über 3\,150 geprüfte Seeds, eine
Zielentfernung, die nach der Umstellung auf das BFS-Laufwegmaß exakt eingehalten
wird, ausreichender Durchsatz für Millionen Schritte und ein spielbarer Client
auf derselben Simulation. Die Kombination aus Spielbarkeit und Kontrollierbarkeit
ist dabei der Punkt, an dem sich der Aufwand eines eigenen Generators gegenüber
einem fertigen Benchmark auszahlt (Abschnitt~\ref{sec:relatedwork}).

\paragraph{F2 (Navigation ohne Orakel): nicht entscheidbar.} Über sieben
unabhängige Läufe erreicht der Agent $65{,}7\,\% \pm 12{,}4$ (Testset~A) und
$66{,}9\,\% \pm 12{,}8$ (Holdout~B) stochastische Erfolgsquote. Das
Holdout-Kriterium ($\geq 60\,\%$) ist damit erfüllt, das Testset-Kriterium
($\geq 70\,\%$) verfehlt. Diese Zahlen sind jedoch nur gegen die
Zufallsuntergrenze von 5--10\,\%
aussagekräftig, nicht gegen eine kompetente ungelernte Referenz: Ein
Kompass-Zufallslauf erreicht 92\,\% und ist bei vergleichbarer Erfolgsquote
zugleich dreimal wegeffizienter (Abschnitt~\ref{sec:baselines}). Bei einem
Episodenbudget vom Hundertfachen der Optimallänge misst die gewählte Metrik
nicht die Fähigkeit, um die es geht, und ein engeres Budget behebt das nicht
(Tabelle~\ref{tab:capsweep}). \textbf{F2 ist mit dem
vorliegenden Aufbau daher nicht entscheidbar: Die Umgebung erfordert das
Gedächtnis nicht, dessen Nutzen sie messen soll.} Dieser Befund betrifft die
Interpretation sämtlicher Leistungszahlen der Arbeit, einschließlich der
Gegenüberstellung in Abschnitt~\ref{sec:ablation}.

\paragraph{Das Verhältnis beider Antworten.} Auf den ersten Blick steht ein
gelungener Generator neben einem unentschiedenen Lernexperiment. Tatsächlich
haben beide dieselbe Ursache. Der Generator erfüllt seine Spezifikation
vollständig, und die Spezifikation war unvollständig: Z1 verlangt lösbare
und in ihrer Entfernung steuerbare Welten, aber keine, in denen die untersuchte
Fähigkeit auch erforderlich ist. Die konkrete Stelle, an der das entsteht, ist
im Generator benennbar und inzwischen vermessen: Ohne die abgeschaltete
Glättungsstufe stammt die Hindernisverteilung aus unkorreliertem Rauschen und
bildet verstreute Einzelhindernisse statt zusammenhängender Barrieren. Der
Umwegfaktor liegt bei 1,10, der kürzeste Weg ist also nur zehn Prozent länger
als die Luftlinie (Abschnitt~\ref{sec:weltgenerator}). Sackgassen, die
Gedächtnis erzwingen würden, entstehen so kaum. Die naheliegende Reparatur,
einfach die vorhandene Glättungsstufe einzuschalten, scheitert an derselben
niedrigen Basisdichte und macht die Welt noch leerer
(Tabelle~\ref{tab:geometrieprobe}).

Damit ist das Ergebnis dieser Arbeit weder eine Aussage über den Agenten noch
eine über den Generator, sondern eine über deren Zusammenspiel: \textbf{Die
Schwierigkeit einer Lernaufgabe ist eine Eigenschaft der Weltgenerierung und
muss dort spezifiziert und geprüft werden, bevor ein Lernverfahren
darauf angesetzt wird.} Die Referenzpolitik aus
Abschnitt~\ref{sec:baselines} ist genau das Instrument dafür, und sie kostet
Sekunden statt Trainingsstunden. Dass dieser Zusammenhang überhaupt sichtbar
wurde, setzte beide Projektteile voraus: Ohne den eigenen, in seinen
Parametern zugänglichen Generator wäre die Ursache nicht lokalisierbar gewesen,
ohne das RL-Experiment wäre sie nie aufgefallen.

\subsection{Beiträge (gemeinsam)}

\paragraph{Technisch.} Entstanden ist ein vollständiges Spiel, das zugleich als
Experimentierplattform dient: ein C++-Kern mit deterministischer prozeduraler
Weltgenerierung (Biomfeld aus interpoliertem Wertrauschen, biomabhängige
Basisbelegung, zelluläre Glättungsstufe, BFS-basierte Exit-Platzierung mit
Lösbarkeitsgarantie), ein spielbarer raylib-Client mit KI-Zuschaumodus und eine
\texttt{pybind11}-Kapselung als Gymnasium-Umgebung, dazu
Curriculum-Trainingspipeline, standardisierte Evaluationsskripte und
Live-Monitoring (Abschnitt~\ref{sec:umgebung} und~\ref{sec:rlumgebung}). Die
deterministische Reproduzierbarkeit pro Seed
und die durchgängige Experiment-Protokollierung waren die Voraussetzung für die
im Projektverlauf durchgeführten Root-Cause-Analysen (Tabelle~\ref{tab:versionsverlauf})
und sind für Folgearbeiten wiederverwendbar.

Der praktische Wert dieser Plattform liegt weniger in den einzelnen Verfahren,
die sämtlich Stand der Technik sind, als in ihrer Zugänglichkeit: Weil
Wandanteil, Glättung, Distanzmaß und Sicherheitsnetze einzeln schaltbar sind,
ließ sich die Geometrie der Welten in Stunden vermessen statt sie zu erraten.
Genau das hat die zunächst geplante Härtung über die Glättungsstufe widerlegt,
bevor eine einzige Trainingsstunde darauf verfiel
(Tabelle~\ref{tab:geometrieprobe}). Ein fremder Benchmark hätte diese Sonde
nicht zugelassen.

\paragraph{Inhaltlich.} Ein rekurrenter Agent ohne BFS-Gradientenfeatures
erreicht mindestens dieselbe stochastische Leistung wie ein MLP \emph{mit}
diesen Features; der LSTM-Zustand ersetzt die vorberechnete lokale
Pfadinformation. Diese Aussage ist doppelt begrenzt: durch die Einschränkungen
der Gegenüberstellung (Tabelle~\ref{tab:ablation}) und dadurch, dass der
globale Luftlinien-Kompass in beiden Bedingungen erhalten bleibt. Ersetzt wird
also die \emph{lokale} Wegweiserinformation, nicht jede globale Zielinformation.

\paragraph{Methodisch.} Die Zwischenauswertung über drei Läufe hatte
$73{,}3\,\%$ / $80{,}0\,\%$ ergeben und beide Kriterien scheinbar erfüllt. Die
Aufstockung auf sieben Läufe senkte beide Werte deutlich. Ein auf drei Läufen
berichtetes \glqq beide Ziele erfüllt\grqq{} wäre nicht falsch gemessen, aber
nicht haltbar gewesen. Zusammen mit dem Baseline-Befund reproduziert die Arbeit
damit zweimal am eigenen Gegenstand, wovor Henderson et al.\
\cite{henderson2018matters} warnen: zu wenige Läufe und ein fehlender
Referenzpunkt führen unabhängig voneinander zu Fehlschlüssen.

\subsection{Einschränkungen (gemeinsam)}

\begin{enumerate}
  \item \textbf{Die Erfolgsmetrik ist nicht trennscharf} und wird von einer
        ungelernten Heuristik übertroffen (Abschnitt~\ref{sec:baselines}). Dies
        ist die schwerwiegendste Einschränkung, weil sie alle übrigen
        Leistungsaussagen relativiert.
  \item \textbf{Das Testset-Kriterium wird verfehlt} ($65{,}7\,\%$ statt
        $\geq 70\,\%$); nur das Holdout-Kriterium ist erfüllt.
  \item \textbf{Der Det/Stoch-Gap bleibt bestehen} ($29{,}1\,\%$ bzw.\
        $32{,}6\,\%$ deterministisch). Er wächst mit der Weglänge und ist
        monoton in der Sampling-Temperatur, also kein Argmax-Artefakt.
        Theoretisch erwartbar ist nur der Anteil aus der epistemischen
        Unsicherheit über den Seed \cite{ghosh2021epistemic}; der Rest ist ein
        Defizit des gelernten Belief-States. Die drei Versuche zu seiner
        Schließung sind Einzelläufe und belegen nicht, dass er
        tuning-resistent \emph{ist}.
  \item \textbf{Begrenzte statistische Präzision:} Die Streuung über Läufe
        beträgt rund $\pm 12$ Punkte. Das wirkt in beide Richtungen: Das
        95\,\%-Konfidenzintervall auf Holdout~B reicht bis $55{,}0\,\%$ hinab und
        damit unter die im Mittel erfüllte Schwelle; das Intervall auf Testset~A
        reicht bis $77{,}2\,\%$ hinauf und schließt die 70\,\%-Schwelle ein. Das
        Testset-Kriterium ist im Punktschätzer verfehlt, nicht aber signifikant
        verfehlt. Für den Hauptbefund der Arbeit ist das ohne Belang, da er
        nicht an dieser Schwelle hängt (Abschnitt~\ref{sec:baselines}). Die
        Aufstockung von $n=3$ auf $n=7$ war eine Ressourcenentscheidung; der
        naheliegende und rechnerisch billige nächste Schritt~-- ein voller Lauf
        kostet auf CPU nur wenige Stunden~-- ist eine Erhöhung auf $n \geq 15$,
        die das Konfidenzintervall spürbar verengen und die Schwellenfrage
        entscheiden würde. Sie steht aus und ist im Ausblick vermerkt
        (Abschnitt~\ref{sec:ausblick}).
  \item \textbf{Ein einziger Generator} und \textbf{keine quantifizierte
        Baseline nicht-rekurrenter Verfahren} (DQN, A2C) auf der finalen
        Umgebung: Der frühe DQN-Anlauf scheiterte vor der v11-Revision und
        wurde nicht wiederholt.
  \item \textbf{Selektion und Berichterstattung verwenden verschiedene
        Betriebsarten.} Ausgewählt wird das nach \emph{deterministischer}
        Validierungs-SR beste Modell (Gating-Metrik der Phasen~3 und~4),
        berichtet und als Primärmetrik begründet wird die
        \emph{stochastische} SR. Bei einem Det/Stoch-Gap von über 30 Punkten,
        also dem zentralen Untersuchungsgegenstand, wird damit auf die eine
        Betriebsart selektiert und die andere gemessen. Das ist eine bewusste
        Entscheidung, weil die stochastische Auswertung theoretisch begründet
        ist (Abschnitt~\ref{sec:detstochgap}) und die deterministische in den
        frühen Phasen kein Signal trägt (Tabelle~\ref{tab:curriculum}); der
        so ausgewählte Checkpoint muss aber nicht der stochastisch beste sein.
        Eine Selektion auf der stochastischen Validierungs-SR steht als
        Gegenprobe aus (Abschnitt~\ref{sec:ausblick}).
\end{enumerate}

\subsection{Ausblick}

Die Reihenfolge der folgenden Punkte ergibt sich aus dem Befund von
Abschnitt~\ref{sec:baselines}: Solange die Aufgabe die untersuchte Fähigkeit
nicht erfordert, ist jede weitere Optimierung des Agenten nicht
interpretierbar. Die Reparatur des Messregimes steht deshalb vor allem anderen.

\subsubsection{Härtung der Weltgenerierung (Laurin)}

Der Wandanteil wird derzeit pro Biom
fest vorgegeben (7\,\% im Wald bis 25\,\% im Bergland) und stammt aus
unkorreliertem Hash-Rauschen; die Hindernisse sind dadurch überwiegend
verstreute Einzeltiles statt zusammenhängender Barrieren
(Abschnitt~\ref{sec:weltgenerator}). Nicht der Wandanteil allein ist
deshalb der wirksame Hebel, sondern die \emph{Korreliertheit} der
Verteilung. Der naheliegende Weg dorthin ist versperrt, und zwar
gemessen: Die implementierte Glättungsstufe einzuschalten senkt die
Wanddichte auf 0,037 und den Umwegfaktor auf 1,001, und jede
Regelvariante, die den Umweg spürbar hebt, zerstört die Lösbarkeit, im
Extremfall auf 3 von 50 Welten (Tabelle~\ref{tab:geometrieprobe}).
Eine Härtung verlangt deshalb drei Eingriffe im Code statt eines
Schalters: eine höhere und korrelierte Basisdichte in
\texttt{World::sampleBaseTile()}, eine aktive
Konnektivitätsvalidierung (\texttt{enableFloodFillValidation},
\texttt{enableMacroGraphPrecheck}) und eine Neuvalidierung nach jeder
Automateniteration. Zielgröße bleibt nicht ein bestimmter Wandanteil,
sondern ein Betriebspunkt, an dem der Kompass-Zufallslauf einbricht,
die Lösbarkeit aber bei 100\,\% bleibt. Das Vorgehen ist
zweistufig: erst die Baseline-Politik als Kalibrierinstrument einsetzen,
dann trainieren. Ein Trainingslauf ohne diese Vorabprüfung wäre erneut
nicht interpretierbar.

\subsubsection{Weiterentwicklung des RL-Trainings (Florian)}

\begin{enumerate}
  \item \textbf{Statistische Absicherung des Endergebnisses.} Bei einer Streuung
        von rund $\pm 12$ Punkten über Läufe überlappt das Konfidenzintervall
        die Erfolgsschwelle (Abschnitt~\ref{sec:endergebnis}). Da ein voller
        Trainingslauf auf CPU nur wenige Stunden kostet, ist eine Erhöhung von
        $n=7$ auf $n \geq 15$ der billigste Weg, die Schwellenfrage auf
        Testset~A statistisch zu entscheiden; sie ist rein rechenzeitgebunden
        und erfordert keine Codeänderung.
  \item \textbf{Nachschärfung des dichten Reward-Signals.} Die Bilanz in
        Tabelle~\ref{tab:rewardbilanz} zeigt, dass nur rund 5\,\% der dichten
        Reward-Masse überhaupt Richtungsinformation tragen. Naheliegend sind
        eine Anhebung von $\beta$ bzw.\ eine kleinere Normierungskonstante,
        eine Deckelung des Explorations-Bonus über die Episode und die
        Überprüfung des Revisit-Penalty, der als letzter verbliebener Term
        des früher verworfenen \glqq Straf-Stackings\grqq{} mit $-6{,}2$ je
        Episode überraschend schwer wiegt, deutlich schwerer als der
        Explorations-Bonus, den er damit faktisch neutralisiert. Da PBRS
        policy-invariant ist, die übrigen Terme aber nicht, verschiebt die
        aktuelle Gewichtung das Optimum weg von der eigentlichen Aufgabe.
  \item \textbf{Billige Diagnosen vor jedem Architekturwechsel.} (a)~Eine
        lineare Probe auf dem LSTM-Hidden-State, die Exit-Richtung bzw.\
        BFS-Distanz vorhersagt: Kollabiert sie auf langen Wegen, liegt ein
        Belief-Tracking-Limit vor, sonst liegt das Problem in der Politik.
        (b)~Entropie-Annealing in Phase~4 mit Modellselektion auf der
        deterministischen Erfolgsquote. (c)~One-Hot- oder Embedding-Kodierung
        des lokalen Grids statt der bisherigen ordinalen Tile-IDs; dass das
        Grid kaum genutzt wird, legt die Baseline-Analyse nahe.
        (d)~Replikation von E1 und E3 mit je zwei weiteren Seeds.
        (e)~Wiederholung des Baseline-Vergleichs mit abgeschalteter
        Early-Stop-Truncation, um auszuschließen, dass diese
        explorationsabhängige Abbruchbedingung
        (Abschnitt~\ref{sec:evalprotokoll}) den Vergleich beeinflusst; bei den
        trainierten Läufen greift sie in unter 3\,\% der Episoden, für die
        Referenzpolitiken ist sie bislang nicht ausgezählt.
  \item \textbf{Restpotenzial in RecurrentPPO.} Ein Auxiliary-Loss-Kopf, der
        aus dem Hidden-State die BFS-Distanz vorhersagt, formt den
        Belief-State explizit und nutzt das Orakel als Trainings-, nicht als
        Testzeitsignal. Ergänzend Burn-in für Sequenzstarts nach R2D2
        \cite{kapturowski2019r2d2}, temperaturkalibrierte Evaluation
        ($T = 0{,}5$ ohne Neutraining) sowie vollwertiges Prioritized Level
        Replay \cite{jiang2021plr} und Self-Imitation \cite{oh2018sil}.
  \item \textbf{Architekturvergleich als Folgearbeit.} Der Wechsel auf
        Transformer-basierte In-Context-Verfahren (z.\,B.\ AMAGO
        \cite{grigsby2024amago}) ist die meistgenannte Richtung, hier aber
        bewusst nur eine Hypothese. Denn erstens ist nicht belegt, dass der Gap
        ein Kapazitätsproblem ist: Eine unter hoher Entropie trainierte Politik
        kann ihr Restrauschen zum Verlassen von Schleifen nutzen, ohne je einen
        Anreiz für eine deterministisch tragfähige Aktionswahl gehabt zu haben.
        Zweitens bleiben rekurrente Modelle im breitesten verfügbaren
        POMDP-Vergleich gegenüber den dort geprüften Attention-Modellen
        konkurrenzfähig; zu beachten ist, dass es sich dabei um
        \emph{lineare} Transformer-Varianten handelt und nicht um volle
        Softmax-Attention, wie AMAGO sie verwendet
        \cite{morad2023popgym}. Die eigene E3-Ablation zeigte, dass mehr
        Gedächtniskapazität hier schadet. Erst wenn Schritt~2 ein
        Kapazitätslimit nachweist, wird der Wechsel zur begründeten nächsten
        Frage; dann als kontrollierter Vergleich auf demselben Generator und
        Protokoll.
\end{enumerate}

\clearpage
\addcontentsline{toc}{section}{Literaturverzeichnis}
\bibliographystyle{unsrt}
\bibliography{references}

\end{document}

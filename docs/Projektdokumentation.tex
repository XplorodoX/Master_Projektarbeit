\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[ngerman]{babel}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, fit, backgrounds,
                decorations.pathreplacing}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage{longtable}
\geometry{a4paper, left=3cm, right=2.5cm, top=2.5cm, bottom=2.5cm}

% Verzeichnisse sollen im PDF-Lesezeichenbaum und im Inhaltsverzeichnis auftauchen
\usepackage[titles]{tocloft}

\begin{document}

% ===================================================================
%  DECKBLATT
% ===================================================================
\begin{titlepage}
\centering
\vspace*{1cm}

{\large\scshape Hochschule Aalen\\
Technik, Wirtschaft und Gesundheit\par}
\vspace{0.4cm}
{\large Fakultät Elektronik und Informatik\par}
\vspace{0.3cm}
{\large Masterstudiengang Informatik und Security (M.\,Sc.)\par}

\vspace{2.5cm}

{\Large\bfseries Projektarbeit\par}

\vspace{1.2cm}

\rule{\textwidth}{0.4pt}
\vspace{0.6cm}

{\huge\bfseries Navigation in prozedural generierten\\[0.25cm]
2D-Welten mit Reinforcement Learning\par}

\vspace{0.7cm}

{\Large Generalisierung eines rekurrenten Agenten auf\\[0.15cm]
unbekannte Welten am Beispiel \emph{Stoneforge}\par}

\vspace{0.6cm}
\rule{\textwidth}{0.4pt}

\vfill

\begin{tabular}{@{}ll@{}}
\textbf{Vorgelegt von:}    & Florian Merlau \\
                           & Laurin Rößler \\[0.5cm]
\textbf{Betreuer:}         & Prof.\ Dr.\ Carsten Lecon \\[0.5cm]
\textbf{Abgabedatum:}      & 25.\ Juli 2026 \\
\end{tabular}

\vspace{1.5cm}

\end{titlepage}

% ===================================================================
%  VORSPANN  (römische Seitenzahlen)
% ===================================================================
\pagenumbering{roman}
\setcounter{page}{2}

% ===================================================================
%  EIDESSTATTLICHE ERKLÄRUNG
% ===================================================================
\section*{Eidesstattliche Erklärung}
\addcontentsline{toc}{section}{Eidesstattliche Erklärung}

Wir versichern hiermit, dass wir die vorliegende Projektarbeit mit dem Thema
\glqq Navigation in prozedural generierten 2D-Welten mit Reinforcement
Learning\grqq{} selbstständig verfasst und keine anderen als die angegebenen
Quellen und Hilfsmittel benutzt haben. Alle Stellen, die wörtlich oder
sinngemäß aus Veröffentlichungen oder anderen Quellen entnommen sind, sind als
solche kenntlich gemacht. Die Arbeit wurde in gleicher oder ähnlicher Form
noch keiner Prüfungsbehörde vorgelegt.

Eingesetzte Hilfsmittel sind im Anhang~\ref{sec:reproduzierbarkeit}
dokumentiert; der vollständige Quellcode sowie das lückenlos geführte
Experiment-Changelog liegen dem Projekt-Repository bei.

\vspace{2.5cm}

\noindent
\begin{tabular}{@{}p{6cm}p{6cm}@{}}
\hrulefill & \hrulefill \\[0.2cm]
Ort, Datum & Florian Merlau \\[2.5cm]
\hrulefill & \hrulefill \\[0.2cm]
Ort, Datum & Laurin Rößler \\
\end{tabular}

\clearpage

% ===================================================================
%  KURZFASSUNG
% ===================================================================
\section*{Kurzfassung}
\addcontentsline{toc}{section}{Kurzfassung}

Diese Projektarbeit untersucht, ob ein Reinforcement-Learning-Agent in
prozedural generierten, nur teilweise einsehbaren 2D-Welten eine
Navigationsstrategie lernt, die auf unbekannte Welten übertragbar ist. Als
Versuchsplattform dient das eigens entwickelte Spiel \emph{Stoneforge}
(C++-Kern, Python-Anbindung). Der Agent sieht nur einen lokalen
$15{\times}15$-Ausschnitt und muss den Ausgang finden. Trainiert wird
RecurrentPPO (PPO mit LSTM-Gedächtnis) mit einem vierstufigen Curriculum;
bewertet wird die Erfolgsquote auf 50 trainingsfremden Welten, gemittelt über
sieben unabhängige Trainingsläufe.

Der Agent erreicht $65{,}7\,\% \pm 12{,}4$ auf dem Testset und
$66{,}9\,\% \pm 12{,}8$ auf dem Holdout und erfüllt damit das Holdout-, nicht
aber das Testset-Kriterium. Über dieses Einzelergebnis hinaus liefert die
Arbeit zwei methodische Beiträge. Erstens einen kalibrierten Bezugspunkt: Eine
ungelernte Kompass-Heuristik erreicht unter identischem Protokoll $92\,\%$ und
ist bei vergleichbarer Erfolgsquote dreimal wegeffizienter als der trainierte
Agent. Die Erfolgsquote ist in dieser Parametrisierung folglich nicht
trennscharf, denn die Umgebung erfordert das Gedächtnis nicht, dessen Nutzen
sie messen soll. Zweitens die Quantifizierung der Streuung über
Trainingsläufe: Die Aufstockung von drei auf sieben Läufe senkte das Ergebnis
um rund acht Prozentpunkte; wenige Läufe führen hier systematisch zu
optimistischen Aussagen. Aus beiden Befunden leitet die Arbeit ein korrigiertes
Messregime für Folgearbeiten ab und stellt mit der Referenzpolitik das
Instrument bereit, mit dem sich die Aufgabenschwierigkeit vorab kalibrieren
lässt.

\vspace{0.5cm}
\noindent\textbf{Schlagwörter:} Reinforcement Learning, POMDP, Generalisierung,
prozedurale Generierung, RecurrentPPO, Curriculum Learning, Baseline-Vergleich

\clearpage

% ===================================================================
%  VERZEICHNISSE
% ===================================================================
\tableofcontents
\clearpage

\listoffigures
\addcontentsline{toc}{section}{Abbildungsverzeichnis}
\clearpage

\listoftables
\addcontentsline{toc}{section}{Tabellenverzeichnis}
\clearpage

\section*{Abkürzungs- und Symbolverzeichnis}
\addcontentsline{toc}{section}{Abkürzungs- und Symbolverzeichnis}

\begin{longtable}{@{}ll@{}}
\toprule
\textbf{Kürzel} & \textbf{Bedeutung} \\
\midrule
\endhead
A2C   & Advantage Actor-Critic, synchrone Variante von A3C \\
\texttt{approx\_kl} & Maß für die Stärke einer Politikänderung pro Update \\
BFS   & \emph{Breadth-First Search}, Breitensuche; liefert den kürzesten begehbaren Weg \\
BPTT  & \emph{Backpropagation Through Time}, Lernverfahren rekurrenter Netze \\
CNN   & \emph{Convolutional Neural Network}, faltendes neuronales Netz \\
DQN   & \emph{Deep Q-Network}, wertbasiertes RL-Verfahren \\
DRQN  & \emph{Deep Recurrent Q-Network}, DQN mit Gedächtnis \\
EV    & \emph{Explained Variance}, Gütemaß der gelernten Wertfunktion \\
GAE   & \emph{Generalized Advantage Estimation}, Schätzung des Vorteils \\
HARKing & \emph{Hypothesizing After the Results are Known} \\
LSTM  & \emph{Long Short-Term Memory}, rekurrente Netzarchitektur (Gedächtnis) \\
MDP   & \emph{Markov Decision Process}, Markow-Entscheidungsprozess \\
MLP   & \emph{Multilayer Perceptron}, vorwärtsgerichtetes Netz ohne Gedächtnis \\
MPS   & \emph{Metal Performance Shaders}, GPU-Backend von Apple \\
PBRS  & \emph{Potential-Based Reward Shaping}, politikerhaltende Belohnungsformung \\
PLR   & \emph{Prioritized Level Replay}, priorisierte Level-Wiederholung \\
POMDP & \emph{Partially Observable MDP}, teilweise beobachtbarer MDP \\
PPO   & \emph{Proximal Policy Optimization}, das hier verwendete RL-Hauptverfahren \\
RL    & \emph{Reinforcement Learning}, bestärkendes Lernen \\
SB3   & \emph{Stable-Baselines3}, verwendete RL-Bibliothek \\
SR    & \emph{Success Rate}, Erfolgsquote: Anteil gelöster Welten \\
\midrule
$\gamma$      & Diskontfaktor: Gewicht künftiger Belohnungen (hier $0{,}999$) \\
$\varepsilon$ & Zufallsanteil der Vergleichspolitik (Abschnitt~\ref{sec:baselines}) \\
$\eta$        & Pfadeffizienz: kürzester Weg geteilt durch tatsächliche Schritte \\
$\pi(a\mid s)$ & Politik: Wahrscheinlichkeit, im Zustand $s$ die Aktion $a$ zu wählen \\
$\Phi(s)$     & Potentialfunktion des Reward Shapings \\
$\sigma$      & Standardabweichung über Trainingsläufe \\
\bottomrule
\end{longtable}

\clearpage

% ===================================================================
%  HAUPTTEIL  (arabische Seitenzahlen)
% ===================================================================
\pagenumbering{arabic}
\setcounter{page}{1}

\section{Einleitung}
\label{sec:einleitung}

\subsection{Motivation}

Ein Computerprogramm, das ein Videospiel-Level fehlerfrei durchspielt, hat
zunächst nur eines bewiesen: dass es \emph{dieses} Level beherrscht. Interessant
wird es erst, wenn es ein Level meistert, das es nie zuvor gesehen hat. Genau
diese Unterscheidung, zwischen Auswendiglernen und tatsächlichem Lernen einer
Strategie, ist die zentrale offene Frage beim praktischen Einsatz von
\emph{Reinforcement Learning} (RL, bestärkendes Lernen): einem Verfahren, bei
dem ein Programm (der \emph{Agent}) nicht durch vorgegebene Regeln, sondern
durch Versuch, Irrtum und Belohnung lernt.

Die Übertragbarkeit auf Unbekanntes heißt in der Fachsprache
\emph{Generalisierung}. Sie zu prüfen ist schwierig, weil ein Agent, der immer
dieselbe Umgebung sieht, deren Eigenheiten mitlernt, ohne dass es auffällt. Ein
etabliertes Gegenmittel ist die \emph{prozedurale Generierung}: Die Umgebung
wird für jeden Durchlauf neu per Zufallszahl erzeugt, sodass Auswendiglernen
nichts nützt \cite{cobbe2020procgen, risi2020increasing}. Auf diesem Prinzip
beruht die vorliegende Arbeit.

\subsection{Die Aufgabe in Kürze}
\label{sec:aufgabe_kurz}

Für diese Arbeit wurde ein eigenes 2D-Spiel entwickelt: \emph{Stoneforge}. Die
Spielwelt besteht aus einem Raster aus Feldern (Wiese, Wald, Fels, Wasser), das
aus einer Zufallszahl, dem sogenannten \emph{Seed}, erzeugt wird. Derselbe Seed
erzeugt immer dieselbe Welt, ein anderer Seed eine völlig andere. Irgendwo in
dieser Welt liegt ein Ausgang (\emph{Exit}); der Agent startet in der Mitte und
soll ihn erreichen. Er darf pro Zug ein Feld nach oben, unten, links oder
rechts gehen.

Die Schwierigkeit liegt darin, dass der Agent die Welt nicht überblickt. Er
sieht nur einen Ausschnitt von $15\times15$ Feldern um sich herum; alles
darüber hinaus ist für ihn schwarz. Zusätzlich kennt er die \emph{Richtung}, in
der der Ausgang liegt, so wie ein Kompass die Himmelsrichtung anzeigt, aber
nicht den \emph{Weg} dorthin. Steht eine lange Felswand zwischen ihm und dem
Ziel, muss er selbst herausfinden, ob er sie nördlich oder südlich umgehen
soll, und sich dabei merken, wo er schon war. Abbildung~\ref{fig:pomdp_schema}
auf Seite~\pageref{fig:pomdp_schema} stellt diesen Unterschied zwischen
Weltzustand und Agentensicht gegenüber.

Gelernt wird durch Belohnung: Erreicht der Agent den Ausgang, erhält er einen
großen positiven Wert; jeder Schritt kostet ein wenig. Über Millionen von
Versuchen passt er sein Verhalten so an, dass die Summe der Belohnungen
möglichst groß wird. Gemessen wird am Ende die \emph{Erfolgsquote}
(\emph{Success Rate}): der Anteil von 50 vorher nie gesehenen Welten, in denen
er den Ausgang findet.

\subsection{Problemstellung und Forschungsfrage}

Aus dem beschriebenen Aufbau folgt die eigentliche Schwierigkeit: Weil der
Agent nur einen Ausschnitt sieht, sind zwei völlig verschiedene Stellen der
Welt für ihn unter Umständen nicht unterscheidbar. Die naheliegende Abhilfe
wäre, ihm den richtigen Weg vorzurechnen und als zusätzliche Eingabe
mitzugeben, ein \glqq Orakel\grqq{}. Dann löst er die Aufgabe zwar, hat aber
nichts gelernt, was ohne dieses Orakel funktioniert.

Daraus ergibt sich die Forschungsfrage dieser Arbeit:

\begin{quote}
\itshape
Kann ein Reinforcement-Learning-Agent in prozedural generierten, nur teilweise
einsehbaren 2D-Welten eine Navigationsstrategie lernen, die auch auf
unbekannten Welten funktioniert, ohne dass ihm der Weg zum Ziel vorberechnet
wird?
\end{quote}

\noindent
Die Frage ist bewusst offen nach der Architektur gestellt. \emph{Wie} ein
Agent das leisten könnte, ist selbst Teil des Untersuchungsgegenstands: Dass
dafür ein internes Gedächtnis erforderlich ist, war zu Projektbeginn eine
Vermutung und ergab sich erst im Verlauf aus dem Scheitern gedächtnisloser
Varianten (Anhang~\ref{sec:entwicklung}).

\subsection{Zielsetzung und Erfolgskriterien}
\label{sec:ziele}

Die Erfolgskriterien, gegen die in
dieser Arbeit berichtet wird, lauten:

\begin{itemize}
  \item \textbf{Testset A} (Seeds 7000--7049): $\geq 70\,\%$ Erfolgsquote,
  \item \textbf{Holdout B} (Seeds 8000--8049): $\geq 60\,\%$ Erfolgsquote,
  \item pro Konfiguration mindestens 3 Trainingsläufe (Mittelwert $\pm$ Standardabweichung).
\end{itemize}

\noindent
Beide Schwellen wurden vor den Auswertungsläufen festgelegt; ihre Herleitung
aus den Eigenschaften der Aufgabe folgt in Abschnitt~\ref{sec:kriterienrevision}.

\subsection{Aufbau der Arbeit}
\label{sec:aufbau}

Die Arbeit folgt dem üblichen Ablauf empirischer Untersuchungen: erst die
Begriffe, dann der Aufbau des Versuchs, dann die Messungen, zuletzt deren
Bewertung.

\begin{itemize}
  \item \textbf{Kapitel~\ref{sec:grundlagen} (Grundlagen)} erklärt die
        verwendeten Verfahren von Grund auf: was ein
        Markow-Entscheidungsprozess ist, warum ein eingeschränktes Sichtfeld
        die Aufgabe grundlegend verändert, wie die eingesetzten Lernverfahren
        arbeiten und was Gedächtnis, Belohnungsformung und Curriculum
        bedeuten. Wer mit RL vertraut ist, kann es überspringen.
  \item \textbf{Kapitel~\ref{sec:relatedwork} (Verwandte Arbeiten)} ordnet die
        Aufgabe in bestehende Forschungsumgebungen ein und begründet, warum
        ein Zahlenvergleich mit ihnen nicht seriös möglich ist.
  \item \textbf{Kapitel~\ref{sec:umgebung} (Umgebung)} beschreibt
        \emph{Stoneforge} im Detail: was der Agent genau sieht, welche
        Aktionen er hat, wie er belohnt wird und warum jede erzeugte Welt
        nachweislich lösbar ist.
  \item \textbf{Kapitel~\ref{sec:methodik} (Methodik)} legt das Vorgehen offen:
        Lernverfahren und Einstellungen, das stufenweise Training, die strikte
        Trennung der Weltmengen für Training, Auswahl und Messung sowie die
        Begründung der Erfolgsschwellen.
  \item \textbf{Kapitel~\ref{sec:technik} (Technische Umsetzung)} dokumentiert
        die Implementierung: C++-Kern, Python-Anbindung, Trainingspipeline und
        Werkzeuge.
  \item \textbf{Kapitel~\ref{sec:evaluation} (Evaluation)} berichtet die
        Messergebnisse: die Gegenüberstellung mit und ohne Orakel, das
        Endergebnis über sieben Läufe, den Vergleich gegen ungelernte
        Referenzpolitiken und die Analyse des Abstands zwischen den beiden
        Auswertungsarten.
  \item \textbf{Kapitel~\ref{sec:ausblick} (Fazit und Ausblick)} bewertet die
        Ergebnisse gegen die Forschungsfrage, benennt die Grenzen der Arbeit
        und leitet daraus die nächsten Schritte ab.
\end{itemize}

\paragraph{Zur Versionsbezeichnung.}
Im Folgenden tauchen wiederholt Kürzel wie \emph{v11} oder \emph{v12} auf. Sie
bezeichnen Entwicklungsstände des Projekts und sind nötig, weil Ergebnisse
verschiedener Stände \emph{nicht} vergleichbar sind: Ändert sich die
Weltgenerierung, erzeugt derselbe Seed eine andere Welt. Zwei Stände sind für
das Verständnis ausreichend:

\begin{itemize}
  \item \textbf{v11} bezeichnet die am 06.07.2026 überarbeitete Umgebung. Sie
        ist der Stand, auf dem alle berichteten Endergebnisse beruhen.
        Wesentliche Änderungen: Die Exit-Entfernung wird seither über den
        tatsächlichen Laufweg statt über die Luftlinie festgelegt, wirkungslose
        Beobachtungsmerkmale wurden entfernt und mehrere Strafterme entschärft
        (Details in Abschnitt~\ref{sec:v11}).
  \item \textbf{v12} bezeichnet die darauf aufbauende \emph{Trainings}-Konfiguration
        vom 08.07.2026, mit der die sieben finalen Läufe erzeugt wurden. Die
        Umgebung ist dabei unverändert v11; v12 unterscheidet sich nur in den
        Trainingseinstellungen (Tabelle~\ref{tab:basiskonfig}).
\end{itemize}

\noindent
Ältere Stände (v2 bis v10) kommen ausschließlich im Entwicklungsverlauf
(Anhang~\ref{sec:entwicklung}) vor. Wo im Hauptteil ältere Zahlen zitiert
werden, ist der zugehörige Stand jeweils angegeben.

\vspace{0.3cm}
\noindent
Zwei Anhänge ergänzen den Hauptteil: Anhang~\ref{sec:reproduzierbarkeit} führt
alle Angaben zusammen, die zur Reproduktion der Ergebnisse nötig sind;
Anhang~\ref{sec:entwicklung} dokumentiert den chronologischen
Entwicklungsverlauf einschließlich der gescheiterten Versuche und ihrer
Ursachenanalysen. Mehrere zentrale Erkenntnisse der Arbeit, darunter die
Wirkung der Batch-Größe und das Eval-Cap-Artefakt, ergaben sich erst aus dem
Vergleich fehlgeschlagener mit funktionierenden Läufen und sind deshalb dort
nachzulesen.

\section{Grundlagen}
\label{sec:grundlagen}

Zwei Entscheidungen dieser Arbeit lassen sich erst vor dem Hintergrund der
folgenden Begriffe begründen: die Wahl einer rekurrenten statt einer reaktiven
Politik und die Wahl der stochastischen Auswertung als Primärmetrik.
Für eine vollständige Einführung in das Gebiet sei auf Sutton und Barto
\cite{sutton2018rl} verwiesen.

\subsection{Reinforcement Learning und der Markov-Entscheidungsprozess}
\label{sec:mdp}

Reinforcement Learning (RL) beschreibt das Lernen durch Interaktion: Ein Agent
wählt in einem Zustand eine Aktion, erhält eine Belohnung (Reward) und geht in
einen Folgezustand über \cite{sutton2018rl}. Formal wird dies als
Markov-Entscheidungsprozess (MDP) modelliert, ein Tupel $(S, A, P, R, \gamma)$
mit Zustandsmenge $S$, Aktionsmenge $A$, Übergangswahrscheinlichkeit
$P(s'\mid s,a)$, Rewardfunktion $R(s,a)$ und Diskontfaktor $\gamma \in [0,1)$.
Ziel ist eine Politik $\pi(a\mid s)$, die den erwarteten diskontierten Return
$G_t = \sum_{k\geq 0} \gamma^k r_{t+k}$ maximiert. Die Zustandswertfunktion
$V^\pi(s) = \mathbb{E}_\pi[G_t \mid s_t=s]$ gibt den erwarteten Return ab einem
Zustand an. Der Diskontfaktor $\gamma$ gewichtet spätere Rewards geringer; im
Projekt wird $\gamma = 0{,}999$ verwendet.

Abbildung~\ref{fig:mdp_loop} zeigt diesen Kreislauf. Er ist die Grundfigur des
gesamten Verfahrens: Alles, was in dieser Arbeit trainiert wird, ist eine
Variante dieser Schleife, in der die Umgebung \emph{Stoneforge} heißt, die
Aktionen die vier Bewegungsrichtungen sind und der Reward aus
Abschnitt~\ref{sec:umgebung} stammt.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, minimum width=3.4cm, minimum height=1.0cm,
              align=center, font=\small},
  >={Stealth[length=2.4mm]}
]
  \node[box] (agent) at (0,2.4) {\textbf{Agent}};
  \node[box] (env)   at (0,0)   {\textbf{Umgebung}};

  % rechts: Aktion vom Agenten hinunter zur Umgebung
  \draw[->, thick] (agent.east) -- (3.0,2.4) -- (3.0,0) -- (env.east);
  \node[font=\scriptsize, align=center, anchor=west] at (3.15,1.2)
       {Aktion\\[-1pt]$A_t$};

  % links: Zustand und Reward von der Umgebung hinauf zum Agenten
  \draw[->, thick] (env.west) -- (-3.0,0) -- (-3.0,2.4) -- (agent.west);
  \node[font=\scriptsize, align=center, anchor=east] at (-3.15,1.2)
       {Zustand $S_t$\\[-1pt]Reward $R_t$};

  % Zeitschritt-Trennung: gestrichelte Linie quer durch den Rueckkanal
  \draw[dashed, gray!70] (-3.9,1.2) -- (-0.9,1.2);
  \node[font=\scriptsize, gray!70, anchor=north] at (-2.0,1.1)
       {$S_{t+1},\; R_{t+1}$};
\end{tikzpicture}
\caption[Agent-Umwelt-Interaktion im MDP]{Die Agent-Umwelt-Interaktion im
Markov-Entschei\-dungs\-pro\-zess; die gestrichelte Linie markiert den Übergang
zum nächsten Zeitschritt. Eigene Darstellung in Anlehnung an Sutton und Barto
\cite[Abb.~3.1]{sutton2018rl}.}
\label{fig:mdp_loop}
\end{figure}

\subsection{Partielle Beobachtbarkeit (POMDP)}
\label{sec:pomdp}

Sieht der Agent nicht den vollständigen Zustand, sondern nur eine Beobachtung,
liegt ein \emph{Partially Observable} MDP (POMDP) vor \cite{kaelbling1998pomdp}.
Die Markov-Eigenschaft gilt dann für die Beobachtung nicht mehr: Dieselbe lokale
Beobachtung kann zu verschiedenen globalen Zuständen gehören. Eine reaktive
Politik, die nur die aktuelle Beobachtung nutzt, ist deshalb im Allgemeinen
nicht optimal. Abhilfe schafft ein internes Gedächtnis, das vergangene
Beobachtungen zu einem \emph{Belief-State} zusammenfasst
\cite{hausknecht2015drqn}. Abbildung~\ref{fig:mdp_vs_pomdp} stellt beide Fälle
gegenüber: Im MDP greift die Politik direkt auf den Zustand zu, im POMDP
liegt eine Beobachtungsfunktion dazwischen, und die Politik muss aus der
Vorgeschichte erst rekonstruieren, wo sie sich vermutlich befindet.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  b/.style={draw, rounded corners=2pt, minimum height=0.8cm, align=center, font=\small},
  >={Stealth[length=2mm]}, node distance=0.9cm
]
% ---------------- MDP ----------------
\begin{scope}
  \node[b, minimum width=1.5cm] (s1) at (0,0) {Zustand\\$s_t$};
  \node[b, minimum width=1.5cm, right=1.3cm of s1] (pi1) {Politik\\$\pi(a\mid s)$};
  \node[b, minimum width=1.5cm, right=1.3cm of pi1] (a1) {Aktion\\$a_t$};
  \draw[->, thick] (s1) -- (pi1);
  \draw[->, thick] (pi1) -- (a1);
  \node[font=\small\bfseries, above=0.35cm of pi1] {MDP: vollständig beobachtbar};
\end{scope}
% ---------------- POMDP ----------------
\begin{scope}[yshift=-2.7cm]
  \node[b, minimum width=1.5cm] (s2) at (0,0) {Zustand\\$s_t$};
  \node[b, minimum width=1.6cm, right=0.75cm of s2, fill=black!7] (o2)
        {Beobachtung\\$o_t = O(s_t)$};
  \node[b, minimum width=1.7cm, right=0.75cm of o2, fill=black!7] (b2)
        {Belief\\$b_t(h_{1:t})$};
  \node[b, minimum width=1.5cm, right=0.75cm of b2] (pi2) {Politik\\$\pi(a\mid b)$};
  \node[b, minimum width=1.2cm, right=0.75cm of pi2] (a2) {Aktion\\$a_t$};
  \draw[->, thick] (s2) -- (o2);
  \draw[->, thick] (o2) -- (b2);
  \draw[->, thick] (b2) -- (pi2);
  \draw[->, thick] (pi2) -- (a2);
  % Rekurrenz
  \draw[->, thick] (b2.north) .. controls +(0,0.75) and +(0,0.75) .. (b2.north west)
        node[midway, above, font=\scriptsize] {$b_{t-1}$};
  \node[font=\small\bfseries, above=1.05cm of pi2] {POMDP: nur Ausschnitt sichtbar};
  \draw[decorate, decoration={brace, amplitude=4pt, mirror}]
        ($(o2.south west)+(0,-0.15)$) -- ($(b2.south east)+(0,-0.15)$)
        node[midway, below=4pt, font=\scriptsize] {vom Agenten zu leisten};
\end{scope}
\end{tikzpicture}
\caption[MDP gegenüber POMDP]{Vollständige (oben) gegenüber partieller
Beobachtbarkeit (unten). Grau hinterlegt sind die beiden Schritte, die im POMDP
zusätzlich anfallen: die Beobachtung $o_t = O(s_t)$ und die Bildung eines
\emph{Beliefs} aus ihr und der Vorgeschichte. Eigene Darstellung; Formalismus
nach Kaelbling et al.\ \cite{kaelbling1998pomdp}.}
\label{fig:mdp_vs_pomdp}
\end{figure} Eine zweite Konsequenz ist für diese Arbeit
zentral: Unter partieller Beobachtbarkeit kann eine \emph{stochastische}
gedächtnislose Politik jede deterministische gedächtnislose erheblich
übertreffen \cite{singh1994pomdp}. Zu beachten ist die Reichweite dieser
Aussage: Sie gilt für \emph{gedächtnislose} Politiken. Ist der Belief-State
hinreichend genau rekonstruiert, existiert wieder eine optimale
deterministische Politik. Ein Leistungsabstand zwischen Sampling- und
Argmax-Auswertung einer \emph{rekurrenten} Politik ist daher gerade kein
notwendiges Merkmal der Aufgabe, sondern ein Indikator für einen
unvollständigen Belief-State; er wird in der Evaluation als solcher geprüft
(Abschnitt~\ref{sec:evaluation}). Stoneforge ist ein POMDP, da der Agent nur einen
lokalen $15{\times}15$-Ausschnitt sieht (siehe Abschnitt~\ref{sec:umgebung}).
Abbildung~\ref{fig:pomdp_schema} stellt den Unterschied schematisch dar.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.47\textwidth]{figures/fig_game_world.png}
  \hfill
  \includegraphics[width=0.47\textwidth]{figures/fig_ai_observation.png}
  \caption[Partielle Beobachtbarkeit am Beispiel Stoneforge]{Partielle Beobachtbarkeit am Beispiel Stoneforge. Links der vollständige
  Weltzustand ($31{\times}31$-Ausschnitt), rechts die Beobachtung des Agenten:
  nur das rot umrandete $15{\times}15$-Sichtfeld ist bekannt. Der Pfeil zeigt
  die Kompassrichtung zum Exit, nicht den Weg.}
  \label{fig:pomdp_schema}
\end{figure}

\subsection{Wertbasierte Verfahren: Q-Learning und DQN}

Wertbasierte Verfahren lernen die Aktionswertfunktion
$Q^\pi(s,a) = \mathbb{E}_\pi[G_t \mid s_t=s, a_t=a]$ und leiten daraus die
Politik ab (Aktion mit dem höchsten $Q$-Wert). Q-Learning aktualisiert $Q$ über
die Bellman-Gleichung. Deep Q-Networks (DQN) approximieren $Q$ mit einem
neuronalen Netz und stabilisieren das Training über einen Replay-Buffer und ein
Target-Netz \cite{mnih2015dqn}. DQN arbeitet nur mit diskreten Aktionsräumen;
sein früher Einsatz im Projekt und der Wechsel zu PPO sind in
Abschnitt~\ref{sec:methodik} begründet.

\subsection{Policy-Gradient- und Actor-Critic-Verfahren}

Policy-Gradient-Verfahren optimieren die Politik $\pi_\theta$ direkt über den
Gradienten des erwarteten Returns. Actor-Critic-Verfahren kombinieren dies mit
einer gelernten Wertfunktion (Critic), die als Baseline die Varianz der
Gradientenschätzung senkt. Der Vorteil (Advantage)
$A(s,a) = Q(s,a) - V(s)$ misst, wie viel besser eine Aktion gegenüber dem
Durchschnitt ist; die Generalized Advantage Estimation (GAE) schätzt ihn über
mehrere Zeitschritte mit einem Parameter $\lambda$ \cite{schulman2016gae}
(im Projekt $\lambda = 0{,}95$). Advantage Actor-Critic (A2C) ist die synchrone
Variante von A3C \cite{mnih2016a3c}.
Wie gut der Critic gelernt hat, misst die \emph{explained variance} (EV): der
Anteil der Varianz der tatsächlichen Returns, den die Wertschätzung erklärt.
EV~$\approx$~1 bedeutet eine verlässliche Wertfunktion, EV~$\approx$~0, dass
der Critic praktisch nichts gelernt hat. Diese Größe dient in der Arbeit als
zentrale Diagnosemetrik des Trainings.

\subsection{Proximal Policy Optimization (PPO)}

PPO ist das im Projekt eingesetzte Hauptverfahren \cite{schulman2017ppo}. Es ist
ein Actor-Critic-Verfahren, das die Politik pro Update nur begrenzt verändert, um
Instabilität durch zu große Schritte zu vermeiden. Dazu wird das
Wahrscheinlichkeitsverhältnis
$r_t(\theta) = \pi_\theta(a_t\mid s_t)/\pi_{\theta_\text{alt}}(a_t\mid s_t)$
in der Zielfunktion abgeschnitten (\emph{clipping}):
\[
L^{\text{CLIP}}(\theta) = \mathbb{E}_t\!\left[\min\!\big(r_t(\theta)\,A_t,\;
\operatorname{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon)\,A_t\big)\right].
\]
Abbildung~\ref{fig:ppo_clip} veranschaulicht die Wirkung des Clippings an einem
einzelnen Zeitschritt. War die Aktion besser als erwartet ($A_t > 0$), lohnt es
sich, ihre Wahrscheinlichkeit zu erhöhen, aber nur bis zum Faktor $1+\epsilon$;
darüber hinaus verläuft die Zielfunktion flach, der Anreiz für einen größeren
Schritt entfällt. War sie schlechter als erwartet ($A_t < 0$), gilt dasselbe
spiegelbildlich bei $1-\epsilon$. Genau das verhindert die zu großen
Aktualisierungen, an denen einfache Policy-Gradient-Verfahren scheitern.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.0, >={Stealth[length=1.8mm]}, font=\small]
% ---------------- A > 0 ----------------
\begin{scope}
  \draw[->] (-0.15,0) -- (2.75,0) node[right, font=\scriptsize] {$r$};
  \draw[->] (0,-0.25) -- (0,1.95) node[above, font=\scriptsize] {$L^{\text{CLIP}}$};
  % linear bis 1+eps, dann flach
  \draw[very thick, blue!70!black] (0,0) -- (1.7,1.7) -- (2.6,1.7);
  \draw[dotted, gray] (1.0,0) -- (1.0,1.0);
  \draw[dotted, gray] (1.7,0) -- (1.7,1.7);
  \node[font=\scriptsize] at (1.0,-0.28) {$1$};
  \node[font=\scriptsize] at (1.78,-0.28) {$1+\epsilon$};
  \fill[red] (1.0,1.0) circle (2pt);
  \node[font=\small] at (1.3,1.85) {$A_t > 0$};
\end{scope}
% ---------------- A < 0 ----------------
\begin{scope}[xshift=5.2cm]
  \draw[->] (-0.15,0) -- (2.75,0) node[right, font=\scriptsize] {$r$};
  \draw[->] (0,-1.95) -- (0,0.35) node[above, font=\scriptsize] {$L^{\text{CLIP}}$};
  % flach bis 1-eps, dann linear fallend
  \draw[very thick, blue!70!black] (0,-0.6) -- (0.6,-0.6) -- (1.85,-1.85);
  \draw[dotted, gray] (1.0,0) -- (1.0,-1.0);
  \draw[dotted, gray] (0.6,0) -- (0.6,-0.6);
  \node[font=\scriptsize] at (0.52,0.22) {$1-\epsilon$};
  \node[font=\scriptsize] at (1.05,0.22) {$1$};
  \fill[red] (1.0,-1.0) circle (2pt);
  \node[font=\small] at (2.0,-0.35) {$A_t < 0$};
\end{scope}
\end{tikzpicture}
\caption[Wirkung des PPO-Clippings]{Ein einzelner Term der abgeschnittenen
Zielfunktion $L^{\text{CLIP}}$ über dem Wahrscheinlichkeitsverhältnis
$r_t(\theta)$, links für positiven, rechts für negativen Advantage. Der rote
Punkt markiert den Startpunkt der Optimierung ($r = 1$). Eigene Darstellung in
Anlehnung an Schulman et al.\ \cite[Abb.~1]{schulman2017ppo}.}
\label{fig:ppo_clip}
\end{figure}

Zusätzlich wird ein Entropie-Term addiert, der Exploration fördert; sein Gewicht
(\texttt{ent\_coef}) steuert, wie stochastisch die Politik bleibt. Trainiert
wird in Zyklen: Erst sammeln die parallelen Umgebungen einen Rollout von
$n_\text{steps}$ Schritten, dann wird dieser in Minibatches der Größe
\texttt{batch\_size} über $n_\text{epochs}$ Durchläufe optimiert. Verwendet wird
die Implementierung aus Stable-Baselines3 \cite{raffin2021stable}.

Eine gelernte stochastische Politik lässt sich auf zwei Arten ausführen:
\emph{stochastisch} (die Aktion wird aus der Verteilung $\pi(a\mid s)$ gezogen,
Sampling) oder \emph{deterministisch} (stets die wahrscheinlichste Aktion,
Argmax). Dasselbe trainierte Modell kann in beiden Betriebsarten
unterschiedlich gut abschneiden. Der Abstand zwischen beiden Messwerten wird in
dieser Arbeit durchgängig als \textbf{Det/Stoch-Gap} bezeichnet; er ist einer
ihrer zentralen Untersuchungsgegenstände, weil er sich als groß und
hartnäckig erwiesen hat (Abschnitt~\ref{sec:gapexperimente}). Anschaulich
gesprochen: Der Agent löst die Aufgabe zuverlässiger, wenn er würfelt, als
wenn er stets seine eigene beste Aktion wählt. Dass dies unter partieller
Beobachtbarkeit möglich ist, folgt aus Abschnitt~\ref{sec:pomdp}; dass es bei
einer Politik \emph{mit} Gedächtnis dennoch erklärungsbedürftig bleibt,
ebenfalls.

\subsection{Rekurrente Politiken (LSTM)}
\label{sec:lstm}

Alle bisher genannten Verfahren lassen sich mit einem gewöhnlichen
vorwärtsgerichteten neuronalen Netz umsetzen, einem \emph{Multilayer Perceptron}
(MLP). Ein solches Netz bildet jede Beobachtung unabhängig auf eine Aktion ab
und besitzt kein Gedächtnis: Es kann nicht unterscheiden, ob es eine Stelle
zum ersten oder zum zehnten Mal sieht. Unter partieller Beobachtbarkeit ist das
eine wesentliche Einschränkung (Abschnitt~\ref{sec:pomdp}); die Bezeichnung
\emph{MLP} steht im Folgenden durchgängig für diese gedächtnislose Variante.

Um in einem POMDP ein Gedächtnis bereitzustellen, wird die Politik daher um ein
rekurrentes Netz erweitert. Long Short-Term Memory (LSTM) ist eine
rekurrente Architektur, die Information über viele Zeitschritte hinweg speichern
kann \cite{hochreiter1997lstm}. Der LSTM-Zustand approximiert den Belief-State
und fasst die bisher gesehenen Beobachtungen zusammen. Die Kombination aus
rekurrentem Netz und wertbasiertem RL wurde als DRQN eingeführt
\cite{hausknecht2015drqn}; im Projekt wird die policy-gradient-Variante
RecurrentPPO (PPO mit LSTM-Politik) aus \emph{sb3-contrib} verwendet, mit einer
LSTM-Größe von 256.

Wie das Gedächtnis wirkt, zeigt Abbildung~\ref{fig:lstm_unrolled} in der über
die Zeit \glqq entrollten\grqq{} Darstellung: Der verborgene Zustand $h_t$ wird
von Schritt zu Schritt weitergereicht und sammelt so Information, die in der
einzelnen Beobachtung nicht enthalten ist. Für Stoneforge ist das genau die
Information \glqq hier war ich schon\grqq{} bzw.\ \glqq diese Wand bin ich
bereits nach Norden abgelaufen\grqq.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  cell/.style={draw, rounded corners=2pt, minimum width=1.15cm, minimum height=0.8cm,
               font=\small, fill=black!7},
  io/.style={font=\scriptsize, align=center},
  >={Stealth[length=2mm]}, node distance=1.5cm
]
  \foreach \i/\lbl in {1/{t{-}1}, 2/{t}, 3/{t{+}1}} {
    \node[cell] (c\i) at (2.3*\i, 0) {LSTM};
    \node[io, below=0.7cm of c\i] (o\i) {Beobachtung\\$o_{\lbl}$};
    \node[io, above=0.7cm of c\i] (a\i) {Aktion\\$a_{\lbl}$};
    \draw[->] (o\i) -- (c\i);
    \draw[->] (c\i) -- (a\i);
  }
  \draw[->, thick] (c1) -- node[above, font=\scriptsize] {$h_{t-1}$} (c2);
  \draw[->, thick] (c2) -- node[above, font=\scriptsize] {$h_{t}$} (c3);
  \draw[->, thick, dashed] (1.35,0) -- (c1);
  \draw[->, thick, dashed] (c3) -- (8.6,0);
  \node[font=\scriptsize, anchor=south] at (8.1,0.12) {$h_{t+1}$};
\end{tikzpicture}
\caption[Rekurrente Politik über die Zeit]{Über die Zeit entrollte rekurrente
Politik: Neben der aktuellen Beobachtung $o_t$ erhält jeder Zeitschritt den
verborgenen Zustand $h_{t-1}$ des Vorschritts. Eigene Darstellung; rekurrente
Politiken im RL nach Hausknecht und Stone \cite{hausknecht2015drqn}.}
\label{fig:lstm_unrolled}
\end{figure}

\subsection{Reward Shaping (PBRS)}

Zusätzliche Belohnungen können das Lernen beschleunigen, dürfen die optimale
Politik aber nicht verändern. Potential-Based Reward Shaping (PBRS) erfüllt dies:
Der Zusatzreward hat die Form $F(s,s') = \gamma\,\Phi(s') - \Phi(s)$ mit einer
Potentialfunktion $\Phi$. Ng et al.\ zeigen, dass eine solche Form
policy-invariant ist, die optimale Politik also erhalten bleibt
\cite{ng1999policy}. Im Projekt ist $\Phi$ an die BFS-Distanz zum Exit
gekoppelt: Die Breitensuche (BFS) liefert die Länge des kürzesten begehbaren
Wegs vom Agenten zum Exit in Schritten. Im Gegensatz zur Luftlinie
berücksichtigt sie Wände und Umwege.

\subsection{Curriculum Learning}

Curriculum Learning bezeichnet das Training mit ansteigender
Aufgabenschwierigkeit statt einer zufälligen Reihenfolge \cite{bengio2009curriculum}.
Im Projekt wird die Exit-Distanz stufenweise erhöht; der Übergang zwischen den
Stufen erfolgt leistungsbasiert (eine Stufe gilt ab einer Ziel-Erfolgsquote als
bestanden).

\subsection{Prozedurale Generierung und Generalisierung}

Wird für jedes Level eine neue, zufällig erzeugte Welt verwendet, kann der Agent
einzelne Karten nicht auswendig lernen und muss allgemeine Strategien bilden.
Die Erzeugung ist über einen \emph{Seed} steuerbar, den Startwert des
Zufallszahlengenerators: Derselbe Seed erzeugt reproduzierbar dieselbe Welt,
verschiedene Seeds verschiedene Welten.
Prozedurale Generierung ist daher ein etabliertes Mittel, um Generalisierung in
RL zu prüfen und Overfitting auf einzelne Level zu vermeiden
\cite{cobbe2020procgen, risi2020increasing}. Die Evaluation auf
trainingsfremden Seeds (Testset~A und Holdout~B) folgt diesem Prinzip.

\section{Einordnung in verwandte Arbeiten}
\label{sec:relatedwork}

Stoneforge liegt strukturell zwischen drei etablierten Benchmarks: MiniGrid
(teilbeobachtbare Grid-Navigation, PPO+LSTM als Standard-Baseline)
\cite{chevalier2023minigrid}, Crafter (prozedural generiertes 2D-Survival-Spiel
mit Mining und Crafting) \cite{hafner2021crafter} und Procgen (Generalisierung
über prozedurale Level) \cite{cobbe2020procgen}. Prozedurale Generierung als
Mittel, Generalisierung zu erzwingen, hat darüber hinaus eine eigene
Forschungstradition \cite{togelius2011search, risi2020increasing}.

\paragraph{Gedächtnis in teilbeobachtbaren Umgebungen.}
Die Standardantwort auf partielle Beobachtbarkeit ist eine rekurrente Politik:
DRQN erweiterte DQN um ein LSTM \cite{hausknecht2015drqn}; R2D2 zeigte, dass
die korrekte Behandlung des rekurrenten Zustands (gespeicherte States im
Replay, Burn-in) die Leistung bei ansonsten unveränderter Architektur
erheblich beeinflusst \cite{kapturowski2019r2d2}. Dieser Befund spiegelt
sich in dieser Arbeit auf der Eval-Seite wider (korrekt geführter LSTM-State,
Abschnitt~\ref{sec:methodik}). Für on-policy-Verfahren untersuchen Pleines
et al.\ Implementierungsdetails rekurrenter PPO-Politiken und stellen
Umgebungen vor, deren Gedächtnisanforderungen über reine Kapazität
hinausgehen \cite{pleines2022recurrent}. Der POPGym-Benchmark vergleicht
13 Gedächtnis-Architekturen systematisch; dort schneidet das einfache GRU
insgesamt am besten ab; aufwändigere Gedächtnismodelle sind nicht
automatisch besser \cite{morad2023popgym}. Der hier verwendete
RecurrentPPO-Ansatz entspricht dieser Standard-Baseline, und der Befund der
eigenen E3-Ablation (LSTM~512 schlägt 256 nicht,
Abschnitt~\ref{sec:gapexperimente}) fügt sich in dieses Bild. Strukturierte
Alternativen wie das In-Context-Verfahren AMAGO \cite{grigsby2024amago}
werden im Ausblick diskutiert.

\paragraph{Curriculum und Exploration.}
Curriculum Learning geht auf Bengio et al.\ zurück
\cite{bengio2009curriculum}; das hier verwendete leistungsbasierte Gating ist
eine einfache Instanz, Prioritized Level Replay (PLR) \cite{jiang2021plr} die
naheliegende Weiterentwicklung auf Level-Ebene. PLR wiederholt gezielt jene
Level, an denen der Agent noch am meisten lernen kann, statt sie
gleichverteilt zu ziehen. Das Training dieser Arbeit enthält eine stark
vereinfachte Variante dieses Gedankens: einen \emph{Seed-Pool}, der einen Teil
der Episodenstarts durch bereits gelöste Welten ersetzt
(Abschnitt~\ref{sec:technik}). Wie in Abschnitt~\ref{sec:etappe5} diskutiert,
kehrt diese Variante die Auswahlrichtung von PLR gerade um und ist deshalb
kritisch zu sehen. Der Explorations-Bonus für neu betretene Tiles ist eine zählbasierte
Form intrinsischer Motivation; kuriositätsgetriebene Verfahren wie ICM
\cite{pathak2017icm} und RND \cite{burda2018rnd} verallgemeinern dieses
Prinzip auf Beobachtungsräume, in denen Neuheit nicht direkt abzählbar ist.
Dies ist hier nicht erforderlich, da die Tile-Position Neuheit unmittelbar definiert.

\paragraph{Zur Vergleichbarkeit der absoluten Zahlen.}
Ein direkter Vergleich der hier erreichten Erfolgsquote mit publizierten Werten
dieser Benchmarks ist \emph{nicht} seriös möglich, weil sie unterschiedliche
Metriken berichten: Procgen aggregiert über den \emph{normalisierten Return}
$(R - R_{\min})/(R_{\max} - R_{\min})$ und nicht über eine Erfolgsquote
\cite{cobbe2020procgen}; Crafter bewertet erreichte \emph{Achievements}
\cite{hafner2021crafter}; für MiniGrid-Aufgaben berichten Folgearbeiten
aufgabenspezifische Erfolgsquoten, die je nach Aufgabe bis 100\,\% reichen. Hinzu
kommen Unterschiede in Sichtradius, Episodenlänge und Zielabstand. Eine Aussage
der Form \glqq unser Wert liegt über/unter dem Stand der Technik\grqq{} wäre
daher eine Scheinpräzision und wird hier bewusst nicht getroffen.

Belastbar ist der Vergleich nur \emph{innerhalb} dieser Arbeit: gegen die eigene
Gegenüberstellung (Tabelle~\ref{tab:ablation}), gegen die Gap-Experimente
(Abschnitt~\ref{sec:gapexperimente}) und gegen die eigenen, vorab fixierten
Kriterien (Abschnitt~\ref{sec:kriterienrevision}). Alle Vergleichsbedingungen
laufen dort auf demselben Generator, mit derselben Metrik und demselben
Eval-Protokoll.

\section{Die Umgebung Stoneforge}
\label{sec:umgebung}

Stoneforge ist ein selbst entwickeltes, rundenbasiertes 2D-Spiel (C++-Kern,
Python-Anbindung über pybind11). Die Welt wird pro Seed deterministisch aus
Hash-Noise generiert (7 Biome, Chunks à $16{\times}16$ Tiles, theoretisch
unendlich); der Agent startet bei $(0,0)$ und muss den Exit erreichen.
Abbildung~\ref{fig:player_client} zeigt die Benutzeroberfläche und die Spielwelt aus Sicht eines menschlichen Spielers.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.72\textwidth]{figures/fig_game_real.png}
  \caption[Benutzeroberfläche des spielbaren C++-Clients]{Benutzeroberfläche des spielbaren C++-Clients (Stoneforge 2D, echter Screenshot): Gezeigt ist die Spielumgebung aus Sichtweise eines menschlichen Spielers mit vollem Sichtbereich, dem Status-Header (Lebenspunkte, Energie, Schritte) und der Spielfigur im Zentrum.}
  \label{fig:player_client}
\end{figure}

\subsection{POMDP-Charakter}

Der Agent sieht nur einen lokalen $15{\times}15$-Ausschnitt der Welt sowie einen
\emph{Luftlinien}-Kompass zum Exit, nicht den Weg. Wände, Sackgassen und
Umwege außerhalb des Sichtfensters sind unbekannt: Die Aufgabe ist ein
\emph{Partially Observable Markov Decision Process} (POMDP,
Abschnitt~\ref{sec:pomdp}). Beide Perspektiven stellt
Abbildung~\ref{fig:pomdp_schema} auf Seite~\pageref{fig:pomdp_schema}
gegenüber: links die Welt, wie sie tatsächlich aussieht, rechts das, was der
Agent davon sieht. Für eine gedächtnislose Politik sind dabei verschiedene
Weltpositionen ununterscheidbar (Abschnitt~\ref{sec:ablation}).

\subsection{Beobachtung, Aktionen, Episodenende}

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llr@{}}
\toprule
\textbf{Bereich} & \textbf{Inhalt} & \textbf{Dim.} \\
\midrule
Lokales Grid & $15{\times}15$ Tile-Typen um den Agenten (Sichtradius 7) & 225 \\
HP & Lebenspunkte (normiert) & 1 \\
Kompass & exitDx, exitDy (Luftlinie, normiert) & 2 \\
Episodenfortschritt & Schritt / 4000 & 1 \\
\midrule
\textbf{Gesamt (Env v11)} & & \textbf{229} \\
\bottomrule
\end{tabular}
\caption[Aufbau der Observation (Env v11)]{Observation der Umgebungsversion v11. Die früheren Features
\emph{Energie} und \emph{Inventar} waren im RL-Setup konstant (tote Features)
und wurden am 06.07.2026 entfernt (vorher 231 Dimensionen).}
\label{tab:obs}
\end{table}

Der Aktionsraum ist \texttt{Discrete(4)}: hoch, runter, links, rechts. Mining,
Bauen und Kampf existieren nur im spielbaren Client und sind aus dem RL-Pfad
vollständig entfernt. Eine Episode endet bei Erreichen des Exits (Sieg), nach
4000 Schritten (Timeout) oder als Truncation nach 256 Schritten ohne positiven
Reward (Early-Stop).

\subsection{Reward-Design}

Das Reward-Design folgt dem Prinzip: \emph{sparse Primärsignal + theoretisch
sauberes dichtes Shaping}. Kernstück ist Potential-Based Reward Shaping (PBRS)
auf der BFS-Pfaddistanz zum Exit:
\begin{equation}
F(s, s') \;=\; \gamma\,\Phi(s') - \Phi(s), \qquad
\Phi(s) \;=\; -\,\frac{d_{\mathrm{BFS}}(s)}{128},
\end{equation}
mit $\gamma = 0.999$ (identisch zum RL-Discount) und Gewicht $\beta = 2.5$.
Ein Tile echter Fortschritt liefert damit rund $+0{,}02$; abzüglich des
Schritt-Malus von $-0{,}01$ (Tabelle~\ref{tab:reward}) verbleiben netto etwa
$+0{,}01$. PBRS ist beweisbar
\emph{policy-invariant}; es beschleunigt die Konvergenz, ohne die optimale
Politik zu verändern \cite{ng1999policy}. Entscheidend ist, dass das Potential
die \emph{echte} Pfaddistanz (BFS durch Wände gerechnet) verwendet, nicht die
Luftlinie: Ein Luftlinien-Potential würde den Agenten systematisch in Sackgassen
führen.

Diese Garantie gilt allerdings nur für den PBRS-Term selbst. Die übrigen
Komponenten in Tabelle~\ref{tab:reward} (Explorations-Bonus, Schritt-, Loop-,
Idle- und Schadens-Penalty) sind gewöhnliche Reward-Terme und verändern die
optimale Politik prinzipiell mit. Der frühere eskalierende Wand-Penalty
($-0.05$/$-0.25$) wurde aus genau diesem Grund entfernt: Die Summe vieler
kleiner Strafen machte Erkunden in engen Korridoren unwirtschaftlicher als
Stillstand.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lr@{}}
\toprule
\textbf{Komponente} & \textbf{Wert} \\
\midrule
Exit erreicht (terminal) & $+100$ \\
Timeout / Tod (terminal) & $-10$ \\
PBRS (BFS-Distanz, pro Schritt) & $\approx \pm 0.02$ \\
Explorations-Bonus (neue Tile) & $+0.02$ \\
Schritt-Penalty & $-0.01$ \\
2-Schritt-Loop-Penalty & $-0.05$ \\
Idle-Penalty (Aktion ohne Positionswechsel) & $-0.04$ \\
Schadens-Penalty (pro verlorenem HP) & $-0.5$ \\
\bottomrule
\end{tabular}
\caption[Reward-Komponenten (Env v11)]{Vollständige Reward-Komponenten der
Umgebungsversion v11 gemäß \texttt{Simulation::computeReward()}.}
\label{tab:reward}
\end{table}

\subsection{Garantierte Lösbarkeit}

Die Exit-Platzierung wählt Kandidaten per Flood-Fill vom Spawn aus
ausschließlich über \emph{begehbare} Tiles; jeder mögliche Exit ist damit per
Konstruktion erreichbar. Empirisch bestätigt: 3\,150 geprüfte Seeds (inkl.\
aller Eval-Sets und Trainingsbereiche) sowie eine erneute Messung am 05.07.2026
(150/150) ergaben \textbf{100\,\% Lösbarkeit}. Ein BFS-Oracle-Agent erreicht den
Exit auf allen Test-Seeds in exakt der BFS-Distanz und bestätigt damit die
gesamte Kette aus Weltgenerierung, Bewegungsmechanik und Reward-Signal.

\section{Methodik}
\label{sec:methodik}

\subsection{Algorithmus}

Trainiert wird RecurrentPPO (PPO \cite{schulman2017ppo} mit LSTM-Politik
\cite{hochreiter1997lstm}; beide Verfahren in Abschnitt~\ref{sec:grundlagen})
aus \emph{Stable-Baselines3 / sb3-contrib} \cite{raffin2021stable}. Zentrale Hyperparameter: $n_\text{steps}=256$,
$n_\text{epochs}=10$, Lernrate $3\cdot10^{-4}$, $\gamma=0.999$,
\texttt{ent\_coef}=0.05, LSTM mit 256 Hidden Units (separater Critic-LSTM),
16 parallele Umgebungen.

\paragraph{Algorithmenwahl.}
Die Trainings-Infrastruktur unterstützt neben PPO auch DQN und A2C. Ein
früher DQN-Anlauf (Mai 2026) wurde nach Diagnose verworfen: Die Q-Werte
kollabierten auf ein nahezu aktionsunabhängiges Niveau, und der Replay-Buffer
konservierte die Folgen eines später behobenen Reward-Fehlers über das
weitere Training. Ein on-policy-Verfahren ist gegenüber solchen Fehlerfolgen
strukturell robuster, weshalb auf PPO gewechselt wurde (Anhang,
Etappe~1). Nach der Umstellung auf die rekurrente Politik wurde kein
wertbasiertes Verfahren erneut vermessen; eine quantifizierte DQN/A2C-Baseline
auf der finalen Umgebung v11 steht damit aus und wird als Grenze der Arbeit
ausgewiesen (Abschnitt~\ref{sec:ausblick}).

\subsection{Curriculum}

Das Training verläuft leistungsbasiert in vier Phasen steigender Exit-Distanz;
eine Phase endet, wenn die Ziel-Erfolgsquote in zwei aufeinanderfolgenden
Evaluationen erreicht wird.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lllll@{}}
\toprule
\textbf{Phase} & \textbf{Exit-Distanz} & \textbf{Ziel-SR} & \textbf{Gating-Metrik} & \textbf{ent\_coef} \\
\midrule
1 & 5--12 & 85\,\% & stochastisch & 0.05 \\
2 & 12--25 & 70\,\% & stochastisch & 0.05 \\
3 & 25--45 (Eval 35--45) & 70\,\% & deterministisch & 0.05 $\to$ 0.001 (Annealing) \\
4 & 25--45 (Greedy Fine-Tune) & 60\,\% & deterministisch & 0.0001 \\
\bottomrule
\end{tabular}
\caption[Curriculum in vier Phasen]{Curriculum (Stand v11). Phase 1/2 gaten auf der stochastischen SR, da
die Politik dort mit \texttt{ent\_coef}=0.05 absichtlich nahe-uniform ist und
die deterministische SR konstruktionsbedingt kein Signal trägt.}
\label{tab:curriculum}
\end{table}

Das Distanz-Curriculum staffelt dabei implizit auch die
\emph{Beobachtbarkeit} des Ziels, und damit die Fähigkeit, die je Phase
gelernt werden muss. Eine Messung über 200 Welten pro Phase (07.07.2026):
In Phase~1 liegt der Exit beim Episodenstart in \textbf{72\,\%} der Welten
bereits im $15\times15$-Sichtfeld (zu lernen: \glqq geh zum sichtbaren Ziel,
weiche lokalen Hindernissen aus\grqq); in Phase~2 nur noch in \textbf{6\,\%}
(zu lernen: dem Kompass folgen, Umwege um Hindernisse); in Phase~3 in
\textbf{0\,\%} (reine Kompass-Navigation über lange Distanz, inklusive
Sackgassen, die nur mit Gedächtnis überwindbar sind). Ein zusätzliches
Curriculum über die Sichtfeldgröße (in der Literatur als Übergang von
voller zu partieller Beobachtbarkeit beschrieben) wäre daher redundant;
zudem wäre es hier riskant, weil ein groß\-es Sichtfeld anfangs eine
\emph{reaktive} Strategie ohne Gedächtnisnutzung belohnt, die beim
Verkleinern des Fensters wertlos wird (dieselbe Nicht-Übertragbarkeit, die
die Gegenüberstellung in Abschnitt~\ref{sec:ablation} für das BFS-Orakel
zeigt).

\subsection{Iterative Modellverbesserung}
\label{sec:iteration}

Die finale Konfiguration ist nicht das Ergebnis einer einmaligen Wahl, sondern
systematischer Fehlersuche über zwölf Umgebungs- bzw.\ Trainingsiterationen;
der vollständige Verlauf inklusive aller Fehlversuche ist im Anhang
dokumentiert (Abschnitt~\ref{sec:entwicklung}). Drei Befunde waren
entscheidungsprägend:

\begin{enumerate}
  \item \textbf{Hyperparameter dominieren Features.} Eine Serie von
        Feature-Erweiterungen (v6--v9) scheiterte vollständig (SR
        0--12\,\%), und zwar nicht an den Features, sondern an nebenbei
        verstellten Basis-Hyperparametern; dominanter Faktor war
        \texttt{ent\_coef} ($0{,}05 \to 0{,}01$: zu wenig Exploration, der
        Exit wird nie gefunden). Aufgeklärt durch Reverse-Engineering des
        letzten funktionierenden Modells (Tabelle~\ref{tab:rootcause}).
  \item \textbf{Einzel-Snapshots validieren nicht.} Die Batch-Größe 64 war
        zunächst durch einen einzelnen Messpunkt \glqq validiert\grqq;
        die vollständige Eval-Kurve zeigte jedoch eine starke Oszillation ohne
        Konvergenz (Critic-\texttt{explained\_variance} $\approx 0{,}1$).
        Mit \texttt{batch\_size}=8 ($\approx 8\times$ mehr Gradientenschritte
        pro Rollout) stieg die EV über $0{,}85$ bei stabilem Lernen
        (Abschnitt~\ref{sec:batchsize}). Hyperparameter-Entscheidungen
        werden seitdem nur auf ganzen Eval-Kurven getroffen.
  \item \textbf{Das Eval-Protokoll ist Teil des Experiments.} Ein
        Episoden-Cap von 600 statt 4\,000 maß dasselbe Modell mit 48\,\%
        statt 86\,\% SR und machte ein Phasenziel unerreichbar
        (Abschnitt~\ref{sec:etappe6}); ebenso
        verzerrte ein Data-Leakage über den Eval-Callback (Modellauswahl auf
        Test-Seeds) die Selektion, bis am 11.06.\ das separate
        Validierungsset eingeführt wurde (Anhang, Etappe~2).
\end{enumerate}

\noindent
Voraussetzung dieser Analysen war die lückenlose Protokollierung aller
Experimente im Changelog; ohne sie wäre insbesondere das
Reverse-Engineering in Befund 1 nicht möglich gewesen.

\subsection{Evaluationsprotokoll}

Zentrale Metrik ist die \emph{Erfolgsquote} (\emph{Success Rate}, SR): der
Anteil von 50 festen,
trainingsfremden Welten, in denen der Agent den Exit innerhalb des Episodenlimits
(4\,000 Schritte) erreicht. Ein zu kurzes Limit verfälscht die SR erheblich
(dasselbe Modell: 48\,\% bei Limit 600 gegenüber 86\,\% bei 4\,000;
Abschnitt~\ref{sec:etappe6}), daher wird stets mit dem vollen Limit gemessen.

Strikte Seed-Trennung: Validierung (6000--6049) steuert Phasenwechsel und
Modellauswahl; Testset A (7000--7049) und Holdout B (8000--8049) werden
ausschließlich final evaluiert. Ein früherer Data-Leakage-Fehler (Modellauswahl
auf den Test-Seeds) wurde identifiziert und behoben. Jede Evaluation misst seit
v11 \emph{beide} Betriebsarten: stochastisch (Sampling aus der Politik) und
deterministisch (Argmax). Bei der rekurrenten Politik werden LSTM-Zustand und
Episoden-Start-Signal an jeden \texttt{predict()}-Aufruf durchgereicht und zu
Episodenbeginn zurückgesetzt, damit kein Zustand über Episodengrenzen
übertragen wird; dies ist ein häufiger, leicht übersehener Fehler beim
Evaluieren rekurrenter Politiken.
Für das Endergebnis werden sieben unabhängige Läufe gemittelt
(Mittelwert $\pm$ Stichproben-Standardabweichung, $\mathrm{ddof}=1$, zusätzlich
95\,\%-Konfidenzintervalle);
als Trainings-Diagnosemetrik dient die \texttt{explained\_variance} des Critics.
Ergebnisse in Abschnitt~\ref{sec:evaluation}.

\subsection{Herleitung der Erfolgsschwellen}
\label{sec:kriterienrevision}

Die folgende Herleitung setzt Kenntnis der Umgebung voraus und steht deshalb
hier und nicht bei der Nennung der Kriterien in
Abschnitt~\ref{sec:ziele}.

Die dort genannten Schwellen (70\,\% auf Testset~A,
60\,\% auf Holdout~B) ersetzen die im Exposé angesetzte Metrik von
$\geq 90\,\%$ über 100 unbekannte Seeds. Drei Eigenschaften der Aufgabe, die zu
Projektbeginn noch nicht bekannt waren, machen einen Wert in dieser Höhe
unerreichbar:

\begin{enumerate}
  \item \textbf{Der POMDP-Charakter der Umgebung.} Bei einem Sichtradius von 7
        und nicht sichtbarem Ziel ist die Aufgabe partiell beobachtbar
        (Abschnitt~\ref{sec:pomdp}). Eine deterministische Politik über der
        \emph{Beobachtung} ist damit nicht mehr garantiert optimal
        \cite{singh1994pomdp}, hinzu kommt die epistemische Unsicherheit über
        den Seed \cite{ghosh2021epistemic}. Eine Schwelle von 90\,\%
        unterstellt dagegen eine nahezu vollständig lösbare Aufgabe.
  \item \textbf{Die tatsächliche Schwierigkeit der Distanzvorgabe.} Bis zur
        Umgebungsrevision v11 wurde die Exit-Distanz als Luftlinie gemessen;
        eine Vorgabe von \glqq 35--45\grqq{} entsprach real einem Laufweg von
        42--75 Feldern (Abschnitt~\ref{sec:v11}).
  \item \textbf{Die Streuung über Trainingsläufe.} Sie beträgt rund $\pm 12$
        Prozentpunkte (Tabelle~\ref{tab:v12}). Eine Schwelle von 90\,\% liegt
        weit außerhalb dessen, was das Verfahren hier stabil liefert.
\end{enumerate}

\noindent
Die Schwellen 70\,\%/60\,\% sind seit dem 16.05.2026 im Experiment-Changelog
festgehalten, also lange vor den finalen Läufen (08.07.) und der
$n=7$-Auswertung (17.07.); sie wurden nicht in Kenntnis der hier berichteten
Ergebnisse gewählt und sind damit kein Fall von \emph{HARKing}
(\emph{Hypothesizing After the Results are Known} \cite{kerr1998harking}).
Dass das Testset-Kriterium am Ende
dennoch verfehlt wird, zeigt, dass auch die revidierte Schwelle keine bequeme
war. Gegen die ursprüngliche Exposé-Metrik gemessen ergeben Testset~A und
Holdout~B zusammen die geforderten 100 unbekannten Seeds und einen Mittelwert
von $66{,}3\,\% \pm 12{,}1$ (stochastisch); die 90\,\%-Schwelle wird deutlich
verfehlt. Das Exposé nennt als zweite Zielgröße die Minimierung der
\emph{Steps per Episode}. Auch sie wird verfehlt, und zwar deutlicher als die
Erfolgsquote: Die in Abschnitt~\ref{sec:baselines} berichtete Pfadeffizienz von
$\eta \approx 0{,}05$ entspricht rund dem Zwanzigfachen des kürzesten Weges.

\subsection{Versuchsübersicht: Setup je Iteration}
\label{sec:versuchsuebersicht}

Die folgenden Angaben zum Setup jeder systematischen Iteration folgen den
Reproduzierbarkeits-Checklisten von Pineau et al.\ \cite{pineau2021repro} bzw.\
Henderson et al.\ \cite{henderson2018matters}.
Alle Läufe teilen die in
Tabelle~\ref{tab:basiskonfig} gelistete Basiskonfiguration; die Iterationen in
Tabelle~\ref{tab:iterationen} verändern jeweils einen \emph{inhaltlichen} Faktor
gegenüber der v12-Baseline. Sie sind allerdings weder vollständig kontrolliert
(E1/E2 laufen zusätzlich mit reduziertem Phase-3-Budget) noch repliziert
($n=1$ je Arm); die daraus ableitbaren Schlüsse sind entsprechend schwach und
werden in Abschnitt~\ref{sec:gapexperimente} eingegrenzt. Die vollständige Historie
inklusive Zwischenständen liegt im Experiment-Changelog (\texttt{CHANGELOG.md});
je Lauf existiert eine \texttt{config.json} mit den hier tabellierten Werten.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Komponente} & \textbf{Wert} \\
\midrule
Algorithmus & RecurrentPPO (SB3-contrib), \texttt{MlpLstmPolicy} \\
$n_\text{steps}$ / \texttt{batch\_size} / $n_\text{epochs}$ & 256 / 8 / 10 \\
Lernrate / $\gamma$ / $\lambda_\text{GAE}$ / clip & $3\cdot10^{-4}$ / 0.999 / 0.95 / 0.2 \\
\texttt{ent\_coef} & 0.05 (Phase 3: Annealing auf 0.001; Phase 4: 0.0001) \\
\texttt{vf\_coef} & 0.5 \\
LSTM & 1 Schicht, 256 Hidden Units, separater Critic-LSTM \\
Beobachtung & 229 Features (Grid $15\times15$ + HP + Kompass + Step-Frac), Env v11 \\
Parallele Umgebungen & 16 (DummyVecEnv), Device CPU \\
Seed-Pool (\glqq Swarm\grqq) & aktiv, Sampling-Wahrscheinlichkeit 0.3 \\
Curriculum & 4 Phasen (Tab.~\ref{tab:curriculum}), leistungsbasiert \\
Eval & Testset A 7000--7049, Holdout B 8000--8049, Cap 4000, det + stoch \\
\bottomrule
\end{tabular}
\caption[Gemeinsame Basiskonfiguration aller Iterationen]{Gemeinsame Basiskonfiguration aller Iterationen (Quelle:
\texttt{config.json} der v12-Läufe).}
\label{tab:basiskonfig}
\end{table}

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llccc@{}}
\toprule
\textbf{Iteration} & \textbf{Änderung ggü.\ Baseline} & \textbf{Start} & \textbf{Test A} & \textbf{Holdout B} \\
 & & & \textbf{(stoch/det)} & \textbf{(stoch/det)} \\
\midrule
\textbf{v12} (Baseline, $n{=}3$) & -- (LSTM 256, \texttt{vf\_coef} 0.5) & Phase 1 & 73/32\,\% & 80/42\,\% \\
\textbf{E1} critic & \texttt{vf\_coef} $0.5\!\to\!1.0$ & Phase 3 & 56/42\,\% & 72/36\,\% \\
\textbf{E2} curric & Phase 3 Exit $25\text{--}45\!\to\!25\text{--}35$ & Phase 3 & 26/16\,\% & 42/24\,\% \\
\textbf{E3} lstm512 & LSTM $256\!\to\!512$ & Phase 1 & 44/14\,\% & 58/20\,\% \\
\bottomrule
\end{tabular}
\caption[Systematische Iterationen zur Schließung des Det/Stoch-Gaps]{Iterationen zur Schließung des Det/Stoch-Gaps; jeder Arm ein Einzellauf,
die Baseline ein Mittel aus drei Läufen. Werte standardisiert (50 Seeds, Limit
4\,000). Zur Aussagekraft siehe Abschnitt~\ref{sec:gapexperimente}.}
\label{tab:iterationen}
\end{table}

\noindent
Zwei Einschränkungen sind beim Lesen der Tabelle zu beachten. E1 und E2 setzen
aus einem gemeinsamen Phase-2-Checkpoint auf und erhalten nur 400k statt
1{,}0\,Mio.\ Phase-3-Schritte; sie variieren gegenüber der Baseline also zwei
Größen und nicht eine. E3 ist dagegen ein voller Lauf ab Phase~1. Da zudem
jeder Arm nur einmal gelaufen ist, die Streuung über Läufe aber rund $\pm 12$
Punkte beträgt, sind Abstände unterhalb von etwa 25 Punkten nicht
interpretierbar.

Ergänzend zeigt die Fehleranalyse in Tabelle~\ref{tab:rootcause} die frühere
Generationenreihe (v2/v10 Referenz, v7--v9 fehlgeschlagen), deren Setup sich
ausschließlich in den dort genannten vier Parametern unterscheidet.

\section{Technische Umsetzung und Implementierung}
\label{sec:technik}

\subsection{Technologie-Stack}

Das System besteht aus drei Schichten: einem C++-Kern für Simulation und
Weltgenerierung, einem Python-Binding und dem Python-Trainingsstack.
Tabelle~\ref{tab:stack} listet die verwendeten Komponenten.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Komponente} & \textbf{Technologie} & \textbf{Zweck} \\
\midrule
Simulationskern & C++20 (ca.\ 4\,500 LOC) & Weltgenerierung, Spiellogik, Reward, BFS \\
Binding & pybind11 2.13 & C++-Kern als Python-Modul (\texttt{stoneforge\_sim}) \\
Build & CMake & Kern, Binding, Clients \\
RL-Umgebung & Python 3.12, Gymnasium 1.2 & \texttt{StoneforgeWorldEnv} (Gym-API) \\
RL-Algorithmen & Stable-Baselines3 / sb3-contrib 2.8 & PPO, DQN, A2C, RecurrentPPO \\
Deep Learning & PyTorch 2.12 (CPU) & Netz- und LSTM-Training \\
Spiel-Client & raylib 5.5 (C++, ca.\ 4\,500 LOC) & spielbarer Client, KI-Zuschaumodus \\
Monitoring & TensorBoard, WebSockets & Trainingsmetriken, Live-Visualisierung \\
Auswertung & NumPy, Matplotlib & Evaluation, Abbildungen \\
\bottomrule
\end{tabular}
\caption{Technologie-Stack.}
\label{tab:stack}
\end{table}

Die Simulation ist deterministisch pro Seed. Training und Evaluation laufen
vollständig auf der CPU; ein Vergleich mit der GPU ergab für dieses kleine Netz
keine Beschleunigung. Der Durchsatz liegt je nach Konfiguration bei ca.\
90--190 Umgebungsschritten pro Sekunde. Die verwendete Hardware und der
zugehörige Benchmark sind in Anhang~\ref{sec:performance} dokumentiert.

\subsection{Umgebung und Binding}

\texttt{StoneforgeWorldEnv} (Python) kapselt den C++-Kern hinter der
Gymnasium-API. Die Beobachtung umfasst 229 Merkmale (Grid $15{\times}15$ als
Tile-Typen, HP, Exit-Kompass $dx/dy$, Schrittanteil); der Aktionsraum ist
\texttt{Discrete(4)} (vier Bewegungsrichtungen). Die im Spiel vorhandenen
Aktionen Mining, Bauen und Kampf sind im RL-Binding deaktiviert und werfen bei
Aufruf einen Fehler; sie existieren nur im spielbaren Client. Eine Episode endet
bei Erreichen des Exits, nach 4\,000 Schritten oder als Early-Stop-Truncation
nach 256 Schritten ohne positiven Reward (Abschnitt~\ref{sec:umgebung}).

Zwei Implementierungsdetails waren fehlerkritisch: (1)~Die
Weltgenerierungs-Konfiguration ist im C++-Kern prozessglobal; jede Env-Instanz
stempelt sie deshalb bei jedem \texttt{reset()} neu, da sonst parallel erzeugte
Evaluations-Umgebungen die Trainingsverteilung verändern (Abschnitt~\ref{sec:v11}).
(2)~Der Exit wird seit v11 nach realer BFS-Laufwegdistanz platziert statt nach
Luftlinie; alle erzeugten Welten sind nachweislich lösbar (150/150 im Test).

Der Reward setzt sich zusammen aus Schritt-Malus ($-0{,}01$), Explorations-Bonus
($+0{,}02$ pro neuem Tile), Loop-Malus ($-0{,}05$), Schadens-Malus, dem
PBRS-Term auf der BFS-Distanz (Abschnitt~\ref{sec:grundlagen}) und dem
Terminal-Reward.

\subsection{Trainings-Pipeline}

Das Curriculum-Training (\texttt{train\_curriculum.py}) orchestriert die vier
Phasen aus Tabelle~\ref{tab:curriculum}. Je Lauf arbeiten 16 parallele
Umgebungen (\texttt{DummyVecEnv}). Alle 25\,000 Schritte evaluiert ein Callback
das aktuelle Modell auf den 50 Validierungs-Seeds in beiden Betriebsarten;
die Phasen-Metrik steuert Gating (Ziel-SR in zwei aufeinanderfolgenden
Evaluationen) und die Auswahl des besten Modells der Phase. Beim Phasenwechsel
wird das beste Modell der Vorphase geladen. In Phase~3 senkt ein Callback den
Entropie-Koeffizienten linear von 0{,}05 auf 0{,}001 über 500\,000 Schritte;
Phase~4 trainiert mit 0{,}0001 (Greedy Fine-Tune). Optional ergänzt ein
Seed-Pool (\glqq Swarm\grqq) 30\,\% der Episodenstarts durch Wiederholung
bereits gelöster Seeds; der Pool wird bei jedem Phasenwechsel geleert. Jeder
Lauf legt pro Phase Modell-Checkpoints sowie \texttt{eval\_history.json} und
\texttt{config.json} ab; alle Experimente sind zusätzlich im
\texttt{CHANGELOG.md} des Repositories dokumentiert.

\subsection{Monitoring und Werkzeuge}

Trainingsmetriken werden nach TensorBoard geschrieben. Neben der bereits
eingeführten \texttt{explained\_variance} (Güte der Wertfunktion,
Abschnitt~\ref{sec:grundlagen}) und der Entropie der Politik ist dabei
\texttt{approx\_kl} von Bedeutung: ein Schätzwert dafür, wie stark sich die
Politik durch ein Update verändert hat. Werte nahe null zeigen an, dass das
Training faktisch stillsteht; sehr große Werte deuten auf zu grobe Schritte
hin. Die Größe dient in dieser Arbeit als schneller Test, ob ein Lauf
überhaupt lernt (Anhang~\ref{sec:entwicklung}). Zusätzlich streamt
ein WebSocket-Server den Trainingszustand in eine Browser-Ansicht:
Erfolgsquoten-Kurven (deterministisch und stochastisch), Minimaps der 16
parallelen Agenten und eine Explorations-Heatmap. Eine Launcher-GUI (Tkinter)
bündelt Training, Evaluation und Abspielen. Zum Vorführen steuert
\texttt{demo\_agent.py} den grafischen Client: Der Client läuft im
KI-Modus und erhält die Aktionen des geladenen Modells über die
Standardeingabe; Client und Trainings-Umgebung erzeugen dieselbe Welt aus
demselben Seed.

\section{Evaluation}
\label{sec:evaluation}

Das Evaluationsprotokoll (Seed-Trennung, Metrik, Betriebsarten) ist in
Abschnitt~\ref{sec:methodik} beschrieben. Die Ergebnisse sind nach Beweislast
geordnet: Der Vergleich gegen ungelernte Referenzpolitiken
(Abschnitt~\ref{sec:baselines}) schränkt die Aussagekraft aller vorher
berichteten Zahlen ein und steht deshalb nach ihnen.

\subsection{Zentrale Gegenüberstellung: Gedächtnis statt lokalem Orakel}
\label{sec:ablation}

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}llcccrr@{}}
\toprule
\textbf{Bedingung} & \textbf{Arch.} & \textbf{BFS in Obs} & \textbf{Curric.} & \textbf{Obs-Dim.} & \textbf{SR stoch.} & \textbf{SR det.} \\
\midrule
A (\texttt{ppo\_phase4}) & MLP & ja & ja & 236 & 32\,\% & 0\,\% \\
B (\texttt{ppo\_no\_bfs}) & MLP & nein & nein & 230 & -- & $\approx 0\,\%$ \\
C (\texttt{ppo\_lstm\_curriculum}) & \textbf{LSTM} & nein & ja & 231 & \textbf{86\,\%} & 36\,\% \\
\bottomrule
\end{tabular}
\caption[Gegenüberstellung: Gedächtnis gegenüber lokalem BFS-Orakel]{Gegenüberstellung dreier Modelle auf Testset~A (Exit 35--45), Stand
Juni 2026; je ein Einzellauf. Zur eingeschränkten Aussagekraft siehe den
folgenden Absatz.}
\label{tab:ablation}
\end{table}

\paragraph{Aussagekraft.}
Diese Gegenüberstellung ist \emph{keine} kontrollierte Ablation. Vier
Einschränkungen begrenzen, was aus ihr folgt. Erstens unterscheiden sich A und
C in \emph{zwei} Faktoren gleichzeitig (Architektur \emph{und} BFS-Features),
der Effekt ist also nicht separierbar. Zweitens laufen die drei Bedingungen auf
unterschiedlichen Observationsräumen und Umgebungsständen, alle vor der
v11-Revision; A wurde zudem mit \texttt{forceGuaranteedPath} und 236 Merkmalen
trainiert und erreicht unter dem damaligen kurzdistanzigen Eval-Protokoll
100\,\% deterministisch, unter dem hier verwendeten (Exit 35--45) hingegen
0\,\%. Drittens ist B kein vollwertiger Vergleichsarm: Der Lauf wurde bei
720\,k von 2\,Mio.\ Schritten abgebrochen und enthielt einen
Stagnations-Penalty, der später als schädlich erkannt und entfernt wurde.
Viertens ist jede Bedingung ein Einzellauf, bei einer Streuung über Läufe von
rund $\pm 12$ Punkten (Tabelle~\ref{tab:v12}).

Belastbar ist daher nur die schwache Aussage, dass eine rekurrente Politik
\emph{ohne} lokale BFS-Features eine Leistung erreicht, die eine gedächtnislose
Politik \emph{mit} diesen Features unter demselben Protokoll nicht erreicht
hat.

Das finale Modell (v12, Mittel über sieben Läufe) erreicht auf Holdout B
$66{,}9\,\%$ (stochastisch) und erfüllt damit das Holdout-Kriterium; auf Testset A
wird die 70\,\%-Schwelle mit $65{,}7\,\%$ verfehlt
(Abschnitt~\ref{sec:endergebnis}). Abbildung~\ref{fig:lernkurve} zeigt den
Trainingsverlauf dieses historischen Referenzlaufs; die Verläufe der sieben
finalen v12-Läufe zeigt Abbildung~\ref{fig:v12kurven}.

\begin{figure}[h]
\centering
\includegraphics[width=0.72\textwidth]{figures/fig_lernkurve.pdf}
\caption[Lernkurve des historischen Referenzlaufs]{Lernkurve des historischen Referenzlaufs
(\texttt{ppo\_lstm\_curriculum}, Bedingung C, Umgebung vor der
v11-Revision; Curriculum-Phase 3, stochastische Evaluation auf 50 Seeds).}
\label{fig:lernkurve}
\end{figure}

\subsection{Endergebnis (v12, $n=7$)}
\label{sec:endergebnis}

Die sieben finalen Curriculum-Läufe (alle vier Phasen durchlaufen) ergeben auf
dem je Lauf nach Validierungs-SR besten Modell:

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Lauf} & \textbf{A stoch.} & \textbf{A det.} & \textbf{B stoch.} & \textbf{B det.} \\
\midrule
Seed 1 & 62\,\% & 38\,\% & 76\,\% & 46\,\% \\
Seed 2 & 84\,\% & 26\,\% & 76\,\% & 44\,\% \\
Seed 3 & 76\,\% & 32\,\% & 80\,\% & 36\,\% \\
Seed 4 & 74\,\% & 40\,\% & 74\,\% & 38\,\% \\
Seed 5 & 58\,\% & 20\,\% & 62\,\% & 34\,\% \\
Seed 6 & 50\,\% & 20\,\% & 50\,\% & 14\,\% \\
Seed 7 & 56\,\% & 28\,\% & 50\,\% & 16\,\% \\
\midrule
\textbf{Mittel $\pm$ Std} & \textbf{65{,}7 $\pm$ 12{,}4} & 29{,}1 $\pm$ 8{,}0 & \textbf{66{,}9 $\pm$ 12{,}8} & 32{,}6 $\pm$ 12{,}7 \\
95\,\%-CI & [54{,}2;\,77{,}2] & [21{,}8;\,36{,}5] & [55{,}0;\,78{,}7] & [20{,}8;\,44{,}4] \\
\bottomrule
\end{tabular}
\caption[Endergebnis der v12-Läufe über sieben Seeds]{Endergebnis der sieben v12-Läufe auf Testset~A und Holdout~B (je 50
Seeds, Limit 4\,000, beide Betriebsarten). Standardabweichung als
Stichproben-Std ($\mathrm{ddof}=1$), Konfidenzintervall über die
$t$-Verteilung ($\mathrm{df}=6$).}
\label{tab:v12}
\end{table}

\noindent
Ausgewertet wird je Lauf nicht der letzte Checkpoint, sondern das nach
Validierungs-Erfolgsquote beste Modell. Die Auswahl erfolgt ausschließlich auf
den disjunkten Seeds 6000--6049. Da über einen Lauf hinweg mehrere Dutzend
Checkpoints bewertet werden, enthält der so gewählte Wert einen
Selektionsoptimismus gegenüber der Validierung, nicht jedoch gegenüber A
und~B.

\textbf{Das Holdout-Kriterium ist erfüllt, das Testset-Kriterium verfehlt:}
B erreicht $66{,}9\,\% \geq 60\,\%$, A bleibt mit $65{,}7\,\%$ um 4{,}3
Prozentpunkte unter der 70\,\%-Schwelle. Deterministisch werden beide Kriterien
verfehlt ($29{,}1\,\%$ / $32{,}6\,\%$); zur Einordnung dieses Gaps siehe
Abschnitt~\ref{sec:detstochgap}. Abbildung~\ref{fig:v12kurven} zeigt die
zugrunde liegenden Trainingsverläufe aller sieben Läufe: Die hohe
Volatilität der Lerndynamik innerhalb der Phasen ist dieselbe Streuungsquelle,
die sich in den $\pm$12 Punkten der Endergebnisse niederschlägt.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/fig_v12_lernkurven.pdf}
\caption[Trainingsverläufe der sieben v12-Läufe]{Trainingsverläufe der sieben v12-Läufe auf den Validierungs-Seeds
6000--6049, getrennt nach Curriculum-Phase. Dünne Linien: stochastische
Erfolgsquote der Einzelläufe; dicke Linien: Ensemble-Mittel stochastisch
(dunkelblau) und deterministisch (grün). Da die Schwierigkeit je Phase
verschieden ist, sind die Werte nur innerhalb eines Panels vergleichbar.}
\label{fig:v12kurven}
\end{figure}

\paragraph{Der Weg von $n=3$ zu $n=7$ ist selbst ein Ergebnis.}
Eine Zwischenauswertung über die ersten drei Läufe hatte
$73{,}3 \pm 8{,}3$ (A) und $80{,}0 \pm 8{,}0$ (B) ergeben und beide Kriterien
scheinbar erfüllt. (Skriptkonsistent gerechnet, d.\,h.\ mit demselben
Eval-Skript wie die $n{=}7$-Auswertung, lauten dieselben drei Läufe
$74{,}0\,\%$ bzw.\ $77{,}3\,\%$; der Effekt der Aufstockung ist also
$74{,}0 \to 65{,}7$ und nicht ein Artefakt des Skriptwechsels.)
Da der Abstand zur Schwelle (3{,}3 Punkte) dort kleiner war als
die Streuung und das damalige Bootstrap-Intervall auf A ($[66{,}7;\,80{,}0]$) die
70\,\%-Schwelle einschloss, war die Aussage \glqq robust über 70\,\%\grqq{} nicht
belegbar. Vier zusätzliche Läufe wurden gezielt zur Klärung dieser Frage
gestartet und senkten beide Mittelwerte deutlich. Der Dreier-Mittelwert war
zu optimistisch.

Das ist exakt der Effekt, den Henderson et al.\ \cite{henderson2018matters} für
Deep RL beschreiben: Die Varianz über Trainingsläufe ist so groß, dass wenige
Seeds systematisch zu Fehlschlüssen führen, weshalb sie Mittelwert $\pm$
Standardabweichung über mehrere Läufe fordern und ausdrücklich vor
\emph{best-of-runs}-Berichten warnen. Die vorliegende Arbeit reproduziert diesen
Befund am eigenen Gegenstand.

\paragraph{Drei Alternativerklärungen wurden geprüft und ausgeschlossen.}
Da die vier Zusatzläufe im Mittel schwächer ausfielen (59{,}5\,\% gegenüber
74{,}0\,\% auf A), wurde geprüft, ob eine systematische Ursache statt Seed-Varianz
vorliegt:

\begin{enumerate}
  \item \textbf{Messfehler:} ausgeschlossen. Das für $n=7$ neu implementierte
        Evaluationsskript reproduziert alle sechs deterministischen Werte der
        Läufe 1--3 exakt (A: 38/26/32; B: 46/44/36); die stochastischen Werte
        weichen um 2--8 Punkte ab, was dem erwarteten Sampling-Rauschen entspricht.
  \item \textbf{Code-Stand:} ausgeschlossen. Die Läufe 1--3 und 4--7 entstanden auf
        unterschiedlichen Commits. Ein Diff über den gesamten Simulationspfad
        (\texttt{src/core}, \texttt{src/python}, \texttt{src/include},
        \texttt{python/}, \texttt{game\_config.json}, \texttt{train\_curriculum.py})
        ergibt genau eine geänderte Datei: \texttt{doc\_logger.py}, und zwar jene
        22 Zeilen, die den Commit-Hash in die \texttt{config.json} schreiben.
        Simulation, Environment und Trainingsskript sind identisch; die übrigen
        Änderungen betreffen ausschließlich Client-Dateien, die nicht in
        \texttt{stoneforge\_sim.so} einfließen.
  \item \textbf{Laufzeitumgebung:} ausgeschlossen. Die installierten Versionen von
        \texttt{torch} und \texttt{sb3-contrib} sind älter als beide Lauf-Chargen;
        die Versions-Pinnung erfolgte deklarativ ohne Neuinstallation.
\end{enumerate}

\noindent
Ein Welch-Test zwischen beiden Chargen (74{,}0 vs.\ 59{,}5) ist mit $p = 0{,}149$
nicht signifikant; bei $n = 3$ bzw.\ $n = 4$ ist die Stichprobe für eine solche
Aussage zu klein. Es handelt sich um Seed-Varianz; die $n=7$-Werte sind
belastbar.

\paragraph{Weitere Beobachtungen.}
Der bei $n=3$ auffällige Abstand $B > A$ verschwindet bei $n=7$ nahezu
($66{,}9$ vs.\ $65{,}7$); beide Sets stammen aus demselben Generator und
derselben Distanzverteilung, die frühere Differenz war eine Eigenschaft der
kleinen Stichprobe. Ein Gegentest mit den Phase-2-Zwischenmodellen ergab keine
Verbesserung; das finale Modell bleibt das Ausgabemodell.

\subsection{Ungelernte Referenzpolitiken und die Grenzen der Erfolgsquote}
\label{sec:baselines}

Eine Erfolgsquote ist ohne Untergrenze nicht interpretierbar: Ohne Bezugspunkt
bleibt offen, ob 65\,\% eine gelernte Fähigkeit ausdrücken oder bereits durch
Zufall erreichbar sind.

Verglichen werden zwei ungelernte Politiken, die \emph{dieselbe} Beobachtung
erhalten wie der Agent:

\begin{itemize}
  \item \textbf{Random:} gleichverteilte Bewegung; absolute Untergrenze.
  \item \textbf{Kompass-Zufallslauf:} mit Wahrscheinlichkeit $\varepsilon$
        gleichverteilt zufällig, sonst ein Schritt in die dominante Richtung des
        Luftlinien-Kompasses ($\mathit{exitDx}/\mathit{exitDy}$). Diese Politik
        nutzt \emph{zwei} der 229 Merkmale; das lokale $15{\times}15$-Grid wird
        vollständig ignoriert. Sie hat kein Gedächtnis, keine Parameter und
        wurde nicht trainiert. Der einzige freie Wert $\varepsilon$ wurde
        ausschließlich auf den Validierungs-Seeds 6000--6049 gewählt, also nach
        demselben Protokoll wie die Modellselektion; Testset~A und Holdout~B
        wurden dafür nicht verwendet.
\end{itemize}

Als zweite Metrik dient die \textbf{Pfadeffizienz}
$\eta = d_{\mathrm{BFS}}(s_0)\,/\,T_{\mathrm{Erfolg}}$, gemittelt über die
erfolgreichen Episoden: das Verhältnis aus kürzestem Weg und tatsächlich
benötigten Schritten. $\eta = 1$ bedeutet optimale Navigation, $\eta = 0{,}05$
einen zwanzigfachen Umweg. Sie ist erforderlich, weil die Erfolgsquote bei
einem Episodenlimit von 4\,000 Schritten und einer mittleren Optimallänge von
40 Schritten (Faktor 100) kaum diskriminiert. Sie greift zugleich die zweite,
im Exposé neben der Erfolgsquote genannte Zielgröße auf (Minimierung der
\emph{Steps per Episode}), die dort nicht quantifiziert worden war.
Tabelle~\ref{tab:baselines} stellt beide Metriken für alle Politiken
gegenüber.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
& \multicolumn{2}{c}{\textbf{Testset A}} & \multicolumn{2}{c}{\textbf{Holdout B}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Politik} & \textbf{SR} & \textbf{$\eta$} & \textbf{SR} & \textbf{$\eta$} \\
\midrule
Random (gleichverteilt) & $5{,}2 \pm 2{,}7$ & 0{,}021 & $10{,}4 \pm 1{,}7$ & 0{,}025 \\
Kompass-Zufallslauf, $\varepsilon = 0{,}4$ & $60{,}4 \pm 4{,}8$ & \textbf{0{,}153} & $61{,}6 \pm 5{,}0$ & \textbf{0{,}161} \\
Kompass-Zufallslauf, $\varepsilon = 0{,}9$ & $\mathbf{92{,}0 \pm 5{,}1}$ & 0{,}047 & $\mathbf{91{,}6 \pm 4{,}3}$ & 0{,}048 \\
\midrule
RecurrentPPO v12 ($n=7$, stoch.) & $65{,}7 \pm 12{,}4$ & 0{,}049 & $66{,}9 \pm 12{,}8$ & 0{,}057 \\
\bottomrule
\end{tabular}
\caption[Ungelernte Referenzpolitiken gegenüber dem trainierten Agenten]{Ungelernte Referenzpolitiken gegenüber dem trainierten Agenten; Protokoll
identisch (je 50 Seeds, Exit 35--45, Limit 4\,000). $\eta$ bezeichnet die
Pfadeffizienz, die beiden $\varepsilon$-Werte die auf der Validierung
erfolgs- bzw.\ effizienzoptimale Einstellung. Alle Baseline-Werte sind Mittel
über fünf Wiederholungen mit unterschiedlichem Politik-Zufallsstrom
(RNG-Seeds 100--104) bei festen Weltseeds. Erzeugt mit
\texttt{scripts/eval\_baselines.py}. Die Random-Zeile reagiert bei ihrem
niedrigen Niveau merklich auf die Wahl des Politik-Zufallsstroms (mit den
RNG-Seeds 0--4: $10{,}4 \pm 4{,}6$ auf A); die Kompass-Zeilen sind stabil
($89{,}6 \pm 1{,}7$ bei $\varepsilon = 0{,}9$).}
\label{tab:baselines}
\end{table}

\paragraph{Befund.} Der Kompass-Zufallslauf übertrifft den trainierten Agenten
auf \emph{beiden} Achsen. Bei vergleichbarer Pfadeffizienz
($\eta \approx 0{,}05$) erreicht er 92\,\% statt 65{,}7\,\%; bei
vergleichbarer Erfolgsquote ($\approx 60\,\%$) ist er mit $\eta = 0{,}15$ rund
dreimal wegeffizienter. Der Agent liegt zwar deutlich über der
Zufallsuntergrenze von 5--10\,\%, erreicht aber an keinem Betriebspunkt die Leistung
einer Heuristik aus vier Zeilen Code, die 227 seiner 229 Eingangsmerkmale
ignoriert.

\paragraph{Kein Episodenbudget kehrt das Bild um.} Naheliegend wäre der
Einwand, das Problem liege allein am großzügigen Limit von 4\,000 Schritten.
Das lässt sich exakt prüfen: Wer eine Welt bei Schritt $T$ löst, löst sie unter
jedem Budget $\geq T$; aus einem einzigen Lauf mit protokollierten
Erfolgsschritten folgt daher die vollständige Kurve
\emph{Erfolgsquote über Budget} (Tabelle~\ref{tab:capsweep}). Das Ergebnis ist
eindeutig: Bei \emph{jedem} Budget erreicht oder übertrifft mindestens eine der
beiden ungelernten Politiken alle drei trainierten Läufe. Ein engeres
Episodenlimit ist als Korrektur damit nicht ausreichend.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}rrrrrr@{}}
\toprule
& \multicolumn{2}{c}{\textbf{Kompass-Zufallslauf}} & \multicolumn{3}{c}{\textbf{RecurrentPPO v12}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-6}
\textbf{Budget} & $\varepsilon = 0{,}4$ & $\varepsilon = 0{,}9$ & Lauf 1 & Lauf 2 & Lauf 3 \\
\midrule
120 ($3\times$ Optimum) & \textbf{4\,\%}  & 0\,\%  & 0\,\%  & 2\,\%  & 0\,\% \\
200                     & \textbf{22\,\%} & 0\,\%  & 0\,\%  & 4\,\%  & 0\,\% \\
300                     & \textbf{28\,\%} & 0\,\%  & 2\,\%  & 10\,\% & 6\,\% \\
500                     & \textbf{36\,\%} & 10\,\% & 12\,\% & 22\,\% & 12\,\% \\
800                     & \textbf{54\,\%} & 18\,\% & 20\,\% & 32\,\% & 30\,\% \\
1\,500                  & \textbf{62\,\%} & 54\,\% & 40\,\% & 48\,\% & 54\,\% \\
4\,000                  & 64\,\%          & \textbf{88\,\%} & 64\,\% & 78\,\% & 74\,\% \\
\bottomrule
\end{tabular}
\caption[Erfolgsquote als Funktion des Episodenbudgets]{Erfolgsquote auf Testset~A in Abhängigkeit des Episodenbudgets
(BFS-Optimum $\varnothing$ 40 Schritte). Hervorgehoben ist je Zeile der
höchste Wert.}
\label{tab:capsweep}
\end{table}

\paragraph{Was daraus folgt und was nicht.} Der Befund entwertet weder die
Plattform noch die methodische Arbeit dieses Projekts; er entwertet die
\emph{Aufgabenstellung} in ihrer aktuellen Parametrisierung. Drei Eigenschaften
wirken zusammen: (1)~Das Episodenlimit übersteigt die Optimallänge um den
Faktor 100, sodass ungerichtete Suche genug Zeit hat; (2)~der
Luftlinien-Kompass liefert dauerhaft eine grob richtige Richtung, sodass ein
verrauschter Gradientenaufstieg genügt; (3)~die erzeugten Welten sind
hinreichend offen, dass Sackgassen durch Zufallsbewegung wieder verlassen
werden. Unter diesen Bedingungen ist Gedächtnis für die Erfolgsquote schlicht
nicht erforderlich, und die Metrik kann folglich nicht messen, ob es gelernt
wurde.

Damit ist die zentrale Leistungsaussage dieser Arbeit einzuschränken: Belegt
ist, dass der Agent die Aufgabe löst und dabei deutlich über der
Zufallsuntergrenze liegt; \emph{nicht} belegt ist, dass er eine Strategie
gelernt hat, die über gerichtete Zufallssuche hinausgeht. Diese Einschränkung
betrifft auch die Gegenüberstellung in Abschnitt~\ref{sec:ablation}: Wenn eine
kompassgetriebene Zufallspolitik 92\,\% erreicht, ist der Vergleich
\glqq LSTM ohne BFS\grqq{} gegen \glqq MLP mit BFS\grqq{} kein Test der
Gedächtnisfähigkeit, sondern misst überwiegend, wie gut die jeweilige Politik
den Kompass ausnutzt und wie stark sie in Schleifen gerät.

\paragraph{Konsequenz für Folgearbeiten.} Die naheliegende Reaktion ist nicht,
den Agenten weiter zu tunen, sondern die Aufgabe selbst zu korrigieren. Da
Tabelle~\ref{tab:capsweep} zeigt, dass eine Änderung des Episodenbudgets allein
nicht genügt, führt der Weg über die Weltgenerierung: Der Wandanteil muss so
weit erhöht werden, dass ungerichtete Suche scheitert, ohne die Lösbarkeit zu
gefährden. Die Baseline-Politik dient dabei als Kalibrierinstrument, mit dem
sich ein geeigneter Schwierigkeitsgrad \emph{vor} jedem weiteren Training
bestimmen lässt. Das Vorgehen ist in Abschnitt~\ref{sec:ausblick} ausgeführt.
Erst unter einem solchen Regime lässt sich die Forschungsfrage dieser Arbeit
beantworten.

Eine unabhängige Wiederholung der Modellmessung mit anderem Zufallsstrom
ergab $68{,}6\,\%$ auf Testset~A statt der in Tabelle~\ref{tab:v12}
berichteten $65{,}7\,\%$. Die Abweichung von knapp drei Punkten entspricht dem
erwarteten Sampling-Rauschen und ändert die Aussage nicht.

\subsection{Der Det/Stoch-Gap: Weglängen-Analyse und POMDP-Einordnung}
\label{sec:detstochgap}

Der Abstand zwischen stochastischer und deterministischer SR ist kein
Schwelleneffekt, sondern wächst kontinuierlich mit der Weglänge
(Abbildung~\ref{fig:detgap}; 120 Validierungs-Welten, nach Startdistanz
gruppiert): Bei 5--15 Feldern erreicht die deterministische Betriebsart 79\,\%,
bei 35--45 Feldern noch 31\,\%, während die stochastische zwischen 100\,\% und
82\,\% bleibt. Die Interpretation ist kumulativ: Je länger der Weg ohne direkte
Zielsicht (Sichtradius 7), desto mehr Kreuzungsentscheidungen unter unsicherem
Belief-State; an jeder liegen die Aktionswahrscheinlichkeiten nahe beieinander,
und jede einzelne kann die Argmax-Politik in eine Oszillationsschleife führen,
aus der Sampling entkommt. Der Gap ist damit teils eine Gedächtnis-Limitation,
teils dem POMDP-Charakter geschuldet (unter Unsicherheit ist eine stochastische
Politik nicht per se suboptimal, Abschnitt~\ref{sec:grundlagen}).

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{figures/fig_det_gap_distanz.pdf}
\caption[Erfolgsquote nach Startdistanz zum Exit]{Erfolgsquote nach Startdistanz zum Exit (120 Validierungs-Welten,
finales v12-Modell, Limit 4\,000): Die deterministische SR fällt kontinuierlich
mit der Weglänge, die stochastische bleibt hoch.}
\label{fig:detgap}
\end{figure}

Zur theoretischen Einordnung ist eine Unterscheidung nötig, die in der
Zwischendokumentation dieses Projekts zunächst unscharf blieb. Singh et al.\
zeigen, dass in POMDPs \emph{stochastische gedächtnislose} Politiken jede
deterministische gedächtnislose erheblich übertreffen können
\cite{singh1994pomdp}. Dieses Resultat erklärt den hier beobachteten Gap
jedoch \emph{nicht}, denn die verwendete Politik ist nicht gedächtnislos: Über
einem hinreichend genauen Belief-State existiert wieder eine optimale
deterministische Politik. Dass die Argmax-Auswertung des LSTM-Agenten dennoch
deutlich abfällt, ist daher primär ein Hinweis auf einen \emph{unvollständigen}
Belief-State, also eine Gedächtnis-Limitation, und nur mittelbar eine
Eigenschaft der Aufgabe. Singh et al.\ liefern die Motivation dafür, warum
stochastisches Verhalten unter partieller Beobachtbarkeit überhaupt nützlich
sein kann, nicht aber eine Rechtfertigung des konkreten Gaps.

Der Teil der Begründung, der auch \emph{mit} Gedächtnis trägt, stammt aus der
Generalisierungs-Anforderung: Ghosh et al.\ zeigen, dass bereits das Ziel, auf
unbekannte Level (hier: neue Seeds) zu generalisieren, selbst eine Form
partieller Beobachtbarkeit induziert (\emph{epistemic POMDP}), deren optimale
Politik stochastisch ist und die durch mehr Gedächtnis über die
\emph{aktuelle} Episode nicht aufgelöst wird \cite{ghosh2021epistemic}. Die
Unsicherheit liegt hier über der Identität der Welt, nicht über dem Zustand
innerhalb einer bekannten Welt. Auf dieses Argument, nicht auf Singh et al.,
stützt sich die Wahl der stochastischen Evaluation als Primärmetrik. Der Gap
ist damit teils erwartbar (epistemische Unsicherheit über Seeds), teils ein
offenes Defizit des gelernten Belief-States; die Trennung beider Anteile ist
mit den vorliegenden Daten nicht möglich und wird als Diagnoseschritt~1 im
Ausblick (Abschnitt~\ref{sec:ausblick}) vorgeschlagen.

\subsection{Versuche zur Schließung des Det/Stoch-Gaps}
\label{sec:gapexperimente}

Um zu prüfen, ob der Gap durch gezielte Eingriffe verkleinerbar ist, wurden drei
Experimente durchgeführt, die je einen inhaltlichen Faktor gegenüber der
v12-Baseline verändern (Setup in Tabelle~\ref{tab:iterationen}):
\textbf{E1} verstärkt den Critic (\texttt{vf\_coef} $0{,}5\to1{,}0$),
\textbf{E2} glättet den Curriculum-Übergang (Phase~3 nur bis Distanz~35 statt~45),
\textbf{E3} vergrößert das Gedächtnis (LSTM $256\to512$).

\paragraph{Aussagekraft.} Diese drei Läufe sind Einzelmessungen ($n=1$) gegen
eine Baseline aus drei Läufen. Die in Abschnitt~\ref{sec:endergebnis}
gemessene Streuung über Trainingsläufe beträgt $\sigma \approx 12$
Prozentpunkte; ein Einzellauf kann also allein durch Seed-Varianz um mehr als
20 Punkte von der Baseline abweichen. Die Arme sind daher \emph{kein}
Wirksamkeitsnachweis, sondern eine Vorauswahl: Sie können eine Hypothese
schwächen, aber nicht widerlegen. Konsequenterweise wird hier nur die schwache
Form berichtet.

\textbf{Kein Arm zeigt einen Hinweis auf Verbesserung.}
E1 hebt die deterministische SR (42\,\% statt 32\,\% auf A), liegt stochastisch
aber niedriger (56\,\% statt 73\,\%). Beide Differenzen liegen innerhalb bzw.\
knapp oberhalb von $1{,}5\,\sigma$ und sind bei $n=1$ nicht belastbar; die
Richtung (mehr Value-Gewicht $\to$ determinierteres Verhalten) ist plausibel,
aber unbestätigt. E2 liegt mit A~26\,\% rund $4\,\sigma$ unter der Baseline;
das ist auch bei $n=1$ deutlich und mechanistisch erklärbar, weil das Modell
auf den längsten, eval-relevanten Distanzen untertrainiert bleibt. Zu beachten
ist, dass E1 und E2 zusätzlich nur 400k statt 1{,}0\,Mio.\ Phase-3-Schritte
erhielten; ihr Rückstand ist also teilweise ein Budget-, kein Faktoreffekt.
E3 liegt mit A~44\,\% rund $2{,}5\,\sigma$ unter der Baseline, ist ein voller
Lauf mit identischem Budget und zeigt in \emph{allen} Curriculum-Phasen Werte
auf oder unter der 256er-Baseline bei rund dreifachem Rechenaufwand. Das ist
der belastbarste der drei Befunde: Mehr Gedächtnis-\emph{Kapazität} bedeutet in
dieser Umgebung jedenfalls nicht mehr Gedächtnis-\emph{Nutzen}. Er passt zu den
bekannten Grenzen rekurrenter Politiken in prozeduraler Navigation, deren
Gedächtnisanforderungen über reine Kapazität hinausgehen
\cite{pleines2022recurrent}. Eine Replikation von E1 und E3 mit je zwei
weiteren Seeds steht aus und ist der nächste offene Punkt
(Abschnitt~\ref{sec:ausblick}).

Ergänzend wurde geprüft, ob der Gap lediglich ein Kalibrierungsartefakt der
Argmax-Auswertung ist. Ein Temperatur-Sweep zwischen reinem Argmax ($T{=}0$) und
vollem Sampling ($T{=}1$) auf den drei v12-Modellen (Tabelle~\ref{tab:temperatur})
zeigt einen \emph{monotonen} Verlauf: Mehr Stochastik ist durchweg besser, keine
Zwischentemperatur übertrifft das volle Sampling. Der Gap ist damit \emph{kein}
Argmax-Artefakt, sondern die empirische Signatur des POMDP-Resultats
\cite{singh1994pomdp}. Bereits $T{=}0{,}5$ holt den Großteil
des Argmax-Defizits zurück (A: $+31$, B: $+29$ Prozentpunkte); dies ist ein
geeigneter Betriebspunkt, falls ein weniger zufälliges Verhalten gewünscht ist.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Temperatur} & \textbf{Testset A} & \textbf{Holdout B} \\
\midrule
Argmax ($T{=}0$)     & $32{,}0 \pm 4{,}9$ & $42{,}0 \pm 4{,}3$ \\
$T = 0{,}25$         & $52{,}0 \pm 0{,}0$ & $52{,}7 \pm 1{,}9$ \\
$T = 0{,}5$          & $63{,}3 \pm 4{,}1$ & $71{,}3 \pm 6{,}6$ \\
$T = 0{,}75$         & $63{,}3 \pm 7{,}5$ & $72{,}0 \pm 6{,}5$ \\
\textbf{Sampling ($T{=}1$)} & $\mathbf{70{,}7 \pm 8{,}2}$ & $\mathbf{79{,}3 \pm 6{,}6}$ \\
\bottomrule
\end{tabular}
\caption[Temperatur-Sweep der Evaluation]{Erfolgsquote in Abhängigkeit der Sampling-Temperatur (Mittel $\pm$ Std
über die drei v12-Läufe, je 50 Seeds, Limit 4\,000).}
\label{tab:temperatur}
\end{table}

\noindent
Ein Hinweis zur Konsistenz: Die Zeile $T{=}1$ ($70{,}7\,\%$ auf A) misst
dieselbe Größe wie die oben genannten $73{,}3\,\%$ und die in
Abschnitt~\ref{sec:endergebnis} nachgerechneten $74{,}0\,\%$. Die drei Werte
stammen aus unabhängigen Messungen mit drei Skripten; die Streuung von rund
drei Punkten entspricht dem Sampling-Rauschen über 50 Bernoulli-Versuche. Das
ist kein Widerspruch, zeigt aber, dass Unterschiede dieser Größenordnung nicht
interpretierbar sind. Vergleiche \emph{innerhalb} der Tabelle bleiben zulässig,
da sie aus demselben Lauf stammen.

Zusammengenommen widersteht der Det/Stoch-Gap dem Tuning einzelner Stellschrauben;
er ist teils fundamental (POMDP). Nicht-architektonische Hebel wie Hilfs- bzw.\
Repräsentationsverluste oder gezielt mehr Langdistanz-Training bleiben ergänzend
denkbar; das \emph{harte}, fundamentale Langhorizont-Belief-Tracking ist jedoch am
ehesten über strukturiertes statt größeres Gedächtnis anzugehen
(Abschnitt~\ref{sec:ausblick}).

\section{Fazit und Ausblick}
\label{sec:ausblick}

\subsection{Ergebnis}

Über sieben unabhängige Läufe erreicht der Agent $65{,}7\,\% \pm 12{,}4$
(Testset~A) und $66{,}9\,\% \pm 12{,}8$ (Holdout~B) stochastische Erfolgsquote.
Das Holdout-Kriterium ($\geq 60\,\%$) ist damit erfüllt, das Testset-Kriterium
($\geq 70\,\%$) verfehlt.

Diese Zahlen sind jedoch nur gegen die Zufallsuntergrenze von 5--10\,\%
aussagekräftig, nicht gegen eine kompetente ungelernte Referenz: Ein
Kompass-Zufallslauf erreicht 92\,\% und ist bei vergleichbarer Erfolgsquote
zugleich dreimal wegeffizienter (Abschnitt~\ref{sec:baselines}). Bei einem
Episodenbudget vom Hundertfachen der Optimallänge misst die gewählte Metrik
nicht die Fähigkeit, um die es geht, und ein engeres Budget behebt das nicht
(Tabelle~\ref{tab:capsweep}). \textbf{Die Forschungsfrage ist mit dem
vorliegenden Aufbau daher nicht entscheidbar: Die Umgebung erfordert das
Gedächtnis nicht, dessen Nutzen sie messen soll.} Dieser Befund betrifft die
Interpretation sämtlicher Leistungszahlen der Arbeit, einschließlich der
Gegenüberstellung in Abschnitt~\ref{sec:ablation}.

\subsection{Beiträge}

\paragraph{Methodisch.} Die Zwischenauswertung über drei Läufe hatte
$73{,}3\,\%$ / $80{,}0\,\%$ ergeben und beide Kriterien scheinbar erfüllt. Die
Aufstockung auf sieben Läufe senkte beide Werte deutlich. Ein auf drei Läufen
berichtetes \glqq beide Ziele erfüllt\grqq{} wäre nicht falsch gemessen, aber
nicht haltbar gewesen. Zusammen mit dem Baseline-Befund reproduziert die Arbeit
damit zweimal am eigenen Gegenstand, wovor Henderson et al.\
\cite{henderson2018matters} warnen: zu wenige Läufe und ein fehlender
Referenzpunkt führen unabhängig voneinander zu Fehlschlüssen.

\paragraph{Inhaltlich.} Ein rekurrenter Agent ohne BFS-Gradientenfeatures
erreicht mindestens dieselbe stochastische Leistung wie ein MLP \emph{mit}
diesen Features; der LSTM-Zustand ersetzt die vorberechnete lokale
Pfadinformation. Diese Aussage ist doppelt begrenzt: durch die Einschränkungen
der Gegenüberstellung (Tabelle~\ref{tab:ablation}) und dadurch, dass der
globale Luftlinien-Kompass in beiden Bedingungen erhalten bleibt. Ersetzt wird
also die \emph{lokale} Wegweiserinformation, nicht jede globale Zielinformation.

\paragraph{Technisch.} Entstanden ist eine vollständige Experimentierplattform:
ein C++-Kern mit deterministischer prozeduraler Weltgenerierung, über
\texttt{pybind11} als Gymnasium-Umgebung gekapselt, dazu
Curriculum-Trainingspipeline, standardisierte Evaluationsskripte,
Live-Monitoring und ein spielbarer Client mit KI-Zuschaumodus
(Abschnitt~\ref{sec:technik}). Die deterministische Reproduzierbarkeit pro Seed
und die durchgängige Experiment-Protokollierung waren die Voraussetzung für die
Root-Cause-Analysen im Anhang~\ref{sec:entwicklung} und sind für Folgearbeiten
wiederverwendbar.

\subsection{Einschränkungen}

\begin{enumerate}
  \item \textbf{Die Erfolgsmetrik ist nicht trennscharf} und wird von einer
        ungelernten Heuristik übertroffen (Abschnitt~\ref{sec:baselines}). Dies
        ist die schwerwiegendste Einschränkung, weil sie alle übrigen
        Leistungsaussagen relativiert.
  \item \textbf{Das Testset-Kriterium wird verfehlt} ($65{,}7\,\%$ statt
        $\geq 70\,\%$); nur das Holdout-Kriterium ist erfüllt.
  \item \textbf{Der Det/Stoch-Gap bleibt bestehen} ($29{,}1\,\%$ bzw.\
        $32{,}6\,\%$ deterministisch). Er wächst mit der Weglänge und ist
        monoton in der Sampling-Temperatur, also kein Argmax-Artefakt.
        Theoretisch erwartbar ist nur der Anteil aus der epistemischen
        Unsicherheit über den Seed \cite{ghosh2021epistemic}; der Rest ist ein
        Defizit des gelernten Belief-States. Die drei Versuche zu seiner
        Schließung sind Einzelläufe und belegen nicht, dass er
        tuning-resistent \emph{ist}.
  \item \textbf{Begrenzte statistische Präzision:} Die Streuung über Läufe
        beträgt rund $\pm 12$ Punkte; das 95\,\%-Konfidenzintervall auf
        Holdout~B reicht bis $55{,}0\,\%$ hinab, also unter die im Mittel
        erfüllte Schwelle.
  \item \textbf{Ein einziger Generator} und \textbf{keine quantifizierte
        Baseline nicht-rekurrenter Verfahren} (DQN, A2C) auf der finalen
        Umgebung: Der frühe DQN-Anlauf scheiterte vor der v11-Revision und
        wurde nicht wiederholt.
\end{enumerate}

\subsection{Ausblick}

Die Reihenfolge der folgenden Punkte ergibt sich aus dem Befund von
Abschnitt~\ref{sec:baselines}: Solange die Aufgabe die untersuchte Fähigkeit
nicht erfordert, ist jede weitere Optimierung des Agenten nicht
interpretierbar. Die Reparatur des Messregimes steht deshalb vor allem anderen.

\begin{enumerate}
  \item \textbf{Härtung der Umgebung.} Der Wandanteil wird derzeit pro Biom
        fest vorgegeben (7\,\% im Wald bis 25\,\% im Bergland) und durch einen
        zellulären Automaten geglättet. Er sollte zu einem steuerbaren
        Parameter werden und so weit erhöht werden, bis der Kompass-Zufallslauf
        einbricht, die Lösbarkeit aber bei 100\,\% bleibt. Das Vorgehen ist
        zweistufig: erst die Baseline-Politik als Kalibrierinstrument einsetzen,
        dann trainieren. Ein Trainingslauf ohne diese Vorabprüfung wäre erneut
        nicht interpretierbar.
  \item \textbf{Billige Diagnosen vor jedem Architekturwechsel.} (a)~Eine
        lineare Probe auf dem LSTM-Hidden-State, die Exit-Richtung bzw.\
        BFS-Distanz vorhersagt: Kollabiert sie auf langen Wegen, liegt ein
        Belief-Tracking-Limit vor, sonst liegt das Problem in der Politik.
        (b)~Entropie-Annealing in Phase~4 mit Modellselektion auf der
        deterministischen Erfolgsquote. (c)~One-Hot- oder Embedding-Kodierung
        des lokalen Grids statt der bisherigen ordinalen Tile-IDs; dass das
        Grid kaum genutzt wird, legt die Baseline-Analyse nahe.
        (d)~Replikation von E1 und E3 mit je zwei weiteren Seeds.
  \item \textbf{Restpotenzial in RecurrentPPO.} Ein Auxiliary-Loss-Kopf, der
        aus dem Hidden-State die BFS-Distanz vorhersagt, formt den
        Belief-State explizit und nutzt das Orakel als Trainings-, nicht als
        Testzeitsignal. Ergänzend Burn-in für Sequenzstarts nach R2D2
        \cite{kapturowski2019r2d2}, temperaturkalibrierte Evaluation
        ($T = 0{,}5$ ohne Neutraining) sowie vollwertiges Prioritized Level
        Replay \cite{jiang2021plr} und Self-Imitation \cite{oh2018sil}.
  \item \textbf{Architekturvergleich als Folgearbeit.} Der Wechsel auf
        Transformer-basierte In-Context-Verfahren (z.\,B.\ AMAGO
        \cite{grigsby2024amago}) ist die meistgenannte Richtung, hier aber
        bewusst nur eine Hypothese. Denn erstens ist nicht belegt, dass der Gap
        ein Kapazitätsproblem ist: Eine unter hoher Entropie trainierte Politik
        kann ihr Restrauschen zum Verlassen von Schleifen nutzen, ohne je einen
        Anreiz für eine deterministisch tragfähige Aktionswahl gehabt zu haben.
        Zweitens bleiben rekurrente Modelle im breitesten verfügbaren
        POMDP-Vergleich gegenüber den dort geprüften Attention-Modellen
        konkurrenzfähig; zu beachten ist, dass es sich dabei um
        \emph{lineare} Transformer-Varianten handelt und nicht um volle
        Softmax-Attention, wie AMAGO sie verwendet
        \cite{morad2023popgym}. Die eigene E3-Ablation zeigte, dass mehr
        Gedächtniskapazität hier schadet. Erst wenn Schritt~2 ein
        Kapazitätslimit nachweist, wird der Wechsel zur begründeten nächsten
        Frage; dann als kontrollierter Vergleich auf demselben Generator und
        Protokoll.
\end{enumerate}

\section{Anhang: Reproduzierbarkeit}
\label{sec:reproduzierbarkeit}

Dieser Anhang folgt den Reproduzierbarkeits-Checklisten von Pineau et al.\
\cite{pineau2021repro} und Henderson et al.\ \cite{henderson2018matters}. Alle Trainingsläufe sind über
Seed, gepinnte Umgebung und \texttt{config.json} reproduzierbar; die vollständige
Änderungshistorie inklusive verworfener Varianten liegt im Experiment-Changelog.
Tabelle~\ref{tab:sysumgebung} listet die verwendeten Software-Versionen.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Komponente} & \textbf{Version / Wert} \\
\midrule
Python & 3.12.13 \\
Betriebssystem & macOS (arm64) \\
Rechen-Device & CPU (bewusst gewählt, s.\ Performance-Analyse) \\
PyTorch & 2.12.0 \\
Stable-Baselines3 / sb3-contrib & 2.8.0 / 2.8.0 \\
Gymnasium & 1.2.3 \\
NumPy & 2.4.4 \\
Abhängigkeiten (gepinnt) & \texttt{requirements.txt} (\texttt{==}) + \texttt{requirements.lock.txt} \\
\bottomrule
\end{tabular}
\caption[Systemumgebung]{Systemumgebung. Nach jeder Änderung an \texttt{game\_config.json} oder
am C++-Kern ist ein Rebuild des Python-Bindings erforderlich.}
\label{tab:sysumgebung}
\end{table}

\noindent\textbf{Reproduzierbarkeits-Checkliste:}
\begin{itemize}
  \item \textbf{Code \& Konfiguration:} Repository mit Trainings-/Eval-Skripten;
        je Lauf eine \texttt{config.json} mit allen Hyperparametern
        (Tab.~\ref{tab:basiskonfig},~\ref{tab:iterationen}).
  \item \textbf{Durchsuchter Hyperparameter-Bereich:} vollständig im
        \texttt{CHANGELOG.md} dokumentiert, inklusive \emph{negativer} Ergebnisse
        (v7--v10-Fehleranalyse, Tab.~\ref{tab:rootcause}; Iterationen E1--E3).
  \item \textbf{Anzahl Läufe:} sieben unabhängige Seeds; berichtet werden
        Mittelwert $\pm$ Standardabweichung, \emph{kein} Best-of-Runs
        (Kernkritik von Henderson et al.\ \cite{henderson2018matters}).
  \item \textbf{Datentrennung:} disjunkte Seed-Mengen: Validierung
        (6000--6049) für Phasenwechsel und Modellauswahl, Testset A
        (7000--7049) und Holdout B (8000--8049) ausschließlich für die
        finale Messung.
  \item \textbf{Evaluationsprotokoll:} 50 Seeds, Episodenlimit 4\,000
        ($=$ Umgebungs-Maximum), stochastisch \emph{und} deterministisch. Bei der
        rekurrenten Politik werden LSTM-Zustand und Episoden-Start-Signal korrekt
        an \texttt{predict()} durchgereicht und je Episode zurückgesetzt; ein
        häufiger Fehler beim Evaluieren rekurrenter Politiken wird damit
        vermieden.
  \item \textbf{Statistische Unsicherheit:} Bei 50 Seeds beträgt das
        95\,\%-Konfidenzintervall einer Erfolgsquote bis zu $\pm14$
        Prozentpunkte; Unterschiede unterhalb dieser Größe sind nicht
        signifikant zu interpretieren.
  \item \textbf{Rechenaufwand:} je voller Curriculum-Lauf ca.\ 8\,h (CPU) bei
        voller Taktung; die Läufe 4--7 liefen durch Energie-Drosselung
        (zugeklappter Laptop) 29--36\,h Wall-Clock, ohne Ergebnisrelevanz, da
        das Training step-basiert und das Curriculum leistungsbasiert ist.
        Trainingszeiten je Lauf in \texttt{results.json}.
  \item \textbf{Zufallsquellen:} Seed steuert Umgebungs-Generierung und
        -Reset (deterministisch); die stochastische Politik samplet Aktionen
        (torch-RNG). Restnicht-Determinismus (Thread-Scheduling) wird durch die
        Mittelung über sieben Läufe aufgefangen.
\end{itemize}

\section{Anhang: Entwicklungsverlauf von der Baseline zum LSTM-Agenten}
\label{sec:entwicklung}

Primärquelle der folgenden Chronologie ist das durchgängig geführte
Experiment-Changelog (\texttt{CHANGELOG.md}), in dem jede Änderung mit Problem,
Lösung und Messergebnis festgehalten wurde. Negative Ergebnisse sind bewusst
enthalten; sie haben die Architekturentscheidungen maßgeblich geprägt.

\subsection{Etappe 1 (Mai 2026): Baselines mit BFS-Orakel}

Nach Fertigstellung der Spiel-Engine (Mitte Mai) begann das RL-Training mit
Features aus dem BFS-Distanzfeld \emph{in der Observation} (5 Werte: aktuelle
Distanz + 4 Nachbarrichtungen), einem \glqq Orakel\grqq, das dem Agenten die
lokal richtige Richtung lieferte.

\textbf{Erster Anlauf mit DQN:} Der erste Agent (DQN) lernte nicht. Diagnose:
korrupter Replay-Buffer, 2-Zellen-Oszillationen, und eine Visited-Mask in der
Observation (455 Dimensionen), die sich jeden Schritt änderte und den
Q-Wert-Argmax destabilisierte. Konsequenz: Wechsel auf PPO, Visited-Mask
entfernt.

\textbf{BFS-Kraftfeld-Bug:} Ein Oracle-Test (greedy dem BFS-Feld folgen)
scheiterte in 3/3 Seeds; \texttt{bfsDistanceAt()} lieferte für Wand-Tiles
einen Manhattan-Fallback, wodurch Wände als attraktive Richtung erschienen.
Nach dem Fix (Wand $\to$ 9999): Oracle 3/3. Dieser Test wurde zum
Standard-Werkzeug, um Umgebung und Reward von Trainingsproblemen zu trennen.

\textbf{Curriculum-Lektion:} Phase-1-Training (Exit 5--12) erreichte 56\,\%,
aber der naive Transfer auf Phase 2 (12--25) kollabierte (22\,\% $\to$ 6\,\%);
die zuvor gelernte Kurzdistanz-Politik ließ sich nicht überschreiben.
Lösung: Mixed-Distribution-Training (Exit 5--45). Damit erreichte
\emph{Phase 3} 86\,\% stochastisch auf Testset A (drei unabhängige Läufe:
$85{,}3 \pm 3{,}8\,\%$ auf A, $79{,}3 \pm 8{,}1\,\%$ auf B) und \emph{Phase 4}
nach Reward-Tuning 98\,\% (A) / 94\,\% (B).

\textbf{Der deterministische Kollaps:} Trotz 98\,\% stochastischer SR lag die
Argmax-Auswertung bei 2\,\%. Die Analyse fand die Wurzel in der
Feature-Kodierung: Die BFS-Absolutwerte ($/64$) unterschieden sich zwischen
Nachbarzellen nur um $\approx 0{,}016$, zu wenig für eine robuste
Richtungspräferenz, und Wände waren durch Clamping nicht von \glqq weit
weg\grqq{} unterscheidbar (Aliasing). Der Wechsel auf
\emph{Delta-Kodierung} (Richtungssignal $\pm 0{,}5$ statt $0{,}016$, Wand
$= +1{,}0$) hob die deterministische SR auf 42\,\% (offene Welt) bzw.\
100\,\% (wanddichte Hard-World); Temperature-Sampling ($\tau = 0{,}2$)
erreichte 100\,\% bei $\varnothing$ 90 Schritten. Ein Experiment, exitDx/exitDy
zu entfernen (\glqq Phase 5\grqq), scheiterte (max.\ 40\,\%); der
Luftlinien-Kompass ist als Fernbereichssignal notwendig.

\subsection{Etappe 2 (Juni 2026): Kernbeitrag, Navigation ohne Orakel}

Mit funktionierender Baseline wurde die Forschungsfrage geschärft: \emph{Kann
der Agent auch ohne BFS-Orakel in der Observation navigieren?} Der Wechsel auf
RecurrentPPO (LSTM) mit Curriculum, Explorations-Bonus und step\_frac-Feature
(231-dim Obs, kein BFS) wurde am 07.06.2026 gestartet und lieferte nach der
Auswertung am Folgetag das zentrale Ergebnis:
\textbf{86\,\% stochastisch / 36\,\% deterministisch} auf Testset A und
68\,\% / 18\,\% auf Holdout B; damals waren beide Zielkriterien stochastisch erfüllt
(der final über sieben unabhängige Läufe reproduzierte Wert liegt bei
$65{,}7\,\%$/$66{,}9\,\%$ für A/B, Abschnitt~\ref{sec:endergebnis}),
erstmals ohne Wegweiser-Orakel (Abbildung~\ref{fig:entwicklung},
Ablation in Tabelle~\ref{tab:ablation}). Parallel wurde die POMDP-These
formuliert (Abschnitt~\ref{sec:ablation}) und, inspiriert vom
PokéRL-Projekt, Infrastruktur ergänzt: Seed-Pool (Swarm), WebSocket-Live-Map,
Heatmap-Evaluation, Early-Stop-Truncation.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_entwicklung.pdf}
\caption[Entwicklung über die Modellgenerationen]{Entwicklung über die Modellgenerationen (Testset A, Exit 35--45).
Links die MLP-Baselines mit BFS-Orakel in der Observation, rechts der
LSTM-Agent ohne Orakel.}
\label{fig:entwicklung}
\end{figure}

\textbf{Methodische Härtung (11.06.):} Ein Data-Leakage-Fehler wurde entdeckt
und behoben. Der Trainings-Callback hatte Modellauswahl und Phasenwechsel
auf den \emph{Test}-Seeds (7000er) gesteuert; seitdem existiert das getrennte
Validierungsset (6000er). Zusätzlich wurde die Lösbarkeit aller relevanten
Welten bewiesen (3\,150 Seeds, 100\,\%).

\subsection{Etappe 3 (13.--25.06.): Fehlkonfigurierte Hyperparameter und Root-Cause-Analyse}

\textbf{Symptom:} Eine Serie von Verbesserungsversuchen (v6--v9:
Wand-Penalty-Entfernung, visit\_count- und Action-Buffer-Features, diverse
Batch-/Epochen-Kombinationen) scheiterte durchgängig (SR 0--12\,\%,
\texttt{approx\_kl} $\approx$ 0).
\textbf{Root-Cause:} Die Aufklärung gelang durch \emph{Reverse-Engineering}
des letzten funktionierenden Modells (v2): Die zwischenzeitlichen
\glqq Optimierungen\grqq{} hatten unbemerkt die kritischen Hyperparameter
verstellt (Tabelle~\ref{tab:rootcause}); dominanter Faktor war
\texttt{ent\_coef} ($0{,}05 \to 0{,}01$: der Agent explorierte nicht mehr
genug, um den Exit je zu finden).
\textbf{Ergebnis:} Die v2-Reproduktion (v10) stellte das Lernen
wieder her; der abgeschlossene Lauf vom 25.06.\ erreichte 86\,\% (Validierung)
nach 2{,}2\,Mio.\ Steps in 3\,h\,12\,m; das aktuelle Referenzmodell
(Abbildung~\ref{fig:lernkurve}).

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lrrl@{}}
\toprule
\textbf{Parameter} & \textbf{v2 (86\,\%)} & \textbf{v7--v9 (0--12\,\%)} & \textbf{Wirkung des Fehlers} \\
\midrule
\texttt{ent\_coef} & 0.05 & 0.01 & zu wenig Exploration; Exit wird nie gefunden \\
\texttt{n\_steps} & 256 & 512 & BPTT zu lang $\to$ verschwindende Gradienten \\
\texttt{batch\_size} & 8 & 256 & zu wenige Gradient-Updates pro Rollout \\
Obs-Dimension & 231 & 249 & Zusatzfeatures ohne funktionierende Basis \\
\bottomrule
\end{tabular}
\caption[Root-Cause-Analyse der fehlgeschlagenen Läufe v7--v9]{Root-Cause-Analyse: \texttt{ent\_coef} erwies sich als dominanter
Faktor. Die Reproduktion der v2-Konfiguration (v10) stellte das Lernen wieder
her.}
\label{tab:rootcause}
\end{table}

\subsection{Etappe 4 (06.07.2026): Umgebungsrevision v11 und Performance}

Ein systematisches Audit (Umgebung, Reward, Trainingspipeline, Usability)
führte zu zehn dokumentierten Änderungen; Details in
Abschnitt~\ref{sec:v11} und Abschnitt~\ref{sec:performance}. Höhepunkte:
präzise Schwierigkeitssteuerung über BFS-Laufweg, Behebung eines
prozess-globalen Config-Lecks (das zuvor unbemerkt die
Phase-3-Trainingsverteilung verschob), Entfernung toter Features und
Straf-Terme, sowie die Wahl der CPU als Rechen-Device. Die an diesem Tag
ebenfalls vorgenommene Verdopplung der Trainingsgeschwindigkeit über
\texttt{batch\_size} 8 $\to$ 64 galt zunächst als validiert, wurde am Folgetag
jedoch widerlegt (Abschnitt~\ref{sec:etappe6}) und zurückgenommen.

\subsection{Etappe 5 (07.07.2026): Erster v11-Gesamtlauf, Monitoring-Redesign
und Seed-Replay-Analyse}
\label{sec:etappe5}

\textbf{Erster v11-Gesamtlauf (Seed 0, laufend).} Der erste vollständige
Curriculum-Lauf auf der revidierten v11-Umgebung
(\texttt{models/ppo\_lstm\_curriculum\_v11}) zeigt folgendes
Zwischenbild (Abbildung~\ref{fig:v11kurve}): Phase~1 endete am Step-Limit bei
42\,\% (stochastisch), deutlich langsamer als die Batch-Validierung
derselben Konfiguration (52\,\% bereits bei 150\,k). Die Diagnose im
Trainingslog: Die Policy-Updates sind durchgehend gesund
(\texttt{approx\_kl}\,$\approx$\,0.03, stabile Entropie), aber die
\texttt{explained\_variance} des Critics verharrt bei $\approx$\,0.1
(Validierungslauf: 0.72); der Lauf wurde zunächst als Kandidat für
eine ungünstige Zufallsinitialisierung eingestuft. \emph{(Nachtrag: Die systematische
Fehlersuche am selben Abend, Abschnitt~\ref{sec:etappe6}, fand die
tatsächliche Ursache: die Batch-Größe.)} Bemerkenswert: Direkt nach dem Wechsel in Phase~2
stieg die SR auf 48\,\% \emph{bei größerer Exit-Distanz}; die breitere
Aufgabenverteilung wirkt als Regularisierer.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_v11_lernkurve.pdf}
\caption[Lernkurve des ersten v11-Gesamtlaufs]{Lernkurve des ersten v11-Gesamtlaufs (Seed 0, Zwischenstand in Phase 3),
beide Betriebsarten. Vertikale Linien: Phasenwechsel; die x-Achse ist über die
Phasen kumuliert. Erzeugt mit \texttt{scripts/plot\_run\_curve.py}.}
\label{fig:v11kurve}
\end{figure}

\textbf{Live-Monitoring-Redesign.} Das WebSocket-Dashboard aus Etappe~2 wurde
nach Dashboard- und Farb-Literatur überarbeitet (Abbildung~\ref{fig:livemap}):
(a)~Charts auf uPlot mit EMA-Glättung nach TensorBoard-Vorbild;
(b)~die SR-Kurve zeigt det.\ und stoch.\ getrennt; der zentrale
Untersuchungsgegenstand war zuvor im Monitoring unsichtbar;
(c)~die Explorations-Heatmap wechselte von der Jet- auf die perzeptuell
uniforme Viridis-Skala (Jet erzeugt künstliche Kontraste und ist nicht
farbfehlsichten-tauglich); (d)~die Agenten-Minimaps rekonstruieren aus dem
gestreamten $15\times15$-Sichtfeld eine wachsende Weltkarte (Wände, Bäume,
Exit) statt nur der Agentenposition. Beim Umbau wurden zwei Fehler gefunden
und behoben: Der Exit-Richtungspfeil war seit der Obs-Umstellung auf 229
Dimensionen stumm defekt, und der SB3-Stepzähler springt beim
Phasenwechsel zurück (es wird das Bestmodell der Vorphase geladen), was
unkorrigiert alle stepbasierten Zeitachsen verfälscht.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{figures/fig_livemap.png}
\caption[Live-Monitoring-Dashboard]{Live-Monitoring (Demo mit Zufallsagenten): KPI-Zeile,
Weltkarten-Minimaps (grau = Wand, blaugrau = erkundeter Boden, grün =
Startpunkt) und uPlot-Charts. Im Training zusätzlich: goldene Raute =
gesichteter Exit, grüner Blitz bei Exit-Erfolg, Viridis-Heatmap.}
\label{fig:livemap}
\end{figure}

\textbf{Seed-Replay-Analyse (Swarm vs.\ PLR).} Das Training wiederholt seit
Etappe~2 bei 30\,\% der Episoden-Resets einen bereits \emph{gelösten} Seed
(\glqq Erfolgs-Swarm\grqq). Ein Literaturabgleich stellt diese Semantik in
Frage: Prioritized Level Replay \cite{jiang2021plr} zeigt, dass das Replay
von Levels mit \emph{hohem Lernpotenzial}, also gerade \emph{nicht}
gemeisterten, Sample-Effizienz und Test-Generalisierung verbessert;
das Wiederholen gemeisterter Levels ist der Fehlermodus, den PLR behebt,
und riskiert im Kontext des Projektziels (Generalisierung auf unbekannte
Seeds) Layout-Memorierung. Da der 86\,\%-Referenzlauf jedoch \emph{mit}
Erfolgs-Swarm entstand, wird die Frage nicht per Meinung, sondern als
A/B-Test entschieden (Erfolgs-Swarm vs.\ \texttt{-{}-plr} vs.\
\texttt{-{}-no-swarm}; Maßnahme B8 in \texttt{docs/BEWERTUNG\_UND\_PLAN.md}).
Ein dabei entdeckter Designfehler wurde sofort behoben: Der Seed-Pool
überlebte Phasenwechsel, obwohl ein \glqq gelöster\grqq{} Seed nach dem
Wechsel der Exit-Distanz eine andere Aufgabe beschreibt; der Pool wird
jetzt bei jedem Phasenstart geleert. \emph{(Der A/B-Test wurde noch am
selben Tag im Zuge der Fehlersuche durchgeführt, siehe
Abschnitt~\ref{sec:etappe6}: Der Erfolgs-Swarm hatte auf Phase~1 keinen
messbaren Effekt.)}

\subsection{Etappe 6 (07.07.2026, nachmittags/abends): Systematische
Fehlersuche vom Eval-Artefakt zur Batch-Größen-Korrektur}
\label{sec:etappe6}

Die Stagnation der v11-Läufe wurde am Nachmittag des 07.07.\ in einer
mehrstufigen, hypothesengetriebenen Fehlersuche aufgeklärt. Die folgende
Darstellung enthält auch die verworfenen Hypothesen, da erst deren Ausschluss
die jeweils nächste Maßnahme bestimmte. Zwei der Befunde, das Eval-Cap-Artefakt
und die Wirkung der Batch-Größe, haben Tragweite über das konkrete Problem
hinaus.

\textbf{Schritt 1: Messartefakt entdeckt, Eval-Episoden-Caps.}
Ausgangspunkt war ein Widerspruch: Der Vergleichslauf Seed~1 stagnierte in
Phase~1 bei 12--16\,\% (wie Seed~0), obwohl die Batch-Validierung derselben
Konfiguration 52\,\% erreicht hatte. Eine Nachmessung des besten
Seed-0-Checkpoints deckte die Ursache auf: Die am 06.07.\ zur
Eval-Beschleunigung eingeführten Episoden-Caps (Phase~1: 600 statt 4\,000
Schritte) unterschätzen die Kompetenz erheblich, weil erfolgreiche
LSTM-Episoden ohne Orakel lange Suchwege enthalten (Median 477, p95
$\approx$\,1\,900, Maximum 3\,239 Schritte). Dasselbe Modell misst sich bei
Cap 600 als 48-\%-Agent, bei Cap 4\,000 als 86-\%-Agent
(Abbildung~\ref{fig:capartefakt}). Damit war das 85-\%-Phasenziel unter Cap
600 strukturell unerreichbar, und die Bestmodell-Selektion bevorzugte
schnelle statt kompetente Politiken. Da die volle Messung nur
$\approx$\,21\,s kostet, wurden alle Caps auf 4\,000 (= Episoden-Limit der
Umgebung = finales Eval-Protokoll) zurückgesetzt. \emph{Lehre: Ein
Eval-Protokoll-Detail (Cap) kann Gating, Modellselektion und
Fortschrittswahrnehmung gleichzeitig verfälschen.}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{figures/fig_cap_artefakt.pdf}
\caption[Eval-Cap-Artefakt]{Erfolgsquote \emph{desselben Modells} (bester Phase-1-Checkpoint, Seed 0)
in Abhängigkeit des Episodenlimits der Evaluation, 50 Val-Seeds. Erzeugt mit
\texttt{scripts/plot\_etappe6\_figures.py}.}
\label{fig:capartefakt}
\end{figure}

\textbf{Schritt 2: Neustart unter korrekter Messung, Stagnation ist real.}
Seed~1 wurde mit korrigierten Caps neu gestartet. Ergebnis: weiterhin
Stagnation (16--28\,\% über 300\,k Schritte, \texttt{explained\_variance}
$\approx$\,0.1); der zweite stagnierende Seed in Folge. Damit war ein
systematisches Problem belegt und eine Verdächtigenliste abzuarbeiten.

\textbf{Schritt 3: Hypothesen nacheinander eliminiert (A/B-Läufe, Seed
fixiert).} Drei Komponenten des Trainings-Stacks wurden durch kontrollierte
Vergleichsläufe mit identischem Seed geprüft:
\begin{itemize}
  \item \emph{Wand-/Loop-Penalties (Maßnahme B7)?} Entlastet ohne neuen
        Lauf: Die Batch-Validierung lief bereits auf der penalty-freien
        v11-Umgebung und lernte.
  \item \emph{Erfolgs-Swarm (Maßnahme B8)?} Ein \texttt{-{}-no-swarm}-Arm
        verlief deckungsgleich mit dem Kontrollarm; entlastet.
  \item \emph{Streaming-Wrapper des Live-Monitorings?} Code-Inspektion
        (rein lesende Zugriffe) und ein Lauf ohne Wrapper: ebenfalls
        deckungsgleich; entlastet. (Nebenbefund behoben: Der Wrapper
        wurde bisher auch bei deaktivierter Live Map angelegt.)
\end{itemize}
Parallel dazu wurde die Batch-Validierung vom 06.07.\ \emph{exakt
reproduziert} (nackter RecurrentPPO ohne Curriculum-Stack, 150\,k Schritte,
batch=64): Sie erreichte am Endpunkt fast dieselben Werte wie damals
(48\,\%/22\,\% vs.\ 52\,\%/22\,\%); der \emph{Verlauf} dazwischen
schwankte stark und unregelmäßig (74 $\to$ 44 $\to$ 66 $\to$ 24 $\to$ 50\,\%,
Abbildung~\ref{fig:batchvgl} oben). Zusätzlich wurde ausgeschlossen, dass
sich Curriculum-Pfad und nackter Pfad überhaupt funktional unterscheiden:
Gewichtsinitialisierung, Weltseed-Folgen und die ersten 8\,192
Trainingsschritte (Rewards, Aktionen, Values) sind zwischen beiden
\emph{bit-identisch}.

\textbf{Schritt 4: Diagnose, hochvariable Lerndynamik statt defekter
Code.} Die Gesamtschau aller Läufe ergab: Mit batch=64 ist die Lerndynamik
hochvolatil (SR-Sprünge zwischen 10 und 74\,\% ohne Konvergenz), der Critic
erreicht nur vereinzelte, nicht anhaltende
\texttt{explained\_variance}-Hochphasen. Die \glqq Validierung\grqq{} der
Batch-Größe am 06.07.\ beruhte auf einem günstigen Einzel-Messpunkt dieses
Prozesses: einem Messwert aus einer stark schwankenden Kurve, nicht auf deren Verlauf.
\emph{Lehre: Hyperparameter-Entscheidungen brauchen Eval-Kurven, keine
Endpunkte.}

\textbf{Schritt 5: Hypothesentest Batch-Größe, bestätigt.} Der einzige
verbliebene Unterschied zum funktionierenden v2/v10-Rezept war
\texttt{batch\_size}=64 statt 8 (bei batch=8 erfolgen pro Rollout
$\approx$\,8-mal mehr Gradientenschritte auf denselben Daten; die
v10-Reproduktion hatte damit EV bis 0.94 erreicht). Ein ansonsten
identischer Lauf mit batch=8 zeigte ein qualitativ anderes Bild
(Abbildung~\ref{fig:batchvgl}): stetiger Anstieg auf \textbf{84--88\,\%
stochastische SR} bei 125--150\,k Schritten, die besten
Phase-1-Werte des Projekts, und erstmals über \textbf{50\,\%
deterministische} SR in Phase~1 (batch=64: nie über 26\,\%). Ab
$\approx$\,100\,k Schritten hält der Critic anhaltende EV-Hochphasen (26\,\%
der Iterationen $>$\,0.5, gegenüber 3\,\% bei batch=64).
Konsequenz: \texttt{batch\_size} wurde auf 8 zurückgesetzt; der
Geschwindigkeitsvorteil von batch=64 (Faktor 2,1) war mit instabilem Lernen
erkauft und damit wertlos.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_batch_vergleich.pdf}
\caption[Batch-Größen-Forensik]{Batch-Größen-Forensik (alle Läufe Seed 1, Phase-1-Setup, 50 Val-Seeds,
Limit 4\,000). Oben: stochastische Erfolgsquote. Unten:
\texttt{explained\_variance} des Critics, EMA-geglättet mit blassen Rohkurven.
Erzeugt mit \texttt{scripts/plot\_etappe6\_figures.py}.}
\label{fig:batchvgl}
\end{figure}

\textbf{Ergebnis der Etappe.} Die v11-Stagnation hatte zwei unabhängige,
sich überlagernde Ursachen: ein \emph{Messartefakt} (Eval-Caps) und eine
\emph{echte Regression} (batch=64). Trainings-Stack, Seed-Replay, Wrapper
und Umgebungsrevision wurden dabei experimentell entlastet, ein für die
weitere Arbeit wertvolles Nebenergebnis, da die v11-Umgebung damit als
methodisch sauber bestätigt ist. Die Konfiguration für die finalen
Mehrfach-Läufe steht damit fest (v11-Umgebung, batch=8, Cap-4000-Evals,
kurvenbasierte Bewertung).

\subsection{Umgebungsrevision v11 (06.07.2026)}
\label{sec:v11}

Eine systematische Überprüfung der Umgebung gegen Best Practices des
Environment- und Reward-Designs führte zu zehn dokumentierten Änderungen. Die
folgenden sieben betreffen Umgebung, Reward und Evaluation; die übrigen drei
(Device- und Batch-Benchmark, Konsistenz- und Usability-Bereinigung) sind in
Abschnitt~\ref{sec:performance} beschrieben.

\begin{enumerate}
  \item \textbf{Tote Features entfernt:} Energie und Inventar waren konstant;
        Observation 231 $\to$ 229 Dimensionen (Tabelle~\ref{tab:obs}).
  \item \textbf{Straf-Stacking entschärft:} Wand-Penalty entfernt, Loop-Penalty
        $-0.15 \to -0.05$ (Tabelle~\ref{tab:reward}).
  \item \textbf{Exit-Platzierung nach BFS-Laufweg:} Zuvor wurden Kandidaten nach
        \emph{Luftlinie} gewählt; der reale Laufweg bei nominell
        \glqq 35--45\grqq{} streute auf 42--75 Tiles ($\varnothing$ 55). Nach dem
        Fix liegen alle 150 Eval-Seeds exakt im Sollbereich
        (Abbildung~\ref{fig:exitdist}); das Curriculum steuert die Schwierigkeit
        damit erstmals präzise.
  \item \textbf{Prozess-globales Config-Leck behoben:} Die
        Weltgenerierungs-Konfiguration ist im C++-Kern global; parallel
        existierende Umgebungen überschrieben sich gegenseitig (u.\,a.\ verschob
        der Eval-Callback die Phase-3-Trainingsverteilung unbemerkt). Jede
        Umgebung stempelt ihre Konfiguration nun bei jedem Reset neu.
  \item \textbf{Mining/Bauen/Kampf aus dem RL-Pfad entfernt} (Binding erlaubt nur
        noch Aktionen 0--3; tote Reward-Terme gestrichen).
  \item \textbf{Entropie-Annealing korrigiert:} stetig $0.05 \to 0.001$ statt
        Sprung $0.05 \to 0.01$ beim Phasenwechsel.
  \item \textbf{Eval-Callback:} phasengerechtes Gating
        (Tabelle~\ref{tab:curriculum}), Episoden-Caps pro Phase, Det+Stoch-Doppelmessung.
\end{enumerate}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{figures/fig_exit_distanz.pdf}
\caption[Wirkung der BFS-basierten Exit-Platzierung]{Wirkung der BFS-basierten Exit-Platzierung: tatsächliche Laufweg-Distanzen
(min/$\varnothing$/max über je 50 Seeds) vor und nach dem Fix.}
\label{fig:exitdist}
\end{figure}

Alle Änderungen wurden einzeln verifiziert (Lösbarkeit 150/150, Oracle-Agent
10/10 mit exakt BFS-Distanz Schritten, Obs-Layout-Konsistenz, Ablehnung
unzulässiger Aktionen). Da sich die Aufgabenverteilung dadurch ändert, sind
Ergebnisse ab v11 als neue Baseline zu behandeln.

\subsection{Performance-Analyse: Training beschleunigen}
\label{sec:performance}

\subsubsection{CPU schlägt Apple-GPU (MPS)}

Sämtliche Läufe dieser Arbeit entstanden auf einem Notebook der Apple-M-Serie
(M1 Pro, 8 Performance-Kerne, macOS arm64); als Rechen-Device stand damit neben
der CPU die integrierte GPU über das MPS-Backend zur Verfügung. Ein
kontrollierter Benchmark (identisches Trainings-Setup, 8192 Steps) zeigt:
Die GPU ist für dieses kleine Netz (LSTM 256, 229 Inputs) in \emph{jeder}
Konfiguration langsamer als die CPU; der Kernel-Dispatch-Overhead pro
Mini-Update übersteigt den Rechengewinn (Abbildung~\ref{fig:fps}). Dies deckt
sich mit der Empfehlung der SB3-Dokumentation, PPO ohne CNN-Politik primär auf
der CPU zu betreiben \cite{sb3docs}.

\begin{figure}[h]
\centering
\includegraphics[width=0.72\textwidth]{figures/fig_fps_benchmark.pdf}
\caption[Training-FPS nach Device und Batch-Größe]{Training-FPS nach Device und Batch-Größe (RecurrentPPO, 16 Umgebungen,
Apple M-Serie). MPS wurde für batch $\geq 128$ nicht weiter gemessen, da bereits
bei 64 unterlegen.}
\label{fig:fps}
\end{figure}

\subsubsection{Batch-Größe: vermeintliche Validierung (06.07.), am Folgetag widerlegt}
\label{sec:batchsize}

Mit \texttt{batch\_size}=8 fallen pro Rollout 5\,120 Gradient-Updates an,
mehr Optimizer-Schritte als Umgebungsschritte. Da die Root-Cause-Analyse
(Tabelle~\ref{tab:rootcause}) die Batch-Größe nie isoliert getestet hatte, wurde
ein Validierungslauf durchgeführt: 150k Phase-1-Steps mit \texttt{batch\_size}=64
(Abbildung~\ref{fig:batchval}). Ergebnis: gesunde Lernmetriken
(\texttt{approx\_kl} 0.01--0.06, \texttt{explained\_variance} bis 0.72) und
\textbf{52\,\% stochastische / 22\,\% deterministische SR} nach nur 150k Steps,
bei \textbf{187 statt 88 FPS}. \texttt{batch\_size}=64 wurde daraufhin
übernommen.

\textbf{Diese Schlussfolgerung hielt der Überprüfung nicht stand.} Die
Forensik vom 07.07.\ (Abschnitt~\ref{sec:etappe6}) zeigte per exakter
Reproduktion, dass der Validierungslauf ein günstiger Einzel-Schnappschuss
einer stark schwankenden Lerndynamik war: Der Endpunkt (52\,\%) war
reproduzierbar, der Verlauf dorthin sprang jedoch regellos zwischen 24 und
74\,\%, und in vollen Curriculum-Läufen stagnierte batch=64 bei
16--28\,\%. Mit \texttt{batch\_size}=8 lernt dieselbe Konfiguration stetig
auf 84--88\,\% (Abbildung~\ref{fig:batchvgl}). Die Batch-Größe \emph{war}
also doch ein kritischer v2-Erfolgsfaktor; sie wurde auf 8 zurückgesetzt.
Die Lehre daraus: \emph{Einzel-Messpunkte volatiler Prozesse validieren
nichts.}

\begin{figure}[h]
\centering
\includegraphics[width=0.72\textwidth]{figures/fig_batch64_validation.pdf}
\caption[Validierungslauf batch\_size=64]{Validierungslauf \texttt{batch\_size}=64 (Phase-1-Setup, Env v11), Stand
06.07.: \texttt{approx\_kl} und \texttt{explained\_variance} über der Zeit. Zur
späteren Widerlegung dieses Befunds siehe Abschnitt~\ref{sec:etappe6}.}
\label{fig:batchval}
\end{figure}

\subsection{Zeitleiste und Lessons Learned}

Tabelle~\ref{tab:timeline} fasst den Verlauf der vorangegangenen Abschnitte
chronologisch zusammen; die anschließende Liste hält die übertragbaren
Erkenntnisse fest.

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lp{7.6cm}l@{}}
\toprule
\textbf{Datum} & \textbf{Meilenstein} & \textbf{Ergebnis (Testset A)} \\
\midrule
12.05. & Spiel-Engine \& Weltgenerierung spielbar & -- \\
16.05. & DQN-Fehlstart $\to$ PPO; BFS-Kraftfeld-Bug gefixt & Oracle 0/3 $\to$ 3/3 \\
16.05. & Curriculum P1; naiver Transfer kollabiert $\to$ Mixed-Dist. & 56\,\% $\to$ 86\,\% stoch \\
16.05. & Phase 4 (Reward-Tuning), 3-Läufe-Protokoll & 98\,\% stoch / 2\,\% det \\
18.05. & Delta-BFS-Kodierung + Temperature-Sampling & 100\,\% stoch / 42\,\% det \\
07./08.06. & \textbf{RecurrentPPO ohne BFS-Orakel (Kernbeitrag)}; Lauf gestartet 07.06., ausgewertet 08.06. & \textbf{86\,\% / 36\,\%} \\
08.06. & POMDP-These; Callback-Bugs; Swarm/Live-Map & -- \\
11.06. & Data-Leakage behoben (Val-Set 6000er); Lösbarkeit 100\,\% & -- \\
13.--15.06. & v6--v9 scheitern $\to$ Root-Cause (ent\_coef/batch/n\_steps) & 0--12\,\% \\
25.06. & v10-Reproduktion abgeschlossen (Referenzmodell) & 86\,\% (Val) \\
06.07. & Env-Revision v11 (10 Änderungen); batch=64 \glqq validiert\grqq{} (am 07.07.\ widerlegt) & 52\,\% stoch @150k (P1) \\
07.07. & v11-Läufe Seed 0/1 stagnieren; Monitoring-Redesign; Swarm-Analyse (B8) + Pool-Fix & 16--28\,\% stoch (P1, Val) \\
07.07. & \textbf{Forensik (Etappe 6):} Eval-Cap-Artefakt gefixt; Swarm/Wrapper/Stack entlastet; \textbf{batch $64 \to 8$} & \textbf{84--88\,\% stoch / 52\,\% det} (P1, Val) \\
08.07. & \textbf{v12-Läufe Seeds 1--3 (batch=8)}; Gap-Experimente E1--E3 + Temperatur-Sweep & 73{,}3\,\% / 80{,}0\,\% (A/B, stoch, $n=3$) \\
17.07. & \textbf{Aufstockung auf $n=7$} (Seeds 4--7); Alternativerklärungen ausgeschlossen & \textbf{65{,}7\,\% / 66{,}9\,\%} (A/B, stoch, $n=7$) \\
\bottomrule
\end{tabular}
\caption[Zeitleiste der RL-Entwicklung]{Zeitleiste der RL-Entwicklung (Auswahl; vollständig in \texttt{CHANGELOG.md}).
SR-Angaben je nach Etappe mit unterschiedlichem Protokoll; Werte vor dem 11.06.\
ohne strikte Val/Test-Trennung.}
\label{tab:timeline}
\end{table}

\textbf{Lessons Learned (negative Ergebnisse mit Erkenntniswert):}
\begin{itemize}
  \item \emph{Visited-Mask in der Observation} destabilisiert den Argmax
        (jede 1-Bit-Änderung kann die Richtungspräferenz kippen).
  \item \emph{Naiver Curriculum-Transfer} zerstört die Vorgänger-Politik;
        Mixed-Distribution bzw.\ leistungsbasierte Phasenwechsel sind nötig.
  \item \emph{Feature-Kodierung schlägt Feature-Auswahl:} Der BFS-Gradient war
        nie \glqq zu schwach\grqq, sondern falsch kodiert (Delta-Fix: Signal
        $32\times$ stärker).
  \item \emph{Straf-Stacking} (Wand-, Loop-, Stagnations-Penalties) macht
        Stillstand lokal optimal; Information gehört in die Observation, nicht
        in den Reward.
  \item \emph{Hyperparameter dominieren Features:} Alle v7-Feature-Ideen
        scheiterten an verstellten Basis-Hyperparametern; \texttt{ent\_coef}
        war der entscheidende Faktor. Ohne das Changelog wäre die
        Root-Cause-Analyse (v2-Reverse-Engineering) nicht möglich gewesen.
  \item \emph{Eval-Protokoll ist Teil des Experiments:} Data-Leakage über den
        Callback und das Gating auf einer signal-losen Metrik (det.\ SR bei
        hoher Entropie) verzerrten Selektion und Laufzeit unbemerkt.
  \item \emph{Eval-Episoden-Caps verfälschen die Messung:} Dasselbe Modell
        misst sich bei Cap 600 als 48-\%-, bei Cap 4000 als 86-\%-Agent;
        eine als harmlos gedachte Beschleunigung machte das Phasenziel
        unerreichbar (Abschnitt~\ref{sec:etappe6}).
  \item \emph{Einzel-Snapshots sind keine Validierung:} Die batch=64-Freigabe
        beruhte auf einem Messpunkt einer stark schwankenden Kurve;
        die exakte Reproduktion traf den Endpunkt, zeigte aber, dass der
        Verlauf dazwischen volatil war. Hyperparameter-Entscheidungen nur auf ganzen Eval-Kurven.
  \item \emph{Negative Kontrollen lohnen sich:} Der bit-genaue Vergleich von
        Curriculum-Pfad und nacktem Trainingspfad schloss eine ganze
        Verdächtigen-Klasse (Stack-Bugs) in Minuten aus und lenkte die Suche
        auf die Hyperparameter.
\end{itemize}

\clearpage
\addcontentsline{toc}{section}{Literaturverzeichnis}
\bibliographystyle{unsrt}
\bibliography{references}

\end{document}
